# STATO DEL PROGETTO (checkpoint — aggiornato a ogni turno)

**Ultimo aggiornamento:** 2026-09-08 — **sessione `arena/01a08232-football-deep-analyzer`** (da `main` `8f44c05`). **Turno 1 — fix xPTS FotMob (obiettivo unico di sessione)**: in `season_xg()` (`src/fda/site/analysis.py`) il fallback FotMob (`if not self.info.empty`) ora calcola **xPTS e punti reali dalle partite finite**: per ogni gara finita della squadra le λ = xG di casa/trasferta (`home_xg`/`away_xg` in `match_info`) → P(V casa), P(N), P(V trasferta) con `scipy.stats.poisson`, xPTS casa = 3·P(V casa)+P(N) e xPTS trasferta = 3·P(V trasferta)+P(N); punti reali 3/1/0 dai gol reali. Nuovo metodo statico `_poisson_xpts(lh, la)` → `(xPTS casa, xPTS trasferta)`; nel return del fallback `"xpts": round(xpts_total, 1), "pts": pts_total` al posto di `None` (resta `None` — riga xPTS assente come prima — solo se mancano i gol reali). Import `numpy as np` e `from scipy.stats import poisson` in cima al file; scipy è già dipendenza di `penaltyblog` (presente in CI con `pip install -e ".[dev]"`), `pyproject.toml` non toccato. Nuovo test `test_season_xg_fotmob_calcola_xpts` (4 gare finite → `pts=7`, `xpts=5.9`; metodo statico verificato contro griglia Poisson indipendente su 7 coppie di λ, simmetria a λ uguali < 1e-6, degradazione `None` senza gol). Verifiche (offline, sandbox): suite **47 passed** (era 46); ruff `--select F,E` su `analysis.py` **76 = baseline 78 → 0 nuove** (solo E501 pre-esistenti; 2 in meno grazie ai refolw), `tests/test_site.py` invariato; `fda build` sui dati reali del repo (141 pagine): **nessun «nan»** (le 67 occorrenze della stringa sono sottostringhe di parole: «segnano», «Fernandes», «vicinanze»…), tutte le **94 righe «Stagione (FotMob…)» ora mostrano «· xPTS … vs … punti reali»** (prima mai: xpts era None) e il narrativo «rispetto agli xPTS» scatta su squadre NED1/POR1 su dati reali (es. Fortuna Sittard +6,4 · AZ Alkmaar +4,3 · FC Utrecht −3,8). Nota: **la «regola permanente sez. F» in `docs/00_regole_di_lavoro.md` non esiste** (verificato: il file finisce alla sezione E sia su questo branch sia su `origin/main`) → non toccata, da chiarire con l'utente. Stato: commit `d9d1258` pushato, **PR #14 aperta verso `main`** (in attesa di merge utente secondo policy sez. D).

**Sessione precedente (dettaglio storico, `arena/01a08176`):** 2026-09-08 (**sessione `arena/01a08176-football-deep-analyzer`**, da `main` `5ef4290` = post-merge PR #10 + commit dati run 14:32. **Turno 1 — hotfix ricostruito**: il commit `394759d` («hotfix max_backfill + pipefail + test», 41 passed) della sessione precedente non fu mai pushato ed è **perso col sandbox** — verificato: assente da tutti i ref locali, remoti e `refs/pull/*`, clone fresco con reflog solo clone+checkout, `fsck` senza oggetti pendenti. **Diagnosi con prove sui dati del run**: il run `daily` dispatch `34238795120` (14:31 UTC, head `02eda11`, primo dopo il merge PR #10) è FALLITO allo step «Prepara il sito per Pages» (exit 2, `deploy` saltato) perché il collect è morto su NED1 nel nuovo backfill 2b: `source_status` ferma a FRA1 14:32:29 (nessuna riga NED1/POR1), `fotmob_standings` solo big-5 (96 righe), 0 partite storiche scaricate, 12 partite NED1 in `match_info` tutte della finestra. Root cause: in `_fetch()` (`collect.py`) `bundle_to_dicts` + `store.upsert` stavano **fuori da `_safe`** → una partita storica anomala uccideva l'intero run; aggravante: pipe `fda daily | tee | tail` **senza `pipefail`** → step «Run giornaliero» ✅ falso e commit dati comunque eseguito. Verifiche locali sui dati del run (`5ef4290`): `fda build` riesce (141 pagine → il problema era nel collect, non nel build), suite **40 passed** (= main pre-hotfix, coerente col «41» = 40+test nuovo). **Fix**: `_save()` dentro `_safe` come `fotmob save <id>` (partita guasta isolata e visibile in `source_status`, il run prosegue) + `set -euo pipefail` nello step «Run giornaliero» di `daily.yml` + test di regressione offline `test_collect_survives_bad_match_save` (crash simulato sul salvataggio di una partita: il run completa, l'altra partita è salvata). **Turno 2 — audit + card «Confronto di stagione»**: audit misurato sulle **86 gare da giocare entro 10 gg** (dati `5ef4290`): classifica presente 100% big-5 / **0% NED1-POR1** (run morto prima del passo 2c → si sblocca col solo merge hotfix + 1 run); arbitro e meteo **0% su tutte** (FotMob li pubblica a T-24/48 h → segnaposto onesti); xG3 via partite FotMob 0% ovunque (big-5 coperte da Understat; NED1/POR1 dal backfill post-hotfix); formazioni: ENG1 0% (pubblicate a ridosso), altre leghe 5-23%. **Nuova card pre-partita «Confronto di stagione»** su tutte le 7 leghe: tabella lato-a-lato (posizione, punti, punti/gara, V-N-P, GF/GC per gara, DR) + righe derivate **Attacco/Difesa × media campionato** (dal GF e GC per gara della lega intera, stessa tabella) con il valore migliore evidenziato; degrada con «—» se una squadra non è in tabella, scompare se nessuna delle due; sostituisce la vecchia riga «Classifica: X° con Y punti». Refactor `h2h` in `_h2h_core`/`h2h_list`/`h2h_stats` + nuova sintesi sui precedenti (**gol/gara e % entrambe a segno** sui 5 mostrati) + segnaposto onesti «Arbitro da definire» / «previsione pubblicata a ridosso della gara» quando mancano in pre-partita. Suite **42 passed** (+`test_season_compare`), ruff 0 nuove (confronto senza numeri di riga: output identico). Verificato anche su dati reali (`fda build`, es. Venezia–Fiorentina: attacco 0,49× vs 0,24× media lega). Nota tool confermata: 3 edit fantasma («success» senza applicazione) — riverifica con grep sempre. **Turno 3 — Monte Carlo di stagione (fase 2 roadmap)**: nuovo modulo `models/season_sim.py` — per lega: fit DC+Elo su storico datahub+correnti (helper `build_hist` estratto e riusato da `fda predict`, fonte unica), probabilità 1X2 per ogni gara restante (ensemble 70/30), poi N stagioni simulate campionando i **punti dalla griglia DC completa** (ρ compreso → tie-break su differenza reti simulata); squadre senza storico DC (neopromosse) simulate con λ neutre di lega (nota esplicita sul sito). Output tabella `season_sim` (chiave lega+squadra): punti attesi, media posizione, P titolo / top-4 / retro (regole per lega: POR1=2, NED1=3 con playoff, altre=3). Nuovo comando `fda simulate [--sims]` + integrato in `fda daily` tra predict e build (fallimento isolato, il sito esce comunque); pagina **`stagione.html` «Proiezioni»** (nav aggiornata; tabella per lega: PG, Pt, Pt attesi, media pos., % 1°, % Top-4, % Retro, virgole decimali; segnaposto onesto finché `season_sim` è vuota). Verifiche: **44 passed** (+2 test simulazione: invarianti somme 1/4/3 e determinismo a seed fisso; squadra-neopromossa senza storico); ruff **0 nuove** (season_sim e test puliti; noqa mirati B008/BLE001 su cli, pattern pre-esistente). Da sandbox: `fda simulate` degrada pulito senza mirror («storico insufficiente», saltata per lega); **primo giro reale in Actions dopo il merge** (stima runtime: fit ~1s/lega + sampling vettorizzato ~10k×300 gare ≈ pochi secondi/lega). Prossimo: Prossimo: merge utente PR #11 → dispatch ×2 (backfill NED1/POR1 ~69 gare a 40/run) → `scripts/verify_standings.py --ref origin/main`; poi candidata: **meteo previsionale Open-Meteo** (fonte gratuita senza chiave, previsioni ~16 gg: riempirebbe il vuoto meteo 0% su TUTTE le future; richiede client nuovo + verifica live in Actions). Sessione precedente (`arena/01a08157`, PR #10): (nuova sessione su branch `arena/01a08157-football-deep-analyzer`, da `main` `3a4303f` = merge PR #9; `main` allineato a `origin/main`, worktree pulito. **Turno 1 — setup + diagnosi**: ambiente installato (`pip install -e ".[dev]"`, PyPI ok), suite **29 passed**, ruff **47 segnalazioni = baseline pre-esistente** (nessuna nuova). Diagnosi parità schede **rimisurata sui dati locali**: nessun file `espn_*` (classifica assente ovunque), Understat solo 5 leghe a 2,0–4,1 gare/squadra, fallback FotMob con 67/230 finite (→ ~1 gara su NED1/POR1). Creato `docs/04_parita_schede.md` con disegno Fix A (classifica da endpoint FotMob `leagues` → nuova tabella `fotmob_standings`) e Fix B (xG stagione NED1/POR1: opzione risposta `leagues` o matchDetails di tutte le finite). Prossimo: fixture + parser + test offline (Fix A). **Turno 2 — mappatura endpoint `leagues`**: da `pseudo-r/Public-FotMob-API` (VERIFIED) + `tommhe14/fotmob-wrapper` via api.github.com — risposta con `details`, `table[].data.table.all[]` e righe `{idx,name,id,played,wins,draws,losses,scoresStr,goalConDiff,pts}`; il wrapper usa `/data/leagues` (stesso base_url nostro, senza x-mas) e `/data/tltable` come endpoint dedicato (fallback candidato, struttura ignota). Divergenza lista-vs-dict sul nodo `table` → parser difensivo. Nessun xG di squadra nella risposta → Fix B = opzione 2 (matchDetails di tutte le finite nelle leghe senza Understat, endpoint già verificati). Tutto annotato in `docs/04_parita_schede.md`; formato reale da confermare in Actions. **Turno 3 — parser tabella FotMob**: `FotMobStandingRow` + `parse_league_table()` in `fotmob.py` (gestisce nodo `table` lista e dict, `scoresStr`→gol, fallback differenza reti, righe senza id scartate, ordinamento per `idx`; `league_raw()` con `season` opzionale) + 2 fixture campione + 2 test. Suite **31 passed**, ruff: 0 nuove segnalazioni sui file toccati (6 pre-esistenti invariate). Prossimo: merge utente della PR parità-schede → dispatch manuale `daily` dalla UI → verifica dati live. **Turno 4 — Fix A integrato**: passo 2b in `collect_league` (tabella `fotmob_standings`, chiave lega+squadra, errore esplicito se tabella vuota/inattesa così resta visibile in `source_status`; TTL `leagues: 6` in config), `analysis.standing()` FotMob-prima/ESPN-riserva (template invariato), nuovo comando `fda fotmob-table` per debug dal PC. Test: collect offline con tabella (Inter 1°/9pt), isolamento guasto `leagues`, preferenza FotMob + fallback ESPN in `standing`. Suite **33 passed**, ruff **47 = baseline esatta** (0 nuove; nota: `typer.Argument("lit")` non attiva B008, solo la forma `None` — tolto `noqa` superfluo). Lezione tool: un edit su due in parallelo a volte non si applica nonostante «success» — ora riverifico sempre con grep. **Turno 5 — Fix B integrato**: passo 2b in `collect_league` (backfill finite fuori finestra solo per leghe senza Understat, recenti prima, tetto `--max-backfill=40`/run, fetch estratto in `_fetch()`); contatore `matches_backfilled` + display «N+M storiche» in `fda collect`. Test: NED1 1+1 recuperi (0 al rerun), ITA1 invariata, `season_xg()` FotMob 3,5/1,5 su 2 finite. Suite **36 passed**, ruff **47 = baseline**. Recuperato anche l'edit §3 di `docs/04` perso al turno 2 (riverifica sistematica). Fix A+B completi offline; resta la verifica dal vivo in Actions. **Turno 6 — PR**: assert e2e rendering «Classifica: 1° con 9 punti» (36 passed, ruff 47 = baseline), workflow `tests` verde sul branch, diff pulito (13 file, no dati); **PR #10 aperta** verso `main` (check `test` SUCCESS, CLEAN — merge all'utente). **Turno 7 — in parallelo al merge utente**: script `scripts/verify_standings.py` (tabelle 7/7 leghe con controlli rank/punti/giocate, copertura xG finite per lega con soglia backfill NED1/POR1, `source_status` FotMob; exit 1 se FAIL) + 4 test funzioni pure. Suite **40 passed**, file nuovi ruff-puliti. Script già provato su `origin/main`: riporta correttamente «parquet assente» pre-merge. Nota: in `verify_ned_por.py` resta un RUF100 pre-esistente (noqa S603/S607, regole non abilitate) — da pulire nel turno ruff.)

## Fatto
- [x] **Processo/handoff — policy «merge lo esegue SEMPRE l'utente»** (decisione utente 2026-09-08, riapplicata in questa sessione perché il commit originale `9d39a21` non era mai stato pushato): sez. D di `00_regole_di_lavoro.md` riscritta (dopo la frase fissa l'agente si ferma e aspetta, non mergea mai), avviso ⚠️ in cima a `BRIEFING_NUOVA_SESSIONE.md`, decisione registrata in «Decisioni aperte». Doc-only; test 23 passed, nessuna nuova segnalazione ruff (baseline: vedi Nota). **PR #9 aperta verso `main`**: check `test` **Success** (49s), mergeable **CLEAN** — in attesa del merge dell'utente (secondo la nuova policy).
- [x] **Processo/handoff — paletti di qualità + briefing nuove sessioni** (fatto, **PR #6 mergiata in `main` `16db091`**): riscritta la sezione B (paletti di qualità: accuratezza/precisione/profondità/qualità), aggiunta la sezione D (policy esplicita su **quando fare Create PR e quando Merge**, con frasi fisse e checklist), e creato `docs/BRIEFING_NUOVA_SESSIONE.md` (porta d'ingresso con architettura, stato, prossimi passi, mappa file e limitazioni, così un agente nuovo non ripete verifiche già fatte). Doc-only, check tests pass (42s).
- [x] Studio di fattibilità e catalogo fonti (`01`, `02`) — commit `ac35f2b`
- [x] Decisioni utente registrate (7 campionati, automazione giornaliera, uso personale, quote sì, solo fonti GitHub Actions) — `03_decisioni_e_funzionamento.md`
- [x] Regole anti-blocco chat — `00_regole_di_lavoro.md`
- [x] Verifiche tecniche: penaltyblog OK (nota pandas 3), datahub CSV OK, FotMob id 7 leghe OK, matchDetails pre-partita ricco (formazione probabile, meteo, arbitro)
- [x] **Passo 1 — scaffolding**: `pyproject.toml`, `src/fda/` (config, http client con cache+rate limit+retry, cli), `config/leagues.yaml` (7 leghe + coppe), `config/sources.yaml` (budget richieste), `tests/` (6 test verdi), `.gitignore`, `data/README.md`. Comandi: `fda leagues`, `fda version`.

- [x] **Passo 2 — client FotMob** (`src/fda/sources/fotmob.py`): `fixtures`, `matchDetails`, `teams`, `playerData`, `matches?date`; parser → record piatti (MatchInfo con arbitro/stadio/meteo/xG/H2H, Shot, TeamMatchStat per periodo, PlayerMatchStat, LineupPlayer con indisponibili+rientro, eventi, momentum). Test su JSON campione (9 verdi). Comandi `fda fotmob-fixtures ITA1`, `fda fotmob-match <id>`.

- [x] **Passo 3 — Understat + ESPN**: `sources/understat.py` (endpoint JSON con cookie+XHR, senza soccerdata/seleniumbase; partite con xG, righe squadra-partita con xPTS/PPDA/deep, giocatori; `team_season_table`) e `sources/espn.py` (scoreboard con statistiche, classifiche, boxscore, arbitri). 11 test verdi. Comandi `fda understat-table ITA1`, `fda espn-today ITA1`.

- [x] **Passo 4 — storage + collect**: `store.py` (Parquet versionati in `data/processed/` + viste DuckDB, upsert per chiave, tipi UTC stabili) e `collect.py` (per lega: calendario → dettagli partite oggi ±3 gg, finite una sola volta, future a ogni run → Understat → ESPN; fonti isolate, esiti in `source_status`). 13 test verdi (incluso un run completo offline). Comandi `fda collect [ITA1 ...]`, `fda db [SQL]`.

- [x] **Passo 5 — modelli**: `sources/history.py` (CSV storici datahub/football-data → schema unico, quote di chiusura se presenti), `teams.py` (nomi canonici tra fonti, ~300 alias per le 7 leghe), `models/predict.py` (Dixon-Coles pesato nel tempo + Elo + ensemble 70/30 con griglia coerente; mercati 1X2, risultati esatti, O/U, BTTS, doppia chance, clean sheet; RPS). Backtest onesto Serie A 2025/26 (train 300 → test 80): RPS ensemble 0,212 vs Elo 0,218 vs naive 0,244. 18 test verdi. Comando `fda predict [ITA1 ...]`.

- [x] **Passo 6 — sito + automazione**: `site/analysis.py` (contesto analitico e frasi in italiano da regole esplicite), template Jinja2 (Oggi, Prossime, Risultati, pagina partita pre/post, Accuratezza con RPS/Brier, Stato fonti), `site/build.py`, comandi `fda build` e `fda daily` (collect → predict → build). Workflow `.github/workflows/daily.yml` (cron 5 volte al giorno + manuale, commit dei Parquet, deploy Pages) e `tests.yml`. 19 test verdi.
- [x] **Passo 6b — primo run dal vivo**: su `main`, run `daily` #2 (2026-09-06, come da handoff) concluso **Success** in 3m17s: collettori → modelli → build sito → commit dati → deploy GitHub Pages. Artefatto `github-pages` di 132 KB; Pages usa source = **GitHub Actions**. Restano solo gli avvisi non bloccanti sulla deprecazione Node.js 20.
- [x] **Correzione PR #2**: merge su `main` nel commit `968f2d7`; lo step di installazione di `daily.yml` usa `pip install -e ".[dev]"`, così `pytest` è disponibile nel workflow.
- [x] **PR #3 mergiata su `main`** nel commit `3359fdc` (workflow con versioni Node 24).
- [x] **Run `daily` #3 su `main` verificato** (id `33999756428`, head `3359fdc`): job `run` **Success** 3m08s + `deploy` **Success** 10s; commit dati `43b848c` pushato; artefatti `github-pages` (135358 B) e `run-log-3`; deployment Pages riuscito. `predictions` 48 → **96** righe (ITA1 20, ENG1 18, ESP1 22, GER1 18, FRA1 18), probabilità coerenti e senza null, tutte gare `scheduled`. FotMob ok 7/7; Understat per le 5 leghe coperte; ESPN standings ancora **403 isolato** (non blocca). Suite locale 20 passed. (Blob di log Actions e pagina Pages non raggiungibili dal sandbox — verifica su metadati/commit/dati.)

## In corso
- [x] **Passo 7a — resilienza del run**: ESPN standings e scoreboard isolati; `fda predict` isola errori/storici per lega. Commit `d8063c1` + `0bcde50`; suite locale 20 passed; workflow `tests`: Success.
- **Passo 7b — storico NED1/POR1 (avviato)**: il mirror `datasets/football-datasets` non copre Eredivisie/Liga Portugal e `football-data.co.uk` è irraggiungibile dagli IP cloud (root cause: nei run #2/#3 NED1/POR1 avevano partite in programma ma 0 predizioni). Risolto puntando NED1 (`eredivisie`) e POR1 (`primeira-liga`) a un mirror `raw.githubusercontent.com` dedicato con la stessa struttura `season-XXXX.csv` (campo per-lega `datahub_base`), + alias squadra alle grafie N1/P1 (`For Sittard`→`Fortuna Sittard`, `Estrela`→`Estrela da Amadora`). Commit `85b515f`.
- **Verifica passo 7b**: suite locale **23 passed** (3 nuovi test); simulazione offline con CSV reali del mirror + calendario FotMob → NED1 **11** e POR1 **9** predizioni (tutte le partite in programma; le promosse ADO Den Haag/Cambuur/Académico Viseu/Marítimo ricevono prior dagli esiti di stagione in corso via FotMob). Workflow `tests` sul branch e check PR: **Success**.
- **PR #4 mergiata su `main`** nel commit `a3c5bf7` (merge eseguito dall'agente, precedente PR #2; tests verdi sul branch e mergeable CLEAN). `main` ora contiene lo storico NED1/POR1 da mirror dedicato.
- **Accuratezza live con gare reali (confermato)**: build del sito sui dati `main` (commit `783e59a`, 2364 fixture) → la pagina `accuratezza.html` ora mostra valutazioni reali pre-partita: **19 partite valutate** su 5 leghe, RPS **0,205** complessivo vs naive 0,244 (Bundesliga 2, LaLiga 6, Ligue 1 3, Premier 2, Serie A 6). NED1/POR1 ancora assenti perché i dati su `main` precedono il merge.
- **✅ Verifica live NED1/POR1 CONFERMATA (2026-09-08 13:33–13:40 UTC, verificato dal vivo)**: dopo l'attesa dei cron dilazionati (slot 10:00 mai partito), l'utente ha avviato manualmente il run `daily` dalla UI GitHub (Actions → daily → Run workflow su `main`): run **`34232722943`** (workflow_dispatch, 13:33:43Z, head `91f7109` post-merge) → job `run` **Success** in 1m33s (tutti gli step: test rapidi, collect→predict→build, commit dati, preparazione Pages) + job `deploy` **Success**. Commit dati **`6a31d39`** (13:35 UTC) verificato con `scripts/verify_ned_por.py`: **NED1 = 11 predizioni** (esattamente le ~11 simulate offline) e **POR1 = 11** (vs 9 simulate: da allora il calendario FotMob ha datato più gare di Liga Portugal, copertura maggiore), tutte con probabilità complete e `n_train=960` → **lo storico del mirror dedicato è stato scaricato e usato** (prima del merge: 0 predizioni per assenza di storico — le predizioni stesse sono la prova, più forte del log). Predizioni sensate (es. PSV 0,83 vs Sparta, Benfica 0,70 a Moreirense 09/09, Porto 0,73 a Casa Pia) e **alias mirror funzionanti** («Fortuna Sittard», «Estrela da Amadora»). `source_status`: FotMob OK 7/7, Understat OK (incl. NED1/POR1), unico errore il noto **ESPN standings 403** isolato (pre-esistente, non blocca). Limitazione: artifact/blob dei log Actions non raggiungibili dal sandbox (Azure) — verifica fatta su dati del commit, come per i run precedenti. Inclusa la gara NED1 di oggi NEC–Excelsior (16:45) → le prime valutazioni reali NED1/POR1 arriveranno in Accuratezza tra 1–6 giorni. Verificato sui dati live (`fixtures.parquet`, ultimo run): il calendario FotMob ha già gare NED1 **oggi** (12:00 UTC+, es. FC Utrecht–Go Ahead Eagles, NEC Nijmegen–Excelsior) e POR1 **da domani** (es. Moreirense–Benfica 19:45 UTC) → il primo `daily` post-merge avrà partite NED1/POR1 da predire: **la conferma dovrebbe arrivare entro poche ore/giorno, non serve altro de-risking offline** (simulazione già fatta: NED1 11 / POR1 9). ESPN standings: 403 isolato anche su NED1/POR1 nell'ultimo run (non blocca).
- **Dopo la conferma live (ora in corso)**: report pre/post in italiano già rifiniti su branch (PR #9: Accuratezza Δ/calibrazione, cartina tiri, momentum, precedenti, italianizzazione) — resta da seguire la risoluzione delle gare previste (incluse le 22 NED1/POR1 di questa settimana) che alimenterà Accuratezza con le prime valutazioni su Eredivisie/Liga Portugal.
- **Rifinitura Accuratezza + fix report partita (su branch, in PR #9 — verificato offline con build locale sui dati del repo)**: pagina `accuratezza.html` rinnovata: colonna **Δ vs naive** (su 19 gare il modello batte la base in ogni lega: Tutti **−0,038**; da −0,025 Serie A a −0,064 Premier), nuova card **Calibrazione** (prob. media prevista vs frequenza osservata: 1 = 42,4% vs 21,1% · X = 24,9% vs 36,8% · 2 = 32,6% vs 42,1% → nel campione attuale il modello sovrastima le vittorie in casa; n=19, dato indicativo) e per singola gara colonna **Top (✓/✗)** + **RPS**. Fix nei report partita (`match.html`): data in italiano con ora locale («domenica 06/09/2026, 15:30 (ora italiana)» — prima weekday inglese + etichetta «UTC» fuorviante) e spettatori formattati «57.000» (prima «57.000.0»); nuovi filtri Jinja `it_dt`/`it_num` in `build.py`. Suite **23 passed** (nuovi assert: Δ, Calibrazione, ✓, RPS 0,087 della previsione 62/21/17, data italiana, spettatori); **ruff: zero nuove segnalazioni** vs baseline (confronto diretto prima/dopo sui file toccati). Note: la pagina live su Pages si aggiornerà al prossimo `daily` su `main`.
- **Cartina dei tiri nei report post-partita (su branch, in PR #9 — verificato offline)**: nuova card «Cartina dei tiri» con due mezzi campi SVG (uno per squadra, porta a destra), pallini dimensionati sull'xG del tiro, colori per esito (verde gol / blu in porta / arancione tratteggiato bloccato / grigio fuori), tooltip con giocatore·minuto·xG e riepilogo «17 tiri · xG 3,06 · 5 gol». Dati: tabella `shots` (coordinate FotMob in metri 105×68, verificate: x max 104,3 / y mediana 34 = centro porta). `analysis.py`: `shot_map()` (trasformazione metri→px con clamp) + `goals` in `shot_summary`; template puro Jinja, nessun JS. Verifiche: **23 passed** (assert: 2 SVG, «xG 0,88» di Lautaro col decimale italiano, pannello vuoto Monza), build locale: **132/132 SVG ben formati** (parse XML), coordinate dei 5 gol di Mainz tutte dentro l'area e coerenti con l'xG (Mwene 0,89 a 6 m dalla porta); rendering PNG non possibile dal sandbox (niente cairo) — la verifica è strutturale/numerica, il primo `daily` su `main` darà la conferma visiva su Pages.
- **Italianizzazione completa del sito (su branch, in PR #9 — verificato offline con audit automatico su 146 pagine)**: richiamo utente «il sito è italiano e usiamo l'ora italiana». (1) **Ora italiana ovunque**: eliminati i 3 residui «UTC» (header di ogni pagina, timestamp della previsione, footer) → nuovo filtro `it_utc` (conversione UTC→Europe/Rome, gestisce anche ora solare/legale); orari delle liste già convertiti, ora dichiarati «(ora italiana)». (2) **Decimali con virgola**: nuovo filtro `dec` applicato a tutte le ~30 formattazioni dei template (xG, quote, gol attesi, Elo/DC, RPS/Brier/Δ in Accuratezza, λ nelle liste, rating) e `_f()` del narrativo («4,1 ammonizioni», «3,12 gol attesi»); fix «10.0 rigori in 24.0 gare» → interi. (3) **Meteo tradotto**: mappa `_WEATHER_IT` con gestione varianti combinate FotMob «Partly Cloudy/Wind» → «parzialmente nuvoloso e ventoso» (valori reali verificati sui dati: Mostly Clear, Fair, Showers in the Vicinity…). (4) **Date di rientro tradotte**: `_return_it()` («Mid October 2026» → «metà ottobre 2026»; catalogo completo dei valori FotMob su 437 indisponibili: Day to day, Doubtful, About 1-2 weeks, Out for season…). Verifiche: **29 passed** (6 nuovi unit test `test_i18n.py` su filtri e traduttori, inclusa conversione ora legale +2 / solare +1); **ruff 0 nuove segnalazioni**; audit automatico su tutte le 146 pagine generate: **0 residui** di UTC / weekday inglesi / meteo inglese / rientri inglesi / decimali con punto.
- **Momentum + ultimi precedenti nei report (su branch, in PR #9 — verificato offline)**: (1) nuova card **«Momentum della partita»** (solo post-partita): grafico SVG a barre minuto per minuto (verde = pressione casa, rosso = trasferta, scala FotMob ±100) con **marker dei gol** presi dalla cronaca e frase di sintesi quando una squadra domina («Momentum a favore di Mainz nel 66% dei minuti» su Hamburger 0-5 Mainz — coerente col risultato); dati: tabella `momentum` già raccolta (6134 righe, 67 partite), orientamento verificato empiricamente (valori fortemente negativi ai 5 gol di Mainz in trasferta). (2) **«Ultimi precedenti»** nel card Contesto (pre e post): fino a 5 incontri reali da tabella `h2h` (1554 righe dal 2009) con data, risultato, campionato e pill V/N/P **dal punto di vista della casa attuale** — bug di inversione V/P trovato e corretto in verifica (Roma–Atalanta: tutte P come atteso), poi inchiodato da test con riga non-pari (il campione aveva solo un 1-1); nomi risolti dalle due squadre in campo (le righe h2h coinvolgono solo loro), righe anomale scartate. Verifiche: **29 passed**; build locale: **198/198 SVG ben formati** (132 cartine + 66 momentum); ruff 0 nuove; audit italiano 0 residui. Nota: **diffidati non implementati** — fonte FotMob non li espone nei dati raccolti (ruoli lineup: starter/sub/unavailable/coach); da valutare fonte alternativa in futuro.

## Nota
- **Preview del sito dal sandbox** (scoperta 2026-09-08): dopo `fda build` (che scrive in `site/`, gitignored), si può servire con `python3 -m http.server 3000 --bind 0.0.0.0 --directory site` via start_process → l'utente vede il sito modificato nel browser (preview Arena) prima del merge. Il sito ufficiale è invece `uamisjd.github.io/football-deep-analyzer` (Pages), aggiornato solo dai run `daily` su `main`.
- Dal sandbox dell'agente la rete verso FotMob, `football-data.co.uk` e `raw.githubusercontent.com` può essere bloccata (si raggiungono solo `github.com` e `api.github.com`): le verifiche di rete vanno fatte in GitHub Actions o sul PC. I parser sono coperti dai test offline. (2026-09-08: PyPI risulta invece raggiungibile dal sandbox, quindi l'installazione dell'ambiente di test locale funziona.)
- Il mirror dedicato NED1/POR1 è configurato solo in `config/leagues.yaml` (campo per-lega `datahub_base`): facile da cambiare se il mirror diventasse irraggiungibile.
- Le quote The Odds API sono opzionali: vengono usate solo se `ODDS_API_KEY` è configurata.
- Baseline ruff (rilevata 2026-09-08, ruff 0.16.6 con vincolo `ruff>=0.5`): **47 segnalazioni pre-esistenti su `main`** (B008 `typer.Argument`, UP, RUF, BLE, I) — nessuna introdotta da questo turno (modifiche doc-only); la CI `tests` esegue solo pytest, quindi non blocca. Da valutare un turno di pulizia dedicato (la regola B.4 richiede ruff pulito sul codice **nuovo**).

## Prossimo passo (Fase 0/7 — un turno per riga)
1. ~~Scaffolding repo~~ ✅
2. ~~Client FotMob~~ ✅
3. ~~Client ESPN + Understat~~ ✅
4. ~~Storage + collect~~ ✅
5. ~~Modelli DC + Elo~~ ✅
6. ~~Sito statico + workflow + Pages~~ ✅
6b. ~~Primo run dal vivo in Actions e correzioni ai collettori~~ ✅
7. ~~Upgrade Node 24~~ ✅
7b. ~~storico NED1/POR1~~ ✅ **confermato dal vivo** (run `34232722943` del 2026-09-08 13:33Z, NED1 11 / POR1 11 predizioni, `n_train=960`, commit dati `6a31d39`). Prossimo: seguire la risoluzione delle gare previste (prime valutazioni NED1/POR1 in Accuratezza) e proseguire la roadmap.
7c. ~~Processo/handoff~~ ✅ (PR #6 in `main` `16db091`). Conferma live NED1/POR1 ✅ (2026-09-08). Prossimo: **Monte Carlo stagione** (fase 2: probabilità titolo/top-4/retrocessione per lega) o rifiniture report in base alle gare che si risolvono.
8. **Per la prossima sessione** (handoff 2026-09-08): (a) **🎯 PRIORITÀ UTENTE — parità di profondità delle schede partita**: «tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; schede di ogni partita ancora da giocare piene di contenuti, NED1/POR1 attualmente scarse». Diagnosi già misurata sui dati del run `6a31d39`: **classifica assente su TUTTE le leghe** (`espn_standings` vuota per il 403 cronico ESPN → fix = tabella di lega FotMob come fonte primaria) e **stagione xG con 1 sola gara su NED1/POR1** (Understat non le copre; fix = xG di stagione da FotMob); arbitro talvolta assente fino a pochi giorni dalla gara (si riempie da sola). Dettagli e lead tecnici in `BRIEFING_NUOVA_SESSIONE.md` sez. 3; (b) **Monte Carlo stagione** — fase 2 della roadmap `01` §8; (c) **Accuratezza**: seguire le gare NED1/POR1 in programma questa settimana (prime valutazioni Eredivisie/Liga Portugal); (d) opzionale: **pulizia ruff** (47 segnalazioni pre-esistenti, CI non bloccata).

## Decisioni aperte
- **Qualità delle schede partita** (direttiva utente 2026-09-08, **prioritaria**): tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; le schede di ogni partita da giocare devono essere piene di contenuti e servizi, con **parità completa tra le 7 leghe** (NED1/POR1 segnalate scarse). Primo intervento: classifica da FotMob (ESPN standings 403 cronico) + xG di stagione per NED1/POR1 (Understat non le copre). Registrata anche in `BRIEFING_NUOVA_SESSIONE.md` sez. 3 e 5.
- **Merge delle PR** (deciso 2026-09-08): **il merge lo esegue SEMPRE l'utente, MAI l'agente**. L'agente apre la PR quando serve (sez. D di `00_regole_di_lavoro.md`), monitora i check e avvisa con la frase fissa «👉 Tutto verde: è il momento di fare Merge (PR #N).», poi aspetta senza fare merge. (La decisione era stata registrata nel commit locale `9d39a21` della sessione precedente, mai pushato e andato perso alla chiusura → riapplicata sui docs in questa sessione.)
- **Fonte storica NED1/POR1** (deciso 2026-09-06): mirror GitHub `raw.githubusercontent.com` di football-data.co.uk dedicato (via `datahub_base` per-lega), scelto perché il mirror datahub copre solo 5 leghe e `football-data.co.uk` diretto è irraggiungibile dagli IP cloud.
- Nessuna altra bloccante. (Telegram bot e protezione accesso: fasi successive.)
