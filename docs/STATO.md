# STATO DEL PROGETTO (checkpoint — aggiornato a ogni turno)

**Ultimo aggiornamento:** 2026-09-19 (sessione `arena/01a0b61a`, quarantunesimo giro) — **P2.2 applicata: le assenze in un posto solo, e la parità fra le schede pre-partita sotto gate (`docs/35`).**

- **Cosa cambia.** La frase dell'analisi elencava fino a quattro indisponibili (129 nomi in totale sulle 60 schede): le stesse persone erano nella tabella dell'infermeria con minuti, gol+assist, xG+xA per 90, motivo e rientro — cioè con più informazione. Adesso la frase tiene il *peso* (quanti, quanti titolari abituali) e manda alla tabella col link **→ Infermeria**, che atterra sull'infermeria della squadra giusta (`#infermeria-home` / `-away`, ancore nuove). L'avviso «il migliore della lista è indisponibile» non ripete più motivo e rientro. **A gara finita i nomi restano nella frase**, perché lì la tabella non c'è (la fonte riporta le assenze una volta su due e la pagina non deve fingere di saperlo).
- **La parità è un gate.** «Clima del club» esisteva su 59 schede su 60 (Alverca–Rio Ave la perdeva quando nessuna delle due squadre aveva segnali): era l'unica differenza di struttura, e nessun controllo guardava le schede l'una contro l'altra. Ora `scripts/parita_schede.py` confronta le schede pre-partita fra loro — stessi id di card, stesso indice, nessuna sezione sotto un quarto della sua mediana di peso, nessuna scheda sotto il 75% della mediana del testo visibile — ed esce 1 se una si discosta; gira **anche in CI** (`.github/workflows/daily.yml`, dopo `verify_site`, prima del commit dei dati).
- **Misura (60 schede pre-partita, `938c42a` → codice attuale).** Nomi elencati nella frase **129 → 0** (mediana 2, max 3); frasi con il link **0 → 60/60**; frase 96 → 90 caratteri (mediana); «Clima del club» **59/60 → 60/60**; struttura identica **59/60 → 60/60**; indice 12 voci in tutte. **Bilancio della sessione** (base `2d3cf27` → oggi, stesse 60 schede): testo visibile mediano **22.905 → 18.987 (−17,1%)**, tutte in calo.
- **Il gate ha trovato un difetto introdotto da me:** il link all'infermeria era agganciato alla sola frase, e su 369 pagine `verify_site` è uscito 1 con «ancora interna mancante #infermeria-away» — a gara finita la tabella non esiste. Corretto con la stessa condizione del template, e la regressione è fissata da un test sugli `href` della narrativa.
- **Gate [offline, sandbox].** `pytest -q` **443 passed**; `ruff` pulito; `fda build` exit 0 (369/2.364/7.480); `scripts/verify_site.py` **0 problemi · 100.020 controlli** (invariante [21] aggiornata: card clima su ogni scheda pre-partita + righe «clima normale» contate); `scripts/parita_schede.py` **0 differenze**; `audit_match_sections.py` exit 0.
- **Anteprima.** `site_preview/` (porta 8000) con **dodici pannelli** prima/dopo — il «prima» è la build del commit `2d3cf27`, l'ultima prima di questa sessione — e il bilancio dei sette interventi.
- **Prossimo passo.** `docs/28` §3: **P2.3** (micro-visivi nelle card di solo testo: barra del percentile, quartili del primo gol, sparkline delle fasce storiche), poi P2.5 (badge forma nell'hero), P2.6 (quote assenti = decisione). Ogni intervento va rimisurato con `prematch_sections` **e** `parita_schede`.

**Ultimo aggiornamento:** 2026-09-19 (sessione `arena/01a0b61a`, quarantesimo giro) — **P2.4 applicata: «Contesto» diviso in «Arbitro e meteo» e «Precedenti» (`docs/34`).**

- **Cosa cambia.** La card «Contesto» teneva insieme tre cose senza niente in comune: la designazione con i suoi numeri, la previsione meteo e la storia della sfida. Ora sono due card con due titoli veri — `#arbitro-meteo` «Arbitro e meteo» (due righe) e `#precedenti` «Precedenti» (bilancio, grafico a ciambella, ultimi incontri, frequenze) — e l'indice ha una voce per ciascuna (11 → 12 voci in una scheda pre-partita). La riga della tabella non ripete più il titolo: «Precedenti (15)» diventa **«Bilancio (15)»**, il numero dei casi su cui si regge la lettura delle frequenze (ed è il numero che l'invariante [5] di `verify_site` ricalcola dall'archivio). Il link «→ precedenti» dei «Fatti rilevanti» punta a `#precedenti`.
- **Misura (60 schede pre-partita presenti in entrambe le build, `700fd84` → codice attuale).** Card 1 → 2; «Contesto» **2 → 0** occorrenze per pagina (max 2 → 0); voci d'indice 11 → 12; etichette che ripetevano il titolo 58 → 0 (e 0 → 58 «Bilancio (N)»); distanza dalla voce d'indice ai precedenti **171 → 0** caratteri visibili; costo **+15 caratteri** visibili (il titolo in più). I precedenti erano l'**83%** del blocco (805 caratteri su 966), non «oltre metà» come stimava docs/28.
- **Gate [offline, sandbox].** `pytest -q` **442 passed** (+1 `test_arbitro_meteo_e_precedenti_card_separate`); `ruff` pulito; `fda build` exit 0 (369/2.364/7.480); `scripts/verify_site.py` **0 problemi · 99.960 controlli** ([33] 3.290 voci d'indice); `audit_match_sections.py` exit 0. Il calo dei controlli totali (da 100.987) è dovuto al sito più piccolo: **369 schede partita** invece di 375 — sei gare sono diventate «giocate» fra le due build, non è un allentamento.
- **Anteprima.** `site_preview/` (porta 8000) con **otto pannelli** prima/dopo: P1.1, P1.2 (hero e «Come arrivano»), P1.4, P1.3, P2.1, la prova che con notizie vere la card non cambia, e il nuovo pannello 7 (card «Contesto» intera a sinistra, le due card a destra, con la coda dell'indice nelle due versioni).
- **Prossimo passo.** `docs/28` §3: **P2.2** (le assenze raccontate in quattro modi diversi: infermeria, analisi, badge «giocherà?», riga di sintesi), poi P2.3 (micro-visivi), P2.5 (badge forma nell'hero), P2.6 (quote assenti = decisione). Un intervento per turno, misura prima/dopo, push immediato; una sola PR a fine sessione (docs/00 §D).

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentanovesimo giro) — **P2.1 applicata: la stima stabilizzata si spiega una volta sola (`docs/33`).**

- **Cosa cambia.** La spiegazione dei marcatori ◎/◇ (media dei pari, peso k, soglia 270′/90′) compariva **4 volte per scheda**: nella nota «Soglia di minutaggio» di ogni card squadra e nell'infermeria. Ora sta **una volta sola**, nella testata di «I giocatori che decidono» (primo punto d'uso), con i marcatori scritti in prosa; le card squadra tengono solo il dato; il tooltip di ogni valore stabilizzato dichiara il **caso specifico** (media dei pari, peso k=, n=); nell'infermeria il totale perde la parentesi «(stima stabilizzata)» e prende il marcatore ◎. Le sorgenti citate 8-14 volte restano: sono una decisione (docs/20 §3).
- **Misura (66 schede pre, due build dello stesso codice, `000f60f` → build corrente).** «stabilizzat» **4 → 1** per scheda (max 4 → 1), «stima stabilizzata» idem; testo visibile **−111 caratteri** in mediana appaiata (73–111; 66/66); mediana visibile per scheda 19.839 → **19.752**. Il vantaggio dichiarato non è la lunghezza: è che la stessa frase non si legge quattro volte.
- **Il gate ha corretto la mia prima stesura:** la legenda con `<b>◇</b>` isolato faceva scattare **[32]** su 66 pagine («cella ◇/◎ senza numero pubblicato») — la regola è giusta (un marcatore seguito subito dal tag di chiusura è indistinguibile da un valore mancante), la legenda è stata riscritta in prosa.
- **Gate [offline, sandbox].** `pytest -q` **441 passed** (+1 `test_legenda_stabilizzata_una_volta_sola`); `ruff` pulito; `fda build` exit 0 (375/2.364/7.480); `scripts/verify_site.py` **0 problemi · 100.987 controlli**; `audit_match_sections.py` exit 0. Non verificato (serve un browser): resa di legenda e tooltip → P2.8 di `docs/19`.
- **Anteprima.** `site_preview/` (porta 8000) con **sette pannelli** prima/dopo: P1.1, P1.2 (hero e «Come arrivano»), P1.4, P1.3, P2.1 e la prova che con notizie vere la card non è cambiata.
- **Prossimo passo.** `docs/28` §3: **P2.4** (separare «Contesto»: arbitro, meteo e precedenti in una card sola, con la parola che compare due volte nella pagina), poi **P2.2** (assenze raccontate in quattro modi), P2.3, P2.5, P2.6. Un intervento per turno, misura prima/dopo, push immediato; una sola PR a fine sessione (docs/00 §D).

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentottesimo giro) — **P1.3 applicata: l'indice della scheda dice i titoli veri; coda P1 di `docs/28` §4 esaurita (`docs/32`).**

- **Cosa cambia.** La barra in cima alla scheda aveva 4 voci e ne azzeccava una («Dati e contesto» atterrava su «Confronto di stagione», «Squadre» sul nome di una squadra, «Post-partita» su «Il prossimo impegno»). Ora le voci sono quelle che la pagina ha davvero, in ordine di pagina e con le stesse guardie delle sezioni: **11 pre-partita** (Analisi · Previsione · Scontro tattico · Come arrivano · I giocatori · Le due squadre · Panchina · Mercato · Vita del club · Contesto · Verifica) e **7 post-partita**; ogni etichetta è l'inizio del titolo della sezione che apre. Ancore aggiunte a «Fatti rilevanti», «Come arrivano», «I giocatori che decidono», «Contesto», «Statistiche», «Cronaca essenziale»; titolo al gruppo delle due squadre; il gruppo delle card del club passa da `#contesto` a `#club` e l'id `#contesto` va alla card che ha quel nome (il link «→ precedenti» ora atterra sui precedenti).
- **Misura (66 schede pre + 12 post, due build dello stesso codice, `ad6f9f9` → build corrente).** Voci dell'indice 4 → 11 (pre) e 3 → 7 (post); voci che dicono il titolo vero **66/264 → 726/726** (pre) e **0/36 → 84/84** (post); card con un'ancora in indice, per scheda, **1 → 9** su 20 (pre) e 1 → 6 (post); **le sei card più pesanti da 0/6 a 6/6 raggiungibili**. Costo dichiarato: **+97 caratteri** di testo visibile per scheda (l'indice è testo nuovo). Il censimento delle card non cambia (20 card-foglia, stesse sezioni e ridondanze).
- **Invariante nuova [33]** (`scripts/verify_site.py`), in entrambe le direzioni su tutte le 375 schede: ogni voce deve puntare a una sezione presente e dirne l'inizio del titolo; una sezione pesante presente ma fuori dall'indice fa fallire il gate. `[33] voci dell'indice della scheda partita verificate: 2.987` → i controlli del gate salgono da 97.903 a **100.890**.
- **Gate [offline, sandbox].** `pytest -q` **440 passed** (+1 `test_indice_della_scheda_dice_i_titoli_veri`); `ruff` pulito; `fda build` exit 0 (375/2.364/7.480); `scripts/verify_site.py` **0 problemi · 100.890 controlli**; `audit_match_sections.py` exit 0. Un test esistente adattato (non il prodotto): il taglio della card «Confronto di stagione» in `test_site_build_end_to_end` usava la parola «Contesto» come confine, che ora compare anche nella barra → usa l'ancora `id="contesto"`. Non verificato (serve un browser): resa della barra con 11 voci → resta P2.8 di `docs/19`.
- **Bilancio del turno (P1.1 + P1.2 + P1.4 + P1.3).** Testo visibile per scheda pre-partita: **22.520 → 18.807 caratteri (−16,5%)**; card «Vita del club» 3.030 → 829 visibili quando non c'è nulla; dato di stagione ripetuto in 3 → 1 riquadri; verifica dei numeri chiusa (−1.767); indice 4 → 11 voci tutte vere. Anteprima con **sei pannelli** prima/dopo in `site_preview/` (fuori dal versionamento, porta 8000).
- **Prossimo passo.** Coda **P1 esaurita**: restano i **P2** di `docs/28` §3 in ordine di peso — P2.1 (legenda unica delle stime stabilizzate), P2.4 (separare «Contesto»), P2.2 (assenze in un posto solo), P2.3, P2.5, P2.6. Un intervento per turno, misura prima/dopo, push immediato; una sola PR a fine sessione (docs/00 §D).

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentasettesimo giro) — **P1.4 applicata: «Verifica approfondita» chiusa e annunciata (`docs/31`).**

- **Cosa cambia.** Il `<details>` con matrice dei punteggi e distribuzione dei gol non ha più `open` e il summary porta i due numeri di testa («moda X gol · mediana Y · per chi vuole controllare i numeri»). Il contenuto **resta nel DOM**. Lo script di `base.html` che riapriva la tendina sopra i 760 px è sostituito: ora **apre da sola la tendina che contiene il bersaglio di un'ancora** (il link «matrice completa ↓» atterrava su una riga chiusa). Il segno ▸/▾ è una regola CSS sola con la tendina di P1.1.
- **Misura (66 schede pre, due build dello stesso codice).** Card: **1.886 → 119 caratteri visibili** (9,2% → 0,6% della pagina, 7,9% in tendina); DOM 1.886 → 1.911 (i due numeri nel summary). Scheda intera: **−1.767 caratteri visibili in mediana appaiata** (1.758–1.782; 66/66). Testo visibile mediano per scheda **20.577 → 18.807 (−8,6%)**. `verify_site` **97.903 controlli, 0 problemi** — invariato: il contenuto c'è, solo chiuso.
- **Gate [offline, sandbox].** `pytest -q` **439 passed** (+1 `test_verifica_approfondita_chiusa_e_annunciata`); `ruff` pulito; `fda build` exit 0 (375/2.364/7.480); `scripts/verify_site.py` 0 problemi · 97.903 controlli; `audit_match_sections.py` exit 0. Non verificato (serve un browser): segno ▸/▾, spaziatura della card chiusa e comportamento dello script all'ancora → resta P2.8 di `docs/19`.
- **Anteprima.** `site_preview/` (fuori dal versionamento, porta 8000) ha ora **cinque pannelli** prima/dopo: P1.1, hero e «Come arrivano» (P1.2), verifica dei numeri (P1.4) e la prova che con notizie vere la card non è cambiata.
- **Prossimo passo.** Ultimo P1 della coda (`docs/28` §4): **P1.3** (nav con le ancore reali), poi i P2 di §3. Un intervento per turno, misura prima/dopo, push immediato; una sola PR a fine sessione (docs/00 §D).

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentaseiesimo giro) — **P1.2 applicata: un dato di stagione, un posto (`docs/30`); anteprima prima/dopo servita in locale.**

- **Cosa cambia.** L'hero non ripete più xG/gara e PPDA (restano esito, λ, Over 2,5, «entrambe a segno»); «Come arrivano» perde la riga di sintesi (medie di stagione e punti contro xPTS, già nella card della squadra) e il PPDA, e al loro posto rimanda a card squadra e «Scontro tattico»; resta la serie gara per gara con la tendenza e lo split casa/trasferta. **Nessun numero esce dalla pagina**: cambia dove sta — card squadra (valori di stagione) e «Scontro tattico» (confronto di stile).
- **Misura (66 schede pre, due build dello stesso codice, `c581a81` → build corrente).** Il dato di stagione si ripete in **mediana 1 altro riquadro** (prima **3**); occorrenze di ripetizione **1.039 → 489**; riga xG/PPDA nell'hero 66/66 → **0**; sintesi in «Come arrivano» 66/66 → **0** (PPDA in quella card 48/66 → 0); **−357 caratteri visibili per scheda** appaiati (188–376, tutte e 66; il calo è bimodale: −119 con una sola squadra con sintesi, −286 con due). Lo strumento ora conta i valori canonici della card squadra (hero incluso), così prima e dopo si misurano con la stessa definizione.
- **Gate [offline, sandbox].** `pytest -q` **438 passed** (+1 `test_xg_e_ppda_una_volta_sola`); `ruff` pulito; `fda build` exit 0 (375/2.364/7.480); `scripts/verify_site.py` **0 problemi · 97.903 controlli**; `audit_match_sections.py` exit 0. Non verificato (serve un browser): resa visiva e spaziature → resta P2.8 di `docs/19`.
- **Anteprima.** `site_preview/` (fuori dal versionamento) servita sulla porta 8000: pagina «Prima / dopo» con i frammenti veri delle due build (P1.1 e P1.2) e link al sito completo; il generatore usa due `git worktree` temporanei (`1669413`, `c581a81`) e `build_match_pages`.
- **Prossimo passo.** `docs/28` §4: **P1.4** («Verifica approfondita» chiusa di default) e poi **P1.3** (nav con le ancore reali). Un intervento per turno, misura prima/dopo, push immediato; una sola PR a fine sessione (docs/00 §D).

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentacinquesimo giro) — **Scheda delle partite da giocare: istruttoria (`docs/28`) e primo intervento applicato — P1.1 «Vita del club» in una riga, con misura prima/dopo (`docs/29`).**

- **Il censimento (nuovo): `scripts/prematch_sections.py`.** Legge l'HTML generato con la sola libreria standard e misura, sulle **66 schede pre-partita** (di 375), tre cose: elenco delle sezioni con presenza e peso, **testo visibile senza aprire le tendine** contro testo dietro una tendina chiusa, e le ripetizioni (stesso dato in più card, frasi ripetute). Ogni numero di `docs/28` si rifà con `.venv/bin/python -m scripts.prematch_sections`.
- **`docs/28` — istruttoria (direttiva utente: «elenco di tutte le sezioni, come migliorarle, controlla le ridondanze»).** 18 blocchi titolati + hero + 2 card squadra. Peso misurato: **Vita del club 13,6% · Mercato 9,9% · Panchina 7,9%** del testo visibile contro **Previsione+Risultati 3,6% · Scomposizione 4,2%**; **42/66 schede senza nessun titolo pubblicabile**; xG/gara e PPDA ripetuti in **mediana 3 card** (max 6); **653 nomi** di indisponibili in mediana 2 card; «stabilizzata» 4× · «partite su 100» 6× · «Understat» 8× · «FotMob» 8-14× per scheda. Ipotesi smentite: banner «prior di lega» **0/66**, «λ limitate» 2/66. Coda: P1.1-P1.4, P2.1-P2.6 (§4).
- **`docs/29` — P1.1 applicata.** `news_quiet_line()` (funzione pura) costruisce la riga unica solo quando **nessuna delle due** squadre ha titoli pubblicati o in riserva; `match.html` mostra la riga e mette intro, imbuto per squadra e nota fonti in un `<details>` (frammenti `{% set %}` resi una volta sola, così `verify_site [20]` resta valido senza modifiche); CSS `.news-more` con soli token di tema. **Misura prima/dopo su due build dello stesso codice** (stash dei tre file → `site_old/`): card visibile **3.030 → 829 caratteri** (mediana sulle 42 schede, **−73%**), DOM 3.030 → 3.354 (nulla tolto), testo visibile della scheda 22.520 → **20.723** (−8,0%), quota della card **13,6% → 5,0%** visibile (+10,8% in tendina).
- **Gate [offline, sandbox]:** suite **437 passed** (+4), `ruff` pulito sui file toccati, `fda build` exit 0 (375/2.364/7.480), `scripts/verify_site.py` **0 problemi · 97.903 controlli** (`[20] 66 pagine`), `audit_match_sections.py` exit 0. Non verificato (serve un browser): resa della tendina a 375 px e contrasto del `summary` — resta il punto aperto P2.8.
- **Prossimo passo:** **P1.2** di `docs/28` §4 (togliere xG/PPDA dall'hero: un solo posto per tipo di dato), poi P1.4 (`#verifica` chiusa di default) e P1.3 (nav con le ancore reali). Misura di riferimento già pronta: «stesso dato in più card» dello script.

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b61a`, trentaquattresimo giro) — **Istruttoria sulla scheda delle partite da giocare: elenco sezioni, miglioramenti, ridondanze. Nessuna modifica al sito in questo turno (per scelta: è un'istruttoria).**

- **Consegna.** `docs/28_scheda_partite_da_giocare_sezioni_e_ridondanze_2026-09-18.md` (chiesta dall'utente: *«elenco di tutte le sezioni, come migliorarle, controlla le ridondanze»*) + **`scripts/prematch_sections.py`** nuovo (sola libreria standard, legge l'HTML generato: sezioni con presenza e peso, frasi ripetute, stesso dato in più card; riproduce ogni numero del doc con `.venv/bin/python -m scripts.prematch_sections`).
- **Perimetro misurato.** 375 schede partita, di cui **66 pre-partita** (gare non finite); build locale `fda build` exit 0 · 375/2.364/7.480 in 3m30s; testo visibile mediano per scheda **22.520 caratteri** (21.139–24.187).
- **Cosa dice il censimento.** 18 blocchi titolati + hero + 2 card squadra. Peso: **Vita del club 13,6% · Mercato 9,9% · Panchina 7,9%** del testo contro **Previsione+Risultati 3,6% · Scomposizione 4,2% · Scontro tattico 6,3%**; **42/66 schede senza nessuna notizia pubblicabile** (card da 3.054 car., 99% prosa metodologica); xG/gara e PPDA ripetuti in **mediana 3 card** (l'hero in 3-6 card su 154 misure); **653 nomi** di indisponibili in mediana **2 card** (max 15); "stabilizzata" 4× · "partite su 100" 6× · "Understat" 8× · "FotMob" 8-14× per scheda. Ipotesi smentita: banner "prior di lega" **0/66** e "λ limitate" 2/66.
- **Priorità proposte.** P1.1 empty-state «Vita del club» + note metodologiche richiudibili (−2.500 car. su 42 schede) · P1.2 hero senza xG/PPDA (un solo posto per dato) · P1.4 «Verifica approfondita» chiusa di default (1.883 car.) · P1.3 nav con ancore reali (4 link per 21 blocchi) · poi P2 (legenda unica per la stima stabilizzata, assenze raccontate una volta, micro-visivi per le card di sola prosa, separare «Contesto», badge forma in hero da `docs/17`).
- **Gate del turno [offline, sandbox]:** suite **433 passed**; `ruff check scripts/prematch_sections.py` pulito; `fda build` e `verify_site` non toccati (nessuna modifica a template/codice di build). Nessun commit di dati/sito (site/ è gitignored).
- **Prossimo passo.** Conferma dell'ordine di priorità da parte dell'utente; poi attuare **P1.1** da solo, con misura prima/dopo e aggiornamento di `docs/28` §4.

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b4ea`, trentatreesimo giro) — **Prima «PR ricca»: P1.12 + P1.15 + P2.9/P2.10 + P2.6 + P2.1/P2.2 + P2.4 + presidio [11e] in un solo giro.** Applicata la direttiva di `docs/00` §D: 7 interventi della coda, ognuno concluso/testato e **pushato subito**, gate pieni alla fine, **una sola PR**.

- **Partenza.** PR #55 verificata in `main` (merge commit `b5d2146`, 14:29:59Z) prima di iniziare, come chiesto dall'utente; branch riallineato con `git rebase origin/main`. Baseline misurata all'inizio: **388 passed**, ruff **173**.

- **P1.12 — claim ridotto in `info.html` (`docs/19` §1.7).** *L'audit aveva una premessa sbagliata e la correzione proposta sarebbe stata falsa.* §1.7 chiedeva di far dire a `info.html` «λ×1,040 scelto su griglia 0,90-1,02», perché il valore pubblicato è fuori da `LAMBDA_SCALE_GRID`. **Misurato:** quella griglia **non sceglie** il valore in produzione — lo sceglie `moment_scale()`, che è **continuo** (rapporto gol osservati/attesi) e limitato da `SCALE_BOUNDS` (0,85–1,05); `LAMBDA_SCALE_GRID` serve solo allo stimatore di confronto (`_best_params` → metrica `confronto_scale_griglia`). Scrivere «scelto su griglia» avrebbe introdotto un'affermazione falsa al posto di una troppo forte. Claim pubblicato: «**stimato dai momenti (media dei gol osservati), limitato a 0,85–1,05**». Aggiunti `Calibration.scale_domain` e `.scale_claim` (dipendono dallo stimatore realmente usato) e `scale_grid_min`/`max` nello store, come §1.7 chiedeva. **4 test.**

- **P1.15 — filtro di `prossime.html` (`docs/19` §3.10).** (a) **Debounce 120 ms** sulla ricerca: digitare «juventus» faceva 8 passaggi completi su 1.987 card, ora uno solo (Invio bypassa il ritardo). (b) **Badge precalcolati**: i conteggi per bucket di stato non dipendono né dalla ricerca né dalla lega, ma venivano ricalcolati **dentro `applyNow()`**, cioè a ogni frame — **1.987 × 5 = 9.935 confronti per frame, l'83% del lavoro totale**; ora contati una volta in `statusCounts`. (c) `content-visibility` era **già presente** (PR #54): l'unico intervento utile restava `contain-intrinsic-size` da `280px` fisso ad **`auto 280px`**, così il browser usa l'altezza reale invece di una stima non verificabile dal sandbox. **5 test** strutturali (nel sandbox non c'è browser: non si misurano millisecondi).

- **P2.9 / P2.10 — candidati del laboratorio con griglia pre-registrata (`docs/19` §1.3 e §1.2).** Prima applicazione della regola P1.11 a candidati **nuovi**. `sharpen_lambda_ratio()` (γ sul rapporto delle λ, **totale gol invariato**) e `recalibrate_1x2()` (temperatura τ, **argmax invariante**). 5 candidati registrati: `gamma_112`, `gamma_124`, `tau_090`, `tau_110`, `tau_120` (27 candidati totali). Le griglie dichiarate sono quelle **oneste**: γ ∈ [1,00 … 1,40] — *non* la [0,94; 1,12] con cui lo stesso esperimento «passava» il protocollo — e τ **simmetrica** attorno a 1,00 (0,90 … 1,30), così il candidato può anche dire «peggiora». τ si applica **dopo** la calibrazione (`_apply_calibration` ricostruisce la riga dalla griglia e cancellerebbe il ritocco) e tocca **solo** l'1X2: limite dichiarato in docstring. **7 test**, incluse 2 controprove end-to-end (totale λ invariato per γ; `p_over25` invariato per τ). **`predict.py` non è toccato: la ricetta di produzione è identica.**

- **P2.6 — `fmt.pct_triple` (`docs/19` §4).** `numpy` era importato **dentro** la funzione, chiamata una volta per card. Serviva solo per un argsort stabile su **3** elementi: `sorted` di Python è stabile per definizione e dà lo stesso ordine, tie-break (1, X, 2) incluso. **Equivalenza verificata su 400.016 confronti** (200.000 vettori casuali + casi degeneri e di parità esatta, `nd=0` e `nd=1`): **0 differenze**. L'«unica implementazione» chiesta da §4 era già a posto. ruff su `fmt.py`: 2 → 1. **4 test.**

- **P2.1 / P2.2 — bordo dell'header fuori tema (`docs/19` §4).** Non un duplicato innocuo ma un **bug reale del tema chiaro**: una seconda regola `header{...}` in fondo a `site.css` sovrascriveva quella di riga 105 forzando `border-bottom:#263142`, cioè il valore del tema **scuro**; in tema chiaro (`--line:#d0d8e6`) il bordo restava scuro. È il caso citato dall'audit. Esaminate **tutte le 27** occorrenze hex fuori dai token: le altre **non** vanno tokenizzate, con motivo verificato — `.bar .h/.d/.a` hanno sfondi identici nei due temi di proposito, e `.scoregrid td.mode #06231a` è documentato (con `--on-accent` diventerebbe `#ffffff` in chiaro: contrasto da **7,95:1 a 2,09:1**). **2 test di guardia**, entrambi verificati **a fallire** reintroducendo il difetto (il guard esistente copriva solo `color:`, non i bordi).

- **P2.4 — le tre frasi-macchina di `narrative()` (`docs/19` §2.8).** Difetti **misurati sul sito costruito**, non ipotizzati: **155** pagine con «ha **-**3,1 punti rispetto agli xPTS» (`dec(plus=True)` su valore negativo: non è italiano), **278** con «Assenze Inter: 3 — A, B, C» (formato record, non frase), **32** con «nettamente favorito» a partire dal 60% (a quella soglia il favorito perde comunque 4 volte su 10, e contraddiceva la cura dichiarata in `prediction_meta()`). Riscritte: «ha **3,0 punti in meno** di quanto dica l'xPTS», «**Inter deve rinunciare a 2 assenti, uno dei quali titolare abituale: A e B**», «Il modello indica X come esito più probabile (72%): **su 100 partite così, 72 finiscono in quel modo**» (frequenze naturali, coerenti col dotplot dei gol). Nuovo helper `_elenco_it()` per la congiunzione prima dell'ultimo nome. **Il criterio di «peso» non cambia**: resta «titolare abituale» (`docs/20` §13), si cambia solo come viene detto. Corretto anche un caso che l'audit non prevedeva: con **un solo** assente «1 assente, uno dei quali titolare» stonava → «1 assente, titolare abituale». Aggiornato `scripts/audit_match_sections.py`, che contava le assenze con la vecchia regex e avrebbe letto 0 su tutte le leghe. **Verifica prescritta da §2.8 superata: `ha −`/`Assenze …: N`/«nettamente favorito» = 0 occorrenze**; pubblicate 278 frasi nuove sulle assenze e 32 in frequenze naturali. **15 test** (1/2/5 indisponibili e `diff=-3.0`, i casi chiesti dall'audit).

- **P2.7 — istruttoria, NON eseguito (decisione motivata).** La paginazione di `prossime.html` (`docs/19` §3.10 livello 3) è **condizionata dall'audit stesso** alla crescita del calendario («se supera ~250 card»). **Misurato oggi: 60 card ricche**, 4× sotto la soglia. Le altre **1.988** partite non sono card ma righe del calendario compatto, **già paginato per mese** con `<details>` (solo il primo aperto) e **già fuori dal layout quando chiuso** (`content-visibility`, `site.css` 498-499). Spezzare la pagina in `prossime/mese-YYYY-MM.html` costerebbe URL nuove da mantenere e in sitemap, e soprattutto **romperebbe il filtro client-side**, che oggi vede tutto il calendario in una pagina sola — una regressione funzionale certa in cambio di un beneficio non misurabile dal sandbox. Rinviato finché la condizione non si avvera.
- **Presidio `[11e]` sul peso delle pagine — il difetto vero trovato istruendo P2.7.** `docs/19` §3.10 chiude dicendo «il blocco `[11e]` di `verify_site.py` con `MAX_PAGE_KB = 900` intercetta la regressione»: **quel controllo non è mai esistito**, verificato sia sul branch sia su `origin/main` (`grep MAX_PAGE_KB` → 0 occorrenze). Era una garanzia solo dichiarata: qualunque pagina avrebbe potuto gonfiare senza che nulla se ne accorgesse, perché in una code review il diff mostra dieci righe di template, non i megabyte che ne escono. Implementato `check_page_weight()`: tetto **900 KB**, con **eccezione dichiarata** `prossime.html` → 1.800 KB (contiene di proposito l'intero calendario) invece di alzare il limite per tutti, e **l'eccezione ha comunque un tetto**. Misure attuali: `prossime.html` **1.416 KB**, seconda pagina `risultati.html` **367 KB** — il margine è ampio. Provato **a fallire** gonfiando una pagina reale a 1.013 KB → exit 1 con messaggio azionabile; corretto anche il raggruppamento del sommario (iniziava con la cifra → `{'1.013': 1}`, illeggibile; ora `{'pagina': 1}`). **4 test.**

- **Gate pieni [offline, sandbox]:** suite **430 passed** (+42, da 388), ruff **172** (baseline 173, **−1** per P2.6: nessuna nuova), `fda build` **exit 0** (375 schede / 2.364 partite / 7.466 giocatori), `verify_site` **exit 0 — 0 problemi · 97.884 controlli** (+4.124: uno per pagina, dal presidio nuovo), `git status --porcelain` vuoto.
- **Nota di continuità (2026-09-18).** A metà sessione il sandbox è stato ripristinato a uno stato precedente (`HEAD` tornato a `1f12f90`, `.venv` perduta). **Nessun lavoro perso**: i 7 commit erano già su `origin` grazie alla regola 8bis (push immediato), branch ricostruito da `origin/arena/01a0b4ea` e ambiente reinstallato. È la prova pratica del perché quella regola esiste. Verificato nel sito generato: claim nuovo in `info.html`, debounce in `prossime.html`, `header{border-bottom:1px solid var(--line)}` in `site.css`.
- **Non verificato (serve un browser reale):** l'effetto percepito del debounce e di `content-visibility` a 375 px. Resta il punto aperto di sempre (P2.8/Lighthouse, fuori dalla PR ricca).
- **Prossimo passo:** la coda P1/P2 di `docs/19` è **esaurita** (P2.7 istruito e rinviato con motivo, sopra). Fuori dalla PR ricca, come da §D: ricetta di produzione, pulizia ruff larga (99 auto-fix), Lighthouse CI, feed Sportmediaset 404, divergenza `match_info`/`fixtures` su `5868067`; **traduzione automatica = decisione dell'utente**. Il primo `fda lab` post-merge misurerà i 5 candidati nuovi.

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b4ad`, trentaduesimo giro) — **P1.11 chiuso: la griglia degli iperparametri del laboratorio è pre-registrata nel codice.** Riferimento: [`docs/19`](19_audit_totale_tre_aree_2026-09-14.md) §1.4 (motivo misurato: lo stesso γ-sharpening *passa* i criteri di promozione con griglia [0,94; 1,12] e *non passa* con [1,00; 1,40]).
- **Cosa è cambiato.** Campo `Candidate.grid` in `src/fda/models/lab.py`: la griglia si dichiara nel codice, prima del run. Le **12 varianti esistenti** dichiarano l'insieme dei valori provati — ξ (0,0010 · 0,0018 · 0,0030), shrinkage (0 · 8 · 16), peso DC (0,5 · 0,7 · 0,85), k Elo (10 · 20 · 40), vantaggio casa (40 · 60 · 80), valore di riferimento compreso — mentre i candidati a parametri fissi (produzione, riferimenti, famiglie alternative) non dichiarano nulla. `walk_forward` marca ogni riga con `grid_dichiarata` e `n_tentativi` (candidati della **stessa famiglia** provati nel run); `summarize()` e `per_league()` pubblicano le due colonne in `model_lab.parquet`. `scripts/verdetto_lab.py` le stampa accanto al verdetto, **anche nel ramo PROMUOVERE**, con avviso esplicito se un candidato promuovibile non ha griglia dichiarata. Regola scritta in `docs/00` §D («la griglia si dichiara nel codice prima del run»; nessuna PR che cambi `predict.py` senza griglia dichiarata).
- **Verificato [offline, sandbox]:** smoke `fda lab --history` (corpus sintetico 264 gare · 4 candidati) → `model_lab.parquet` con le colonne nuove **popolate** (`dc_xi10` griglia `0.001|0.0018|0.003` · tentativi_famiglia 3; `mix_50` `0.5|0.7|0.85` · 3; `elo_k10` `10.0|20.0|40.0` · 1) e le righe dei run precedenti a NaN, come previsto («il run precedente resta con la sua griglia»); ramo PROMUOVERE provato con Parquet artificiale (un candidato con griglia, uno senza → nota «SENZA griglia dichiarata»); Parquet poi **ripristinato** (`git checkout`). Suite **388 passed** (385 + 3 test nuovi: griglie pre-registrate ordinate e col valore provato dentro, marchiatura walk_forward/summarize/per_league, ripiego per righe senza colonne P1.11), ruff **173** = baseline, `git status --porcelain` pulito.
- **Non tocca nulla di pubblicato:** nessuna modifica a `predict.py` (la ricetta di produzione è identica) e il sito legge solo il numero di righe di `model_lab` (`info.html`): per questo `fda build`/`verify_site` non sono stati rieseguiti in questo giro. Il primo `fda lab` post-merge (lunedì o dispatch) scriverà le colonne nuove nello store.
- **Merge di PR #54 (deroga alla regola D, 2026-09-18).** L'utente ha scritto «ok fai il merge e prepara il nuovo agente su cosa bisogna fare»: merge eseguito **dall'agente** dopo la verifica prescritta (check `test` SUCCESS, PR `MERGEABLE · CLEAN`, `git status --porcelain` vuoto, `origin/main..HEAD` = solo i 3 commit della PR). Merge commit **`1f12f90`** su `main`. Registrato qui e in `docs/13` («Deroga al flusso di merge (2026-09-18, PR #54)»).
- **Direttiva utente nuova (2026-09-18): PR ricca.** L'utente vuole «più cose possibile senza commettere errori prima di fare il merge»: le sessioni future accumulano sul branch più interventi della coda (ognuno concluso, testato, pushato subito) e aprono **una sola PR** dopo i gate pieni. Regola completa scritta in `docs/00` §D («PR ricca di sessione»), con l'elenco di ciò che **resta fuori** (ricetta di produzione, pulizia meccanica larga, nuovi workflow, richieste specifiche dell'utente). Registrata anche nelle direttive persistenti del briefing.
- **Per il prossimo agente (nuovo branch `arena/…` da `main`):**
  1. **Non rifare** (già in `main`): potatura 14 giorni, font locali, fix `font-locali.yml`, **P1.11** (PR #54, `1f12f90`).
  2. **Modo di lavorare: PR ricca** (`docs/00` §D) — un intervento per turno, commit+push immediati, gate pieni a fine sessione, una sola PR. Turni piccoli (regola A2 invariata), PR densa.
  3. **Primo blocco: P1.12** (*claim ridotto* in `info.html`: «λ×1,040 scelto su griglia 0,90-1,02», **non** rifare il fit — `docs/19` §1.7) **+ P1.15** (debounce 120 ms + `content-visibility:auto` sulle card di `prossime.html` — `docs/19` §3.10) **+ P2.9** (candidato γ-sharpening respinto registrato in `model_lab.parquet` — `docs/19` §1.3) **+ P2.10** (ricalibrazione del vettore 1X2/temperatura come candidato del laboratorio — `docs/19` §1.2; **prima applicazione della regola P1.11**: griglia pre-registrata in `Candidate.grid`).
  4. **Se resta tempo** (tutti offline-verificabili): **P2.1** (62 selettori CSS duplicati), **P2.2** (35 colori hard-coded → token con test di guardia), **P2.4** (3 frasi-macchina di `narrative()` + frequenze naturali 1X2), **P2.6** (pulizia `fmt.pct_triple`).
  5. **PR separate, non nella PR ricca:** ricetta di produzione (`predict.py`/`calibration.py`: protocollo laboratorio + griglia), pulizia ruff larga (100 auto-fix), Lighthouse CI (P2.8, nuovo workflow), feed Sportmediaset 404 (`docs/25` §5), divergenza `match_info`/`fixtures` su `5868067` (`docs/26` §9). **Traduzione automatica** del materiale straniero scartato (`docs/25` §7): decisione dell'utente, non di iniziativa.
  6. Merge sempre dell'utente salvo deroga esplicita. Tutto in italiano. Mai numeri senza misura.

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b48c`, trentunesimo giro) — **Esito in produzione di PR #51 e #52, e fix del workflow `font-locali` che mascherava gli errori.**
- **PR #51** mergiata in `0a2a2f8` (11:08:07Z). Primo daily post-merge: run `35338118021`, commit dati `daba341` (11:23 UTC). In `source_status` la riga `news:NEWS` delle 11:11:00Z dichiara **`potate 5192`**; `news.parquet` passa da **19.841 righe / 5.136.037 byte** (`00b4ec3`, 09:11 UTC, ancora senza la potatura) a **14.824 / 3.747.043** (−1.388.994 byte). Il run successivo (`3fc5ced`, 12:30 UTC) dichiara `potate 6` (il tetto è già in vigore) e il file sta a **14.951 righe / 3.777.609 byte**.
- **PR #52** mergiata in `66afcd9` (12:15:14Z, merge dell'utente). Nel repo: **9 woff2 · 255.952 byte**, `fonts.css` 12.140 byte con **0** URL `https://`. Il CSS pubblicato (`https://uamisjd.github.io/football-deep-analyzer/assets/fonts/fonts.css`) è lo stesso, URL relative. L'HTML grezzo di Pages non è raggiungibile dal sandbox (TLS EOF verso `github.io`); sul sito pubblicato l'utente ha contato **0** `fonts.googleapis.com`, **1** `assets/fonts/fonts.css`, **9** woff2 · 250 kB serviti — i file nel repo coincidono (255.952 byte = 250 KiB).
- **Difetto del workflow.** Il run `font-locali` `35340877847` (11:41:29–11:42:15Z) è uscito **verde**: il bot ha committato `7550c74` alle 11:42:10Z, ma la PR #52 è stata **aperta a mano da uamisjd alle 12:10:57Z** (`author` della PR = utente, non `github-actions[bot]`). Causa: `gh pr create || echo "Una PR … è già aperta"` maschera ogni exit 1, incluso il 403 per il permesso «Allow GitHub Actions to create and approve pull requests». Fix: si cattura l'uscita di `gh pr create`; se contiene `already exists` si esce 0, altrimenti si stampa la causa più comune e si esce 1. Dettaglio in [`docs/26`](26_revisione_totale_2026-09-17.md) §12.
- **Deroga alla regola D (2026-09-18, questa sessione).** L'utente ha scritto «ok fai il merge» sulla PR **#53**, con il vincolo che per continuare serve una **nuova sessione**. Scelta: mergiare questo giro (fix atomico, CI verde) e **non** iniziare P1.11 qui — un lavoro lasciato su `arena/01a0b48c` dopo il merge resterebbe orfano (la sessione nuova clona `main` su un branch `arena/` nuovo).
- **Per il prossimo agente (nuovo branch `arena/…` da `main`):**
  1. **Non rifare** (già in produzione / in questa PR): potatura 14 giorni (`potate 5192`, `news.parquet` 19.841/5.136.037 → 14.824/3.747.043), font locali (9 woff2 · 255.952 byte, 0 `fonts.googleapis.com`), fix `font-locali.yml` (`already exists` → 0, il resto → 1).
  2. **Primo lavoro = P1.11** (`docs/19` §1.4 e tabella §4): campo `Candidate.grid` in `src/fda/models/lab.py`, colonne `grid_dichiarata` e `n_tentativi` in `summarize()` / `model_lab.parquet`, regola in `docs/00` §D («la griglia si dichiara nel codice prima del run»). Motivo misurato: lo stesso γ-sharpening **passa** con griglia [0,94; 1,12] e **non passa** con [1,00; 1,40]. Rischio basso; non tocca la ricetta di produzione.
  3. Poi **P1.12** come *claim ridotto* in `info.html` («λ×1,040 scelto su griglia 0,90–1,02»), **non** rifare il fit (`docs/19` §1.7, rischio alto).
  4. Poi **P1.15**: debounce 120 ms del filtro + `content-visibility:auto` sulle card di `prossime.html` (`docs/19` §3.10).
  5. Poi i P2 di `docs/19` §4. **P2.3 (font) è chiuso** da PR #52.
  6. **PR separate**, non mescolare: pulizia ruff (100 auto-fix su 173); feed Sportmediaset 404 (`docs/25` §5, da confermare in produzione: sandbox senza rete verso l'URL); divergenza `match_info`/`fixtures` su `5868067` (`docs/26` §9).
  7. **Traduzione automatica** del materiale straniero scartato (`docs/25` §7): **decisione dell'utente**, non di iniziativa — tocca la promessa «nessun testo è inventato».
  8. Merge sempre dell'utente salvo deroga esplicita. Tutto in italiano. Mai numeri senza misura.

**Ultimo aggiornamento:** 2026-09-18 (sessione `arena/01a0b0ab`, trentesimo giro) — **Le tre decisioni di `docs/26` §8 sono state sottoposte all'utente, che le ha prese; applicate nella stessa PR #51: ritenzione delle notizie a 14 giorni e font auto-ospitati (con un workflow che li scarica, perché dal sandbox Google non è raggiungibile).**
- **Ritenzione 30 → 14 giorni (`src/fda/collect.py`).** Costante nominata `NEWS_RETENTION_DAYS = 14` al posto del numero a mano nell'argomento di `collect_news`. È il valore minimo che copre le **due** finestre che leggono la tabella: la card «Vita del club» guarda 7 giorni (`NEWS_WINDOW_DAYS`, `analysis.py:1336`) e il gate `verify_site` [20] ne verifica 12 (`verify_site.py:105`) — quindi **nessuna riga pubblicata o ricalcolata viene persa**. Misurato **eseguendo la potatura** su `news.parquet` reale: potate **5.028** righe su 18.705 (18.705 → 13.677 · 4.891.680 → 3.525.759 byte); a regime **~21.400 righe ≈ 5,6 MB** e history **~28 MB/giorno** invece di ~60. **Prova che non si perde nulla di pubblicato:** sito ricostruito sui dati potati → `fda build` exit 0 (373 schede · 4.124 pagine) e `verify_site` **0 problemi · 93.574 controlli**, identico ai dati non potati; poi dati ripristinati (md5 identico). Nuovo test che impedisce di abbassare la costante sotto le due finestre e che il default **sia** la costante. Conferma dal vivo del difetto: il run `00b4ec3` di `main` (2026-09-18 09:11 UTC) ha portato `news.parquet` da 4.891.680 a **5.136.037 byte**, +244 kB in un solo run.
- **Font auto-ospitati (`base.html` caricava Sora/Inter da Google su ogni pagina: 8.252 link su 4.126).** `scripts/font_locali.py` scarica i woff2 (CSS di Google con User-Agent moderno → sottoinsiemi woff2 piccoli), riscrive le URL in percorsi relativi e scrive `LICENSE.txt` (SIL OFL 1.1); `SiteBuilder` pubblica `assets/fonts/` se i file ci sono; `base.html` sceglie: con i font locali **zero** richieste a Google, senza resta il caricamento da Google (nessuna pagina senza caratteri). Glob `assets/fonts/*` aggiunto a `package-data` **prima** che i file esistano: stessa classe di difetto di `site.css`. **Limite dichiarato:** dal sandbox Google Fonts non è raggiungibile (`curl` → `000`), quindi i binari **non** sono stati scaricati qui; il workflow `font-locali.yml` (`workflow_dispatch`) li scarica in un runner e apre una **PR** su `font-locali` — il merge resta dell'utente (regola D).
- **Difetto trovato nel codice scritto in questo giro, da `ruff`:** `scripts/font_locali.py` citava una variabile inesistente (`mancanti` invece di `mancano`) nella riga di diagnostica, cioè `NameError` esattamente nel caso in cui `--check` doveva segnalare i file mancanti. Corretto, e coperto da due test sul ramo di errore e su quello completo; controprova: rimettendo il nome sbagliato il test **fallisce**.
- **`source_status`: nessun intervento**, confermato dall'utente (misurato ~198 righe/giorno, ~0,5 MB/anno).
- **Verifiche:** suite **385 passed** (da 377: +1 ritenzione, +7 font) · `ruff check .` **173** = baseline (le due segnalazioni nuove introdotte e tolte nello stesso giro, una era il `NameError` qui sopra) · `fda build` exit **0** · `verify_site` **0 problemi · 93.574 controlli** · `scripts/font_locali.py --check` exit 1 con messaggio chiaro (font non ancora scaricati).
- **Sul conteggio delle pagine: 4.124 invece di 4.126, 373 schede invece di 375 — verificato, non è un regresso.** Le schede vengono da oggi + i prossimi 7 giorni + gli ultimi 7 **solo se `status == "finished"`** (`build_indexes`, `build.py:408-420`) più l'archivio finito. Le due schede in meno sono `5868063` (Real Betis–Getafe, 17/09 17:00 UTC) e `5868068` (Málaga–Villarreal, 19:30): ieri erano «oggi», oggi sono ieri e nei dati locali lo stato è ancora `live`/`scheduled` — dal sandbox non si raccolgono dati, i Parquet sono quelli del checkout. In produzione `fda daily` aggiorna lo stato prima del build. Terza riga senza pagina (`5868067`): `fixtures` la mette al 2026-10-21 con stato `scheduled`, mentre `match_info` dice 2026-09-16 — **divergenza fra le due tabelle notata, non toccata**.
- **Correzione di misure mie (2026-09-18, prima del merge).** Una prima versione di questi appunti dava «4.700 righe potate», «24 oltre i 30 giorni» e «~233 giorni al tetto GitHub»: erano contate per **giorni di calendario**, mentre il codice confronta i **timestamp**. Rieseguendo la misura con la regola del codice: **5.028** potate a 14 giorni, **133** oltre 30, **~250** giorni al tetto. Le conclusioni non cambiano, i numeri sì — e ora sono misurati eseguendo `collect_news`, non stimati.
- **Deroga alla regola D (2026-09-18).** L'utente ha chiesto esplicitamente «Please merge the pull request», quindi il merge di **PR #51** è stato eseguito **dall'agente** (CI verde, `MERGEABLE · CLEAN`, `merge-tree` senza conflitti). Registrato qui e in `docs/13` («Deroga al flusso di merge (2026-09-18)»), come prescritto per le eccezioni alla regola D: senza una richiesta esplicita il merge resta dell'utente.
- **Prossimo passo (scelta dell'utente):** un clic su `font-locali` (Actions → Run workflow) e merge della PR che ne esce; poi verifica del **primo `daily` post-merge** — la potatura deve comparire nell'imbuto di *Stato fonti* come `potate ~5.000` e `news.parquet` deve scendere da 4,89 a ~3,5 MB.

**Giri archiviati (regola A5):** dal **ventiduesimo** al **ventinovesimo** in [`STATO_archivio_2026-09-17.md`](STATO_archivio_2026-09-17.md); dal **tredicesimo** al **ventunesimo** in [`STATO_archivio_2026-09-16.md`](STATO_archivio_2026-09-16.md); fino al 2026-09-15 sera (primo candidato) in [`STATO_archivio_2026-09-15.md`](STATO_archivio_2026-09-15.md).

---

## Fatto
- [x] **Audit pre-merge delle schede in programma (2026-09-13, quinto giro, `arena/01a09a79`, PR #27)** — misurate 95 schede pre-partita su 7 leghe: **parità di sezioni al 100%** (Analisi pre-partita, Scontro tattico, I giocatori che decidono, Contesto, Confronto di stagione, Fatti rilevanti), blocchi del modello 93/95, audit per campo **0 «mancante»** dopo 4 correzioni (previsione non più classificata «attesa dalla fonte»; distinta senza assenze = presente e non buco; la scheda **dice** «nessun indisponibile segnalato» invece di tacere, solo pre-partita; footer «Prossime partite e calendario»). Diagnosi con dati alla mano: le 2 schede senza modello sono neopromosse senza storico nel corpus surrogato del sandbox, e «Come nasce questa probabilità» manca su 70/95 solo perché le 138 previsioni committate sono `dc-elo-ens-0.1/-0.2` (25/25 con `-0.3` ce l'hanno) → si risolve da solo al primo `fda predict` post-merge. Copertura schede sulle partite in programma **4,6%** (Tappa 2 = decisione utente). Suite **172 passed**, `verify_site` **0 problemi · 11.793 controlli**. Tabella di parità e misure: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9.6.
- [x] **Tappa 1 — orizzonte esteso a tutto il calendario (2026-09-13, quarto giro, `arena/01a09a79`)** — `predictions` a una riga per partita (`latest_per_match` + migrazione 3.210 → 138 dichiarata a schermo), `fda predict --days-ahead 0` (tutto il calendario, zero richieste in più), pagina «Prossime» con «Tutto il calendario»: 1.987 righe compatte raggruppate per mese (1X2 / gol attesi / Over 2,5, preferito in grassetto e a colore, link solo se la scheda esiste, «senza previsione» quando manca). Copertura **6,6% → 96%** (138 → 2.000 partite su 2.083), **1,6 kB → 0,27 kB per partita coperta**, 29,7 ms per previsione, `prossime.html` 274 kB → 1.102 kB (**83 kB gzip**), `build_indexes` 2,4 s, suite **169 passed** (+6), `verify_site` **0 problemi · 11.793 controlli** col nuovo **[11]** (9.643 controlli sul calendario). Misure e criteri: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9. Tappa 2 rinviata (§9.5).
- [x] **Auto-audit accuratezza (2026-09-13, terzo giro, `arena/01a09a79`)** — calibrazione v1.1 a momenti su finestra 730 giorni (λ×0,9135: bias λ fuori campione +0,264→+0,053, Brier mercati 0,20646→0,20516; sul campione pieno bias +0,245→−0,024 e pareggio 23,9%→26,2% su osservato 25,6%), limiti di sicurezza sulle λ invertite (nessuna scheda da 8,4 gol attesi: 69 gare toccate, 1,19%, RPS invariato e mercati migliori proprio dove il modello esagerava), laboratorio reso equo (miscela non più appiattita sul DC, livello dei gol corretto per ogni candidato con inclinazione esponenziale), candidati 15→22 (`dc_elo_tilt`, pesi 0,50/0,85, iperparametri Elo), `GRID_SIZE` unica in `dc_grid.py`, 3 difetti nei verificatori e anteprima che riapplica i limiti. Suite **163 passed**, `verify_site.py` **0 problemi · 2.213 controlli numerici**. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Modelli + schede (2026-09-13, `arena/01a09a79`)** — calibrazione della griglia (λ×0,94, Δρ−0,04: bias gol +0,245→+0,058, pareggio 23,9%→25,7%, Brier mercati walk-forward 0,2065→0,2055), laboratorio walk-forward a 15 candidati con IC appaiati (`fda lab`, workflow settimanale), storico di allenamento salvato a ogni run (`history.parquet`), distribuzione dei gol totali + dotplot quantile e scomposizione della probabilità in scheda, controlli [9]/[10] in `verify_site.py`, suite a 150 test. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Processo/handoff — policy «merge lo esegue SEMPRE l'utente»** (decisione utente 2026-09-08, riapplicata in questa sessione perché il commit originale `9d39a21` non era mai stato pushato): sez. D di `00_regole_di_lavoro.md` riscritta (dopo la frase fissa l'agente si ferma e aspetta, non mergea mai), avviso ⚠️ in cima a `BRIEFING_NUOVA_SESSIONE.md`, decisione registrata in «Decisioni aperte». Doc-only; test 23 passed, nessuna nuova segnalazione ruff (baseline: vedi Nota). **PR #9 aperta verso `main`**: check `test` **Success** (49s), mergeable **CLEAN** — in attesa del merge dell'utente (secondo la nuova policy).
- [x] **Processo/handoff — paletti di qualità + briefing nuove sessioni** (fatto, **PR #6 mergiata in `main` `16db091`**): riscritta la sezione B (paletti di qualità: accuratezza/precisione/profondità/qualità), aggiunta la sezione D (policy esplicita su **quando fare Create PR e quando Merge**, con frasi fisse e checklist), e creato `docs/BRIEFING_NUOVA_SESSIONE.md` (porta d'ingresso con architettura, stato, prossimi passi, mappa file e limitazioni, così un agente nuovo non ripete verifiche già fatte). Doc-only, check tests pass (42s).
- [x] Studio di fattibilità e catalogo fonti (`01`, `02`) — commit `ac35f2b`
- [x] Decisioni utente registrate (7 campionati, automazione giornaliera, uso personale, quote sì, solo fonti GitHub Actions) — `03_decisioni_e_funzionamento.md`
- [x] Regole anti-blocco chat — `00_regole_di_lavoro.md`
- [x] Verifiche tecniche: penaltyblog OK (nota pandas 3), datahub CSV OK, FotMob id 7 leghe OK, matchDetails pre-partita ricco (formazione probabile, meteo, arbitro)
- [x] **Passo 1 — scaffolding**: `pyproject.toml`, `src/fda/` (config, http client con cache+rate limit+retry, cli), `config/leagues.yaml` (7 leghe + coppe), `config/sources.yaml` (budget richieste), `tests/` (6 test verdi), `.gitignore`, `data/README.md`. Comandi: `fda leagues`, `fda version`.

- [x] **Passo 2 — client FotMob** (`src/fda/sources/fotmob.py`): `fixtures`, `matchDetails`, `teams`, `playerData`, `matches?date`; parser → record piatti (MatchInfo con arbitro/stadio/meteo/xG/H2H, Shot, TeamMatchStat per periodo, PlayerMatchStat, LineupPlayer con indisponibili+rientro, eventi, momentum). Test su JSON campione (9 verdi). Comandi `fda fotmob-fixtures ITA1`, `fda fotmob-match <id>`.

- [x] **Passo 3 — Understat + ESPN**: `sources/understat.py` (endpoint JSON con cookie+XHR, senza soccerdata/seleniumbase; partite con xG, righe squadra-partita con xPTS/PPDA/deep, giocatori; `team_season_table`) e `sources/espn.py` (scoreboard con statistiche, classifiche, boxscore, arbitri). 11 test verdi. Comandi `fda understat-table ITA1`, `fda espn-today ITA1`.

- [x] **Passo 4 — storage + collect**: `store.py` (Parquet versionati in `data/processed/` + viste DuckDB, upsert per chiave, tipi UTC stabili) e `collect.py` (per lega: calendario → dettagli partite oggi ±3 gg, finite una sola volta, future a ogni run → Understat → ESPN; fonti isolate, esiti in `source_status`). 13 test verdi (incluso un run completo offline). Comandi `fda collect [ITA1 ...]`, `fda db [SQL]`.

- [x] **Passo 5 — modelli**: `sources/history.py` (CSV storici datahub/football-data → schema unico, quote di chiusura se presenti), `teams.py` (nomi canonici tra fonti, ~300 alias per le 7 leghe), `models/predict.py` (Dixon-Coles pesato nel tempo + Elo + ensemble 70/30 con griglia coerente; mercati 1X2, risultati esatti, O/U, BTTS, doppia chance, clean sheet; RPS). Backtest onesto Serie A 2025/26 (train 300 → test 80): RPS ensemble 0,212 vs Elo 0,218 vs naive 0,244. 18 test verdi. Comando `fda predict [ITA1 ...]`.

- [x] **Passo 6 — sito + automazione**: `site/analysis.py` (contesto analitico e frasi in italiano da regole esplicite), template Jinja2 (Oggi, Prossime, Risultati, pagina partita pre/post, Accuratezza con RPS/Brier, Stato fonti), `site/build.py`, comandi `fda build` e `fda daily` (collect → predict → build). Workflow `.github/workflows/daily.yml` (cron 5 volte al giorno + manuale, commit dei Parquet, deploy Pages) e `tests.yml`. 19 test verdi.
- [x] **Passo 6b — primo run dal vivo**: su `main`, run `daily` #2 (2026-09-06, come da handoff) concluso **Success** in 3m17s: collettori → modelli → build sito → commit dati → deploy GitHub Pages. Artefatto `github-pages` di 132 KB; Pages usa source = **GitHub Actions**. Restano solo gli avvisi non bloccanti sulla deprecazione Node.js 20.
- [x] **Correzione PR #2**: merge su `main` nel commit `968f2d7`; lo step di installazione di `daily.yml` usa `pip install -e ".[dev]"`, così `pytest` è disponibile nel workflow.
- [x] **PR #3 mergiata su `main`** nel commit `3359fdc` (workflow con versioni Node 24).
- [x] **Run `daily` #3 su `main` verificato** (id `33999756428`, head `3359fdc`): job `run` **Success** 3m08s + `deploy` **Success** 10s; commit dati `43b848c` pushato; artefatti `github-pages` (135358 B) e `run-log-3`; deployment Pages riuscito. `predictions` 48 → **96** righe (ITA1 20, ENG1 18, ESP1 22, GER1 18, FRA1 18), probabilità coerenti e senza null, tutte gare `scheduled`. FotMob ok 7/7; Understat per le 5 leghe coperte; ESPN standings ancora **403 isolato** (non blocca). Suite locale 20 passed. (Blob di log Actions e pagina Pages non raggiungibili dal sandbox — verifica su metadati/commit/dati.)

## In corso

> ⚠️ **Le voci qui sotto sono storiche** (fase 0-7b e PR #4/#9, 2026-09-08/09): restano come
> tracciabilità, ma sono tutte concluse. **La coda di lavoro vera è nel giro più recente in cima al
> file** (oggi: P1.11 → P1.12 → P1.15, poi i P2 di `docs/19` §4) e nella tabella di `docs/19` §4.

- [x] **Passo 7a — resilienza del run**: ESPN standings e scoreboard isolati; `fda predict` isola errori/storici per lega. Commit `d8063c1` + `0bcde50`; suite locale 20 passed; workflow `tests`: Success.
- **Passo 7b — storico NED1/POR1 (avviato)**: il mirror `datasets/football-datasets` non copre Eredivisie/Liga Portugal e `football-data.co.uk` è irraggiungibile dagli IP cloud (root cause: nei run #2/#3 NED1/POR1 avevano partite in programma ma 0 predizioni). Risolto puntando NED1 (`eredivisie`) e POR1 (`primeira-liga`) a un mirror `raw.githubusercontent.com` dedicato con la stessa struttura `season-XXXX.csv` (campo per-lega `datahub_base`), + alias squadra alle grafie N1/P1 (`For Sittard`→`Fortuna Sittard`, `Estrela`→`Estrela da Amadora`). Commit `85b515f`.
- **Verifica passo 7b**: suite locale **23 passed** (3 nuovi test); simulazione offline con CSV reali del mirror + calendario FotMob → NED1 **11** e POR1 **9** predizioni (tutte le partite in programma; le promosse ADO Den Haag/Cambuur/Académico Viseu/Marítimo ricevono prior dagli esiti di stagione in corso via FotMob). Workflow `tests` sul branch e check PR: **Success**.
- **PR #4 mergiata su `main`** nel commit `a3c5bf7` (merge eseguito dall'agente, precedente PR #2; tests verdi sul branch e mergeable CLEAN). `main` ora contiene lo storico NED1/POR1 da mirror dedicato.
- **Accuratezza live con gare reali (confermato)**: build del sito sui dati `main` (commit `783e59a`, 2364 fixture) → la pagina `accuratezza.html` ora mostra valutazioni reali pre-partita: **19 partite valutate** su 5 leghe, RPS **0,205** complessivo vs naive 0,244 (Bundesliga 2, LaLiga 6, Ligue 1 3, Premier 2, Serie A 6). NED1/POR1 ancora assenti perché i dati su `main` precedono il merge.
- **✅ Verifica live NED1/POR1 CONFERMATA (2026-09-08 13:33–13:40 UTC, verificato dal vivo)**: dopo l'attesa dei cron dilazionati (slot 10:00 mai partito), l'utente ha avviato manualmente il run `daily` dalla UI GitHub (Actions → daily → Run workflow su `main`): run **`34232722943`** (workflow_dispatch, 13:33:43Z, head `91f7109` post-merge) → job `run` **Success** in 1m33s (tutti gli step: test rapidi, collect→predict→build, commit dati, preparazione Pages) + job `deploy` **Success**. Commit dati **`6a31d39`** (13:35 UTC) verificato con `scripts/verify_ned_por.py`: **NED1 = 11 predizioni** (esattamente le ~11 simulate offline) e **POR1 = 11** (vs 9 simulate: da allora il calendario FotMob ha datato più gare di Liga Portugal, copertura maggiore), tutte con probabilità complete e `n_train=960` → **lo storico del mirror dedicato è stato scaricato e usato** (prima del merge: 0 predizioni per assenza di storico — le predizioni stesse sono la prova, più forte del log). Predizioni sensate (es. PSV 0,83 vs Sparta, Benfica 0,70 a Moreirense 09/09, Porto 0,73 a Casa Pia) e **alias mirror funzionanti** («Fortuna Sittard», «Estrela da Amadora»). `source_status`: FotMob OK 7/7, Understat OK (incl. NED1/POR1), unico errore il noto **ESPN standings 403** isolato (pre-esistente, non blocca). Limitazione: artifact/blob dei log Actions non raggiungibili dal sandbox (Azure) — verifica fatta su dati del commit, come per i run precedenti. Inclusa la gara NED1 di oggi NEC–Excelsior (16:45) → le prime valutazioni reali NED1/POR1 arriveranno in Accuratezza tra 1–6 giorni. Verificato sui dati live (`fixtures.parquet`, ultimo run): il calendario FotMob ha già gare NED1 **oggi** (12:00 UTC+, es. FC Utrecht–Go Ahead Eagles, NEC Nijmegen–Excelsior) e POR1 **da domani** (es. Moreirense–Benfica 19:45 UTC) → il primo `daily` post-merge avrà partite NED1/POR1 da predire: **la conferma dovrebbe arrivare entro poche ore/giorno, non serve altro de-risking offline** (simulazione già fatta: NED1 11 / POR1 9). ESPN standings: 403 isolato anche su NED1/POR1 nell'ultimo run (non blocca).
- **Dopo la conferma live (ora in corso)**: report pre/post in italiano già rifiniti su branch (PR #9: Accuratezza Δ/calibrazione, cartina tiri, momentum, precedenti, italianizzazione) — resta da seguire la risoluzione delle gare previste (incluse le 22 NED1/POR1 di questa settimana) che alimenterà Accuratezza con le prime valutazioni su Eredivisie/Liga Portugal.
- **Rifinitura Accuratezza + fix report partita (su branch, in PR #9 — verificato offline con build locale sui dati del repo)**: pagina `accuratezza.html` rinnovata: colonna **Δ vs naive** (su 19 gare il modello batte la base in ogni lega: Tutti **−0,038**; da −0,025 Serie A a −0,064 Premier), nuova card **Calibrazione** (prob. media prevista vs frequenza osservata: 1 = 42,4% vs 21,1% · X = 24,9% vs 36,8% · 2 = 32,6% vs 42,1% → nel campione attuale il modello sovrastima le vittorie in casa; n=19, dato indicativo) e per singola gara colonna **Top (✓/✗)** + **RPS**. Fix nei report partita (`match.html`): data in italiano con ora locale («domenica 06/09/2026, 15:30 (ora italiana)» — prima weekday inglese + etichetta «UTC» fuorviante) e spettatori formattati «57.000» (prima «57.000.0»); nuovi filtri Jinja `it_dt`/`it_num` in `build.py`. Suite **23 passed** (nuovi assert: Δ, Calibrazione, ✓, RPS 0,087 della previsione 62/21/17, data italiana, spettatori); **ruff: zero nuove segnalazioni** vs baseline (confronto diretto prima/dopo sui file toccati). Note: la pagina live su Pages si aggiornerà al prossimo `daily` su `main`.
- **Cartina dei tiri nei report post-partita (su branch, in PR #9 — verificato offline)**: nuova card «Cartina dei tiri» con due mezzi campi SVG (uno per squadra, porta a destra), pallini dimensionati sull'xG del tiro, colori per esito (verde gol / blu in porta / arancione tratteggiato bloccato / grigio fuori), tooltip con giocatore·minuto·xG e riepilogo «17 tiri · xG 3,06 · 5 gol». Dati: tabella `shots` (coordinate FotMob in metri 105×68, verificate: x max 104,3 / y mediana 34 = centro porta). `analysis.py`: `shot_map()` (trasformazione metri→px con clamp) + `goals` in `shot_summary`; template puro Jinja, nessun JS. Verifiche: **23 passed** (assert: 2 SVG, «xG 0,88» di Lautaro col decimale italiano, pannello vuoto Monza), build locale: **132/132 SVG ben formati** (parse XML), coordinate dei 5 gol di Mainz tutte dentro l'area e coerenti con l'xG (Mwene 0,89 a 6 m dalla porta); rendering PNG non possibile dal sandbox (niente cairo) — la verifica è strutturale/numerica, il primo `daily` su `main` darà la conferma visiva su Pages.
- **Italianizzazione completa del sito (su branch, in PR #9 — verificato offline con audit automatico su 146 pagine)**: richiamo utente «il sito è italiano e usiamo l'ora italiana». (1) **Ora italiana ovunque**: eliminati i 3 residui «UTC» (header di ogni pagina, timestamp della previsione, footer) → nuovo filtro `it_utc` (conversione UTC→Europe/Rome, gestisce anche ora solare/legale); orari delle liste già convertiti, ora dichiarati «(ora italiana)». (2) **Decimali con virgola**: nuovo filtro `dec` applicato a tutte le ~30 formattazioni dei template (xG, quote, gol attesi, Elo/DC, RPS/Brier/Δ in Accuratezza, λ nelle liste, rating) e `_f()` del narrativo («4,1 ammonizioni», «3,12 gol attesi»); fix «10.0 rigori in 24.0 gare» → interi. (3) **Meteo tradotto**: mappa `_WEATHER_IT` con gestione varianti combinate FotMob «Partly Cloudy/Wind» → «parzialmente nuvoloso e ventoso» (valori reali verificati sui dati: Mostly Clear, Fair, Showers in the Vicinity…). (4) **Date di rientro tradotte**: `_return_it()` («Mid October 2026» → «metà ottobre 2026»; catalogo completo dei valori FotMob su 437 indisponibili: Day to day, Doubtful, About 1-2 weeks, Out for season…). Verifiche: **29 passed** (6 nuovi unit test `test_i18n.py` su filtri e traduttori, inclusa conversione ora legale +2 / solare +1); **ruff 0 nuove segnalazioni**; audit automatico su tutte le 146 pagine generate: **0 residui** di UTC / weekday inglesi / meteo inglese / rientri inglesi / decimali con punto.
- **Momentum + ultimi precedenti nei report (su branch, in PR #9 — verificato offline)**: (1) nuova card **«Momentum della partita»** (solo post-partita): grafico SVG a barre minuto per minuto (verde = pressione casa, rosso = trasferta, scala FotMob ±100) con **marker dei gol** presi dalla cronaca e frase di sintesi quando una squadra domina («Momentum a favore di Mainz nel 66% dei minuti» su Hamburger 0-5 Mainz — coerente col risultato); dati: tabella `momentum` già raccolta (6134 righe, 67 partite), orientamento verificato empiricamente (valori fortemente negativi ai 5 gol di Mainz in trasferta). (2) **«Ultimi precedenti»** nel card Contesto (pre e post): fino a 5 incontri reali da tabella `h2h` (1554 righe dal 2009) con data, risultato, campionato e pill V/N/P **dal punto di vista della casa attuale** — bug di inversione V/P trovato e corretto in verifica (Roma–Atalanta: tutte P come atteso), poi inchiodato da test con riga non-pari (il campione aveva solo un 1-1); nomi risolti dalle due squadre in campo (le righe h2h coinvolgono solo loro), righe anomale scartate. Verifiche: **29 passed**; build locale: **198/198 SVG ben formati** (132 cartine + 66 momentum); ruff 0 nuove; audit italiano 0 residui. Nota: **diffidati non implementati** — fonte FotMob non li espone nei dati raccolti (ruoli lineup: starter/sub/unavailable/coach); da valutare fonte alternativa in futuro.

## Nota
- **Preview del sito dal sandbox** (scoperta 2026-09-08): dopo `fda build` (che scrive in `site/`, gitignored), si può servire con `python3 -m http.server 3000 --bind 0.0.0.0 --directory site` via start_process → l'utente vede il sito modificato nel browser (preview Arena) prima del merge. Il sito ufficiale è invece `uamisjd.github.io/football-deep-analyzer` (Pages), aggiornato solo dai run `daily` su `main`.
- Dal sandbox dell'agente la rete verso FotMob, `football-data.co.uk` e `raw.githubusercontent.com` può essere bloccata (si raggiungono solo `github.com` e `api.github.com`): le verifiche di rete vanno fatte in GitHub Actions o sul PC. I parser sono coperti dai test offline. (2026-09-08: PyPI risulta invece raggiungibile dal sandbox, quindi l'installazione dell'ambiente di test locale funziona.)
- Il mirror dedicato NED1/POR1 è configurato solo in `config/leagues.yaml` (campo per-lega `datahub_base`): facile da cambiare se il mirror diventasse irraggiungibile.
- Le quote The Odds API sono opzionali: vengono usate solo se `ODDS_API_KEY` è configurata.
- **Baseline ruff aggiornata (rilevata 2026-09-16, ventitreesimo giro, ruff 0.16.8 su `main` `6404a74`): 173 segnalazioni pre-esistenti** — prime voci: RUF046 39, UP017 26, BLE001 25, I001 21, FURB167 12, RUF100 12. Sostituisce la baseline del 2026-09-08 (47 con ruff 0.16.6: il numero è cresciuto soprattutto perché le regole di lint e il codice sono cambiati, non per un degrado improvviso) e il riferimento recente è **173** (docs/23: «175 → 173»). La CI `tests` esegue **solo pytest**, quindi ruff non blocca; la regola B.4 chiede ruff pulito sul codice **nuovo**, e su questo giro le 0 nuove sono verificate. Resta da valutare un turno di pulizia dedicato (100 delle 173 sono auto-fixabili con `--fix`).

## Prossimo passo (Fase 0/7 — un turno per riga)
1. ~~Scaffolding repo~~ ✅
2. ~~Client FotMob~~ ✅
3. ~~Client ESPN + Understat~~ ✅
4. ~~Storage + collect~~ ✅
5. ~~Modelli DC + Elo~~ ✅
6. ~~Sito statico + workflow + Pages~~ ✅
6b. ~~Primo run dal vivo in Actions e correzioni ai collettori~~ ✅
7. ~~Upgrade Node 24~~ ✅
7b. ~~storico NED1/POR1~~ ✅ **confermato dal vivo** (run `34232722943` del 2026-09-08 13:33Z, NED1 11 / POR1 11 predizioni, `n_train=960`, commit dati `6a31d39`). Prossimo: seguire la risoluzione delle gare previste (prime valutazioni NED1/POR1 in Accuratezza) e proseguire la roadmap.
7c. ~~Processo/handoff~~ ✅ (PR #6 in `main` `16db091`). Conferma live NED1/POR1 ✅ (2026-09-08). Prossimo: **Monte Carlo stagione** (fase 2: probabilità titolo/top-4/retrocessione per lega) o rifiniture report in base alle gare che si risolvono.
8. **Per la prossima sessione** (handoff 2026-09-08): (a) **🎯 PRIORITÀ UTENTE — parità di profondità delle schede partita**: «tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; schede di ogni partita ancora da giocare piene di contenuti, NED1/POR1 attualmente scarse». Diagnosi già misurata sui dati del run `6a31d39`: **classifica assente su TUTTE le leghe** (`espn_standings` vuota per il 403 cronico ESPN → fix = tabella di lega FotMob come fonte primaria) e **stagione xG con 1 sola gara su NED1/POR1** (Understat non le copre; fix = xG di stagione da FotMob); arbitro talvolta assente fino a pochi giorni dalla gara (si riempie da sola). Dettagli e lead tecnici in `BRIEFING_NUOVA_SESSIONE.md` sez. 3; (b) **Monte Carlo stagione** — fase 2 della roadmap `01` §8; (c) **Accuratezza**: seguire le gare NED1/POR1 in programma questa settimana (prime valutazioni Eredivisie/Liga Portugal); (d) opzionale: **pulizia ruff** (47 segnalazioni pre-esistenti, CI non bloccata).
9. **Questa sessione** (`arena/01a08295`): (a) **verifica sito/progetto** ✅ → `docs/05_verifica_sito_progetto.md`; (b) **P0** ✅ (formato arbitro/recupero, attribuzione fonti, ESPN→AVVISO); (c) **P1** ✅ (Open-Meteo, formazione pulita, playoff POR1). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo del meteo Open-Meteo** (run `daily` in Actions: attesa tabella `weather_forecast` popolata e schede «(previsione Open-Meteo)» sui futuri oltre le 48h) e di `future_days=7`; poi: (d) **B5 — collegare o rimuovere il client SofaScore** (codice inerte); (e) roadmap: **M2 diffidati**, **M3 schede giocatore**, **M5 Telegram/xG proprio**; (f) **pulizia ruff E501** (257 pre-esistenti, CI non bloccata).
10. **Questa sessione** (`arena/01a082c6`): **(a) card post-partita «Migliori in campo» per squadra** ✅ (top 3 per squadra con gol/assist/minuti/rating stagionale — avvio Fase 3; verificato su dati reali, suite 64 passed, 0 nuovi errori ruff). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo Open-Meteo + `future_days=7`** (primo run `daily` post-merge); poi: (b) proseguire **Fase 3** (schede giocatore vere: radar/percentili/xG per 90); (c) **B5 — SofaScore** (collegare o rimuovere); (d) **pulizia ruff E501/E741** (254 pre-esistenti).
11. **Sessione `arena/01a08613`**: **(a) fix header CalcioMetro ri-applicato** ✅ (PR #19 mergiata).
12. **Questa sessione** (`arena/01a08630`): **(a) card pre-partita «Fatti rilevanti»** ✅; **(b) B5 SofaScore rimosso** ✅ (codice morto). **Prossimo**: PR verso `main` → merge utente; poi Fase 3 schede giocatore, pulizia ruff.
13. **Questa sessione** (`arena/01a0864d`): **(a) Fase 3 schede giocatore** ✅ offline (hub + 7 tabelloni + 3.457 schede con radar/percentili/log; progetto `docs/07`); **(b) backfill finite esteso a tutte le leghe** ✅; **(c) fix i18n `unavailability_type`** ✅. Suite 73 passed, ruff 0 nuove. **Prossimo**: PR verso `main` → merge utente → **verifica dal vivo al primo `daily`** (catch-up `player_stats` 134→~231 partite; poi campioni percentili pieni per lega). Dopo: fase 3b opzionale (`playerData`: contratto, infortuni storici, trofei — rotazione ~80 richieste/run), pulizia ruff E501/E741, M2 diffidati (valutare fonte), fase 5 Telegram.
14. **Questa sessione** (`arena/01a09a79`, dopo il merge): **(a)** primo run `daily` in Actions → verificare che `fda predict --days-ahead 0` preveda **tutte** le gare in programma (~2.000, attese 1-2 minuti per il passo), che al primo run compaia la riga di collasso «previsioni: 3.210 → 138», **quante righe restano senza previsione** con lo storico reale (atteso: molte meno delle 150 offline), che `fda calibrate` produca `n_fit ≥ 1.200` e `bias_lambda < 0,10`, che `history.parquet` compaia in `data/processed` e che in Accuratezza il pareggio dichiarato cada dentro l'intervallo di Wilson con Over 2,5/BTTS entro 2 pp dall'osservato; **(b)** primo run del workflow `lab` (dispatch) → leggere `model_lab.parquet` su storico reale: **si cambia modello solo se** un candidato batte `dc_elo_prod` con IC 95% appaiato interamente negativo e il vantaggio vale in ≥5 leghe su 7; **(c)** P2 di `docs/13`: xG storici cumulati (candidato `dc_xg`), ξ per lega, tabella del laboratorio pubblicata in Accuratezza, dotplot dei punti nelle Proiezioni, lettura in frequenza dell'1X2 in scheda; **(d)** ora misurabile: **RPS per anticipo** sulla pagina Accuratezza (`made_at` vs `utc_kickoff` su tutto il calendario, §9.4 punto 3); **(e)** decisione utente sulla **Tappa 2** (schede per le partite lontane, §9.5).

15. **Questa sessione** (`arena/01a0a1a3`): (push poi riuscito: vedi ultimo punto) — il commit locale di questa sessione (docs/19 + STATO.md) **non era stato pushato al primo tentativo**: `gh auth status` → «The github.com token in GH_TOKEN is no longer valid». Regola `00` §8bis: il commit resta locale solo se il push fallisce; **la prima azione della prossima sessione è `git push origin arena/01a0a1a3-football-deep-analyzer`** (dopo aver ricollegato GitHub in Arena). Nessun file è andato perso: `docs/19_audit_totale_tre_aree_2026-09-14.md` e questo STATO.md sono nel working tree. **audit totale in tre aree** ✅ → `docs/19_audit_totale_tre_aree_2026-09-14.md` (33 riscontri: 9 P0, 14 P1, 10 P2; ogni punto con Osservato → Misura → Impatto → patch pronta → verifica). Nessuna modifica a codice/dati in questa sessione: solo il documento.
    - **Riscontro principale (nuova misura)**: scaricate le quote di chiusura Pinnacle di **tutte e 7 le leghe × 3 stagioni** dal mirror già usato per NED1/POR1 (`huhao930422-debug/football-odds-mirror`, 21 CSV, verificate via API GitHub). Aggancio al backtest con `fda.teams.canonical()`: **5.543/5.815 (95,3%)**, valutabili **4.372**. **RPS modello 0,19803 vs mercato 0,18843 → Δ +0,00960, IC95% [+0,00794; +0,01126], peggio in 7 leghe su 7.** Diagnosi: sottostima dei favoriti (decile 10: 0,728 previsto vs 0,799 osservato). Oggi `history.parquet` ha quote solo per NED1/POR1 (1.519/7.387) perché `football-data.co.uk` è usato solo come fallback su errore, mai come arricchimento.
    - **Correzione testata e RESPINTA** (da non ripetere): sharpening γ sul rapporto delle λ. Con griglia stretta [0,94;1,12] passerebbe il protocollo (ΔRPS −0,000457, IC interamente negativo, 6/7 leghe); con griglia onesta [1,00;1,40] **non passa** (ΔRPS −0,000417, IC [−0,001037; **+0,000203**], 4/7 leghe) e rompe la calibrazione del pareggio (25,20% vs 25,6% osservato). Da qui il riscontro metodologico §1.4: **la griglia degli iperparametri va pre-registrata nel codice** (`Candidate.grid`), altrimenti "IC<0" si ottiene accorciando la griglia.
    - **Difetti visivi misurati senza browser** (nessun headless installabile in sandbox): barre 1X2 che sommano 99/101 in **37/167** schede hero (la funzione `pct_triple` **esiste ed è testata**, è collegata in un solo punto: `build.py:276`); **5 pagine** con barre dei gol a `height:183%`; CSS inline duplicato **144,4 MB su 248,3 MB (58%)** del sito. Il tema chiaro — prima stimato come «7 token mancanti» — si è rivelato più grave e in parte diverso: **corretto in `docs/19` §3.2** (il fallback `@media` senza 18 token colpisce **solo chi non esegue JS**, perché lo script inline imposta già `data-theme`; i difetti gravi stavano in colori hard-coded che nessuna palette copriva: la «V» della guida-forma a **1,11:1**, il testo selezionato a **1,02:1**, il punteggio più probabile a **2,09:1**, l'hover a **1,78:1**, e il segmento ospite della barra 1X2 a **2,92:1 in entrambi i temi**).
    - **P0.1 + P0.2 ✅ ESEGUITI in questa sessione**: 22 token di foreground nuovi in `base.html`, ogni `color:` hard-coded sostituito, palette chiara ≥4,5:1 su **4** superfici, fallback OS completo e identico, gradienti della barra 1X2 corretti, override della cella modale eliminato, `--lose`/`--draw` del tema scuro portati a AA (4,59/4,57), leggende dei grafici tokenizzate in `match.html`. Aggiunti **14 test senza browser** in `tests/test_tema_contrasto.py` che **derivano dal CSS** l'uso di ogni token (testo → 4,5:1, grafica → 3:1), con le eccezioni dichiarate e motivate nel file. Verifica: **190 passed** (176 + 14), `ruff` pulito sul file nuovo, `fda build` 2m22s → 4.123 pagine, `verify_site.py` **nessun problema · 11.565 controlli**. **Non verificato**: la resa estetica (serve un browser; Lighthouse CI è il §3.11).
    - **Difetti di contenuto/dati**: colonna "Richieste" di *Stato fonti* = contatore **cumulativo** (NED1/POR1 accreditati di chiamate Understat mai fatte); pagina *Accuratezza* con **84 gare di cui solo 3 del modello corrente** (per questo la tabella live e il backtest sembrano contraddirsi); baseline naive hard-coded 45/27/28 che gonfia il Δ fino a **+0,00205**; **34% delle curiosità FotMob scartate in silenzio** (20% delle schede ne mostra 2); etichette arbitro con soglie assolute (POR1 media 5,04 gialli/gara vs soglia "molto severo" 5,0); `verify_site.py` con **11.590 controlli ma zero invarianti di pubblicazione**.
    - **Verificato e chiuso** (non intervenire): xGOT 99,6-99,9% su 7 leghe; parità post-partita **100%** su 6 tabelle; 12.089 `<svg>` tutti accessibili; 0 `nan`/`None`/residui inglesi reali; dotplot, matrice punteggi, in-play, Wilson, backtest già coperti da `verify_site.py`; `PlayerCatalog` 0,3 s (la doppia costruzione **non** è un collo di bottiglia).
    - **P0.3 ✅ ESEGUITO**: `fmt.pct_triple(p, nd=0)` generalizzato (unità intere di `10**-nd` → somma esatta; tie-break reso coerente con la docstring: la parità ora premia (1, X, 2), prima premiava l'ultimo), filtro Jinja `pct3`, **tre** barre collegate (non due: anche la mini-barra di `_matchlist.html:51`, che calcolava pure il favorito sui grezzi), `aria-label` aggiunto alla barra hero, **wrapper duplicato di `build.py` eliminato** (15 righe di copia di riserva), `fav_i` ora = indice del massimo *stampato*. Nuovo controllo di pubblicazione **`verify_site.py [11a]`** (larghezze sommano 100 = etichette = aria-label; un solo favorito ed è il massimo; il wordmark dell'header resta fuori da solo). **Trovato e corretto anche un difetto del verificatore**: il controllo `[10]` ricalcolava i passi con arrotondamento indipendente tollerando 0,06 pp, cioè *difendeva* il difetto — dopo la correzione segnalava 14 pagine giuste; ora confronta con `pct_triple` e lo scarto ammesso è zero. Misura sulla popolazione (2.152 previsioni): **565 (26,3%)** non sommavano 100 con interi indipendenti, **521 (24,2%)** con un decimale → **0** con `pct_triple` (la stima dell'audit, 22,2%, era prudenziale). Verifica: **193 passed** (3 test nuovi, inclusa una scheda generata end-to-end con il vettore `0,4049/0,4049/0,1902` che prima stampava 99%), `fda build` 2m13s, `verify_site.py` **nessun problema · 14.108 controlli** (erano 11.565) con **565 barre** verificate, ruff invariato sul baseline pre-esistente.
    - **Preview del sito attiva** (richiesta utente): server statico sulla porta **8080** che serve `site/` — utile per la resa dei due temi, che in sandbox non è verificabile senza browser.
    - **P0.4 ✅ ESEGUITO**: causa confermata in `advanced.py:goals_view` — la scala era `pmax = max(p.max())` sui soli totali 0…cap mentre la barra della coda usava `tail / pmax`, quindi nelle gare ad alto λ la barra «7+» usciva dal contenitore (**9 barre su 2.152 previsioni, max 183%**; le 5 pagine pubblicate avevano λ totale 4,68-5,50). Correzione: `scala = max(p.max(), tail)`, **senza clamp** (limitare a 100 avrebbe mentito sul grafico). **Secondo difetto trovato correggendo il primo**: la didascalia dichiarava «nel **90%** dei casi il totale resta fra `q10` e `q90`» — copertura garantita >80%, misurata media 87,4% — e con il percentile nella coda stampava «7» invece di «7+» (intervallo aperto letto come chiuso). Provata e **scartata** l'alternativa 5°-95° (frase vera ma intervallo quasi sempre «fra 0 e 6» = tutto l'asse, quindi inutile): ora `goals_view` pubblica `copertura` (massa vera fra q10 e q90) e `q90_label`. Tre invarianti nuove in `verify_site.py [9]` (altezza ≤100% **e** = proporzione ricalcolata; barra più alta a piena scala; copertura 80-100% e uguale alla ricalcolata). Corretta anche una fragilità del verificatore: 3 regex della didascalia dipendevano dagli **a capo del template** e hanno segnalato 160 pagine corrette dopo una riformattazione → ora `\s+`. Verifica: 194 passed, `fda build` 2m23s, `verify_site.py` **nessun problema · 15.548 controlli** (erano 14.108), `height:[0-9]*%` oltre 100 → **0**.
    - **Residuo di P0.3 chiuso**: anche il tooltip dell'«Esito più probabile» in testa alla scheda arrotondava tre volte in modo indipendente (`match.html:21`) → ora usa lo stesso vettore `pct3` della barra.
    - **Audit delle 10 sezioni della scheda partita ✅ consegnato** → `docs/20_audit_dieci_sezioni_scheda_partita_2026-09-15.md` (14 riscontri: 1 P0, 6 P1, 7 P2). Metodo: censimento automatico su tutte le **76 schede pre-partita** (371 totali, 295 post) + lettura integrale di due schede vere (Roma-Inter ITA1 ed Estrela-Académico Viseu POR1) + verifica incrociata sui Parquet. I cinque riscontri principali, tutti misurati: **(1)** «Come nasce questa probabilità» pubblica come passo 2 la colonna `blend_p_*` chiamandola «Media con i rating Elo — peso DC 70%, Elo 30%», ma da quando la produzione è `ENSEMBLE_MODE="tilt"` quella colonna è il vettore della **griglia sulle λ inclinate**: scarto medio dalla media vera **1,18 pp**, massimo **7,87 pp**, **8 gare su 2.080** con esito più probabile diverso, e il passo del tilt non compare affatto; **(2)** tutte le **371** schede dichiarano «`verify_site` controlla **11.582** numeri» scritto a mano nel template — il valore vero è **15.548** (−25,5%) ed era stantio già prima; **(3)** il tooltip di ogni riga xG di «Scontro tattico» dichiara «media stagionale **FotMob**» mentre il valore viene da **Understat** in 5 leghe su 7, con bias sistematico **+9,5%** (massimo 0,641 xG/gara: Roma 3,45 contro 2,81 sulle stesse 4 gare); **(4)** nella stessa tabella le componenti FotMob (2,55 + 0,25 = 2,80, scomposizione che **chiude** dentro FotMob) non sommano al totale della riga sopra (3,45, Understat) → sembra un errore di conto; **(5)** «Come arrivano» accosta punti-sopra-attesi e tendenza degli xG senza soggetto: **10 righe su 29 (34%)** si leggono in contraddizione. Parità fra leghe misurata: POR1 **5,4** righe narrative contro NED1 **7,7**, e **0** occorrenze di «giocatore di peso» nelle assenze POR1 contro 7 in ITA1 (è la regola che parla solo nei casi estremi, non mancanza di dati: la forma è stampata più sotto). **Ipotesi smentita dai dati** (registrata nel doc): che il Δ attribuito alla «Calibrazione» fosse in realtà prodotto dal tilt — no, la griglia sulle λ pubblicate riproduce l'1X2 con scarto max 0,038 pp e il Δ della calibrazione è davvero suo.
    - **Prossimo passo — direttiva utente 2026-09-14 (prioritaria)**: applicare le correzioni di `docs/20` nell'ordine del §15 — prima **#2** (una riga di template, affermazione falsa su 371 pagine) e **#1** (catena a quattro passi, ingressi già tutti salvati), con l'invariante nuova in `verify_site.py`: *ogni Δ pp pubblicato deve equalare la differenza fra due passi adiacenti stampati*. Poi #3+#4 (stessa tabella), #5+#6+#7 (stessa riga di testa), #10+#13 (narrativa e parità), infine le finiture. Vecchio riferimento: audit profondo delle **10 sezioni della scheda partita** (Analisi pre-partita, Previsione del modello, Risultati esatti più probabili, Come nasce questa probabilità, Scontro tattico, Fatti rilevanti, Come arrivano, Confronto di stagione, Contesto, Verifica approfondita) perché siano accurate, intuitive, precise, profonde, di qualità e logica. Poi i P0 residui: **P0.5** (CSS esterno, −144 MB), **P0.6** (composizione del campione in *Accuratezza*), **P0.8** (invarianti di pubblicazione). La baseline naive hard-coded `45/27/28` di `build.py:500` (§1.6, P1) resta da sostituire con la frequenza osservata del campione.
    - ✅ **Push riuscito in questo turno** (il token GitHub è tornato valido): `b034bbb` (docs/19) e `c645f96` (tema chiaro AA) sono su `arena/01a0a1a3-football-deep-analyzer`. Aperta **PR #32** verso `main` → check `test` **pass in 50s** (verifica indipendente in Actions dei 14 test nuovi). Merge di competenza dell'utente.

## Decisioni aperte
- **Backfill partite finite esteso a tutte le 7 leghe** (deciso 2026-09-09, `arena/01a0864d`, fase 3): prima il backfill completo di stagione riguardava solo NED1/POR1 (per l'xG FotMob); ora vale per tutte (statistiche giocatore complete dall'1ª giornata + report storicizzati). Costo: 97 richieste una tantum (stagione in corso al 9/9), poi ~10/lega a giornata; tetto `--max-backfill=40`/lega/run invariato. In attesa di conferma dal vivo al primo `daily` post-merge.
- **B5 SofaScore — RIMOSSO 2026-09-09** (decisione del 2026-09-09, eseguita in `arena/01a08630` come commit dedicato dopo gli insights). Cancellati `sources/sofascore.py`, `tests/test_sofascore.py` e la voce in `config/sources.yaml`. Il client non era mai collegato a `collect.py`. Reversibile da git (`805bcb5`). Il catalogo in `02` resta come nota (403 da IP datacenter).
- **Qualità delle schede partita** (direttiva utente 2026-09-08, **prioritaria**): tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; le schede di ogni partita da giocare devono essere piene di contenuti e servizi, con **parità completa tra le 7 leghe** (NED1/POR1 segnalate scarse). Primo intervento: classifica da FotMob (ESPN standings 403 cronico) + xG di stagione per NED1/POR1 (Understat non le copre). Registrata anche in `BRIEFING_NUOVA_SESSIONE.md` sez. 3 e 5.
- **Merge delle PR** (deciso 2026-09-08): **il merge lo esegue SEMPRE l'utente, MAI l'agente**. L'agente apre la PR quando serve (sez. D di `00_regole_di_lavoro.md`), monitora i check e avvisa con la frase fissa «👉 Tutto verde: è il momento di fare Merge (PR #N).», poi aspetta senza fare merge. (La decisione era stata registrata nel commit locale `9d39a21` della sessione precedente, mai pushato e andato perso alla chiusura → riapplicata sui docs in questa sessione.)
- **Fonte storica NED1/POR1** (deciso 2026-09-06): mirror GitHub `raw.githubusercontent.com` di football-data.co.uk dedicato (via `datahub_base` per-lega), scelto perché il mirror datahub copre solo 5 leghe e `football-data.co.uk` diretto è irraggiungibile dagli IP cloud.
- Nessuna altra bloccante. (Telegram bot e protezione accesso: fasi successive.)

## Direttive utente persistenti — aggiunta 2026-09-08
- Quote bookmaker: **non interessano all’utente**, non sono requisito editoriale e non vanno prioritarizzate.
- Requisito principale: schede partita complete, accurate, profonde e con parità tra le 7 leghe; nessun dato realmente mancante quando la fonte dovrebbe averlo pubblicato.
- Distinguere sempre `presente`, `atteso dalla fonte` e `mancante`; non inventare dati per riempire campi.
- Fare ricerche approfondite su GitHub e altre fonti prima di scegliere integrazioni; annotare i risultati in `BRIEFING_NUOVA_SESSIONE.md` per gli agenti futuri.
- Implementato audit automatico delle schede future in `src/fda/site/audit.py`, visualizzato in `stato.html`; test suite: 49 passed.
- **Riferimento di mercato come metro esterno** (misurato 2026-09-14, `arena/01a0a1a3`, `docs/19` §1.1): le quote di chiusura Pinnacle di tutte e 7 le leghe sono disponibili sullo **stesso mirror già in uso** per NED1/POR1. Decisione da prendere: **(A, raccomandata)** le quote restano fuori dalla pipeline e diventano solo un benchmark riproducibile in CI (`scripts/benchmark_quote.py` + job mensile) — rischio zero sul modello; **(B)** `datahub_base` esteso alle 5 grandi leghe (5 righe di config) per avere le quote dentro `history.parquet` e nel laboratorio — rompe `tests/test_models.py:79-90` che codifica la decisione opposta e lega il progetto a un repo personale. **Non è in contraddizione con la direttiva utente sulle quote** (non sono un requisito editoriale): qui non si pubblicano quote, si misura la distanza del modello dal livello di mercato.
- **Candidato γ-sharpening RESPINTO** (2026-09-14, `docs/19` §1.3): da non ritestare con griglia stretta. Se si vuole attaccare la sottostima dei favoriti (§1.2), la strada è la **ricalibrazione del vettore 1X2** (temperatura) come candidato del laboratorio con griglia pre-registrata (§1.4, §1.2).
- **Giro di revisione lingua, secondo atto** (2026-09-17, branch `arena/01a0afc8`, PR #50,
  `docs/25` §9): la card «Vita del club» pubblicata su GitHub Pages non è ancora quella della PR
  (il merge lo fa l'utente). Nel build della PR i due titoli spagnoli di Málaga-Villarreal sono
  respinti dal gate; il contatore «in un'altra lingua» è stato aggiunto e messo **prima** degli
  altri vagli (prima i titoli stranieri venivano contati come «servizio o cronaca»: 131 → 11, con
  121 dichiarati come stranieri); due fatti nuovi dai nostri dati («Porta inviolata»,
  «Finale da brividi» sui gol subiti dopo il 75') hanno portato le righe «Da sapere» da 227 a 295
  su 68 partite, sempre con 0 titoli non italiani. Test 371, `verify_site` 94.024 controlli, ruff
  baseline.
- **Deroga al merge (2026-09-17):** su richiesta esplicita dell'utente («Please merge the pull
  request»), la PR **#50** (lingua italiana garantita + card «Vita del club»; commit `cab20ec` e
  `4053e5c`) è stata **fusa dall'agente** su `main`, in deroga alla regola D che riserva il merge
  all'utente. Condizioni: CI verde (`test pass`), PR `MERGEABLE · CLEAN`. Registrato anche in
  `docs/13` §«Deroga al flusso di merge». La regola generale resta: senza richiesta esplicita
  dell'utente, l'agente apre la PR e si ferma.
