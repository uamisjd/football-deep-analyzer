# STATO DEL PROGETTO (checkpoint — aggiornato a ogni turno)

**Ultimo aggiornamento:** 2026-09-17 (sessione `arena/01a0abd9`, venticinquesimo giro) — **la card «Vita del club» approvata dall'utente è passata dal prototipo alla produzione, e vale per tutte le partite in programma** («si mi garba… procedi»; «verrà fatto anche per le altre partite in programma giusto?» → sì: nessun hardcode sulla partita di esempio). Il port tocca cinque file di codice più test e documento: `sources/news.py` (due edizioni Google News per squadra — italiana e lingua del campionato — con `GOOGLE_EDITIONS`/`editions_for()`; `news_value()` come gate «annuncio / piatto»; `TOPIC_RULES` riscritte in 12 categorie multi-lingua: Squalifiche, Infortuni, Panchina, Spogliatoio, Società, Fuori dal campo, Tifoseria, Stadio e città, Mercato, Squadra, Dichiarazioni, Club), `collect.py` (paese della lega → edizione locale; `ricerche` contate per richiesta effettiva: con due query `len(teams)` mentiva), `site/analysis.py` (`team_news()` riscritto — finestra **7 giorni** senza recupero, gate del valore con conteggi, punteggio freschezza/sostanza/rilevanza, tetto 3 per squadra · 2 per categoria · 1 per soggetto, **«in riserva»** fino a 2, imbuto dichiarato — più `news_sapere()`), `templates/match.html` (card «Vita del club» con «Da sapere», conteggi e caso vuoto motivato), `scripts/verify_site.py` (`[20]` riscritto: ricalcola imbuto, categorie, riserva, finestra, gate e «Da sapere»; **67 pagine** riconciliate), `config/sources.yaml` (tetto richieste 200 → 320 = 244 richieste/run con due edizioni). **Regole in produzione:** finestra `kickoff−7g`; niente infortuni/squalifiche/mercato (hanno la loro card e si contano a parte); annunci, logistica e fatti «piatti» fuori e contati; il titolo deve parlare della squadra; −4 se nomina un altro club; diversità 3/2/1 con riserva; «Da sapere» derivato dai nostri dati (stadio diverso dall'abituale, panchina cambiata da ≤3 gare). **Misure sul port** (tutte le 2.055 partite future del 17/09, Parquet committati): 2.582 righe in finestra → 1.130 di servizio/formazioni/seconde squadre, 32 fuori categoria, 9 annunci, 270 «piatti», 120 candidati → **24 pubblicati in 12 partite** (9 Panchina, 6 Società, 5 Tifoseria, 2 Spogliatoio, 1 Fuori dal campo, 1 Club), su 4.110 colonne-squadra; fra i pubblicati «Bologna, esonerato Tedesco: arriva Palladino con contratto fino al 2029», l'inchiesta su Lotito/Lazio, il caso Maldini, la protesta dei tifosi del Genoa, la sentenza rinviata dell'Udinese. **Quattro difetti trovati misurando e corretti:** le voci italiane di inchieste e sentenze finivano **fuori categoria** («indagine», «sentenza», «perquisizioni», «minacce» non erano nelle regole) e il pattern spagnolo `contrat` pescava il «contratto» italiano di un giocatore; aggiunte anche formazioni e seconde squadre al filtro di servizio («Sambenedettese-Atalanta U23» era stata pubblicata come «Società»); i **titoli di agenzia in maiuscolo** sfuggivano al dedup dei soggetti (due volte lo stesso esonero per il Bologna: ora i titoli senza minuscole passano da una lista di parole di servizio); e una **voce di un'altra squadra** entrava perché il nome della città è anche il nome del club (nuovo contatore «su un'altra squadra»). **Verifiche:** suite **360 passed**; `fda build` exit 0 (**374** pagine partita, 2.364 fixture, 7.478 giocatori); `verify_site` **0 problemi · 92.892 controlli**, `[20]` su 67 pagine; ruff sul baseline (173). **Non verificabile dal sandbox:** Google News è irraggiungibile (TLS azzerato: `curl` 000, `requests` `SSLZeroReturnError`), quindi la seconda edizione si misurerà al primo run di raccolta in Actions; `data/processed/news.parquet` (6.877 righe) contiene la sola edizione italiana, ed è la ragione per cui la card oggi resta quasi sempre vuota — il rifornimento è la raccolta, non un allargamento dei filtri. **Aperto:** (a) lo stadio di `5868063` è sbagliato a monte (il dato della partita dice «Villamarín», le due gare interne precedenti sono a La Cartuja): la card lo **dichiara** in «Da sapere» invece di inventare, la correzione del `location` JSON-LD resta da fare; (b) il testo pubblicato resta quello della fonte (la riscrittura in italiano era fatta a mano nel prototipo). **MERGE FATTO (17/09, deroga esplicita):** l'utente ha scritto «Please merge the pull request» e l'agente ha eseguito `gh pr merge 46 --merge` dopo i controlli pre-merge (check `test` **pass** 1m4s run `35163094351`, PR **MERGEABLE · CLEAN**, `git status` vuoto, HEAD locale = remoto `b1f5bf3`, **nessun file di dati** nella PR): merge commit **`6f2bf98`** in `main` alle **08:44:27Z**, deroga registrata in `docs/13` **§9.14** (con la nota che il sandbox ha riconsegnato il repository **shallow**: `main` con 1 commit locale, quindi i controlli sono stati fatti sulle API GitHub, non con un `git diff` locale). Col merge sono partiti `tests` **`35201313366`** e il **daily** **`35201313177`** — la **prima esecuzione reale della seconda edizione** in raccolta (≈244 richieste notizie) e il primo build in `main` con la card nuova. **Da verificare in quel run:** gate `verify_site` verde e deploy Pages con «Vita del club»; in `news.parquet` l'arrivo della seconda edizione (righe più che doppie, titoli in lingua locale); in *Stato fonti* `news:NEWS` con ≈244 richieste e non più 132; le colonne vuote in calo dalle 4.086 misurate sull'archivio italiano. **Primo daily post-merge (`35201313177`, commit dati `ea6a099`): success, deploy success, e la seconda edizione funziona** — `news:NEWS` da 132 a **244 richieste**, 21.990 articoli visti, `news.parquet` da 6.877 a **16.623 righe** (mediana 125/squadra); card misurata sull'archivio nuovo: **65 fatti in 32 partite** (erano 24 in 12), fra cui Betis–Getafe con Pellegrini/Ezzalzouli, Bordalás e il bilancio del Getafe. **Tre difetti nuovi trovati in quei titoli e corretti** (`news.py`, `analysis.py`, card, verificatore): `incidente` da solo non basta (la «maglia di Ceuta» finiva fra i guai giudiziari); i contratti generici (`vertrag`, `contrat`, `renewal`, `manager`) non sono la panchina («Profi-Vertrag für Tim Binder»); e lo stesso evento poteva entrare due volte in categorie diverse — nuovo `un_evento()` con contatore `doppioni`. Misura dopo: **58 fatti in 29 partite**, 3 doppioni fermati, 8 voci di altre squadre contate. Verifiche con l'archivio nuovo: 360 test, `fda build` exit 0 (375/2.364/7.488), `verify_site` **0 problemi · 93.233 controlli**, ruff 173 = baseline. **Prossimo passo:** PR **#47** (sola documentazione: §9.14 + STATO + docs/24 §3.6) da far fondere all'utente; poi il sito pubblicato mostra la card nuova. Il port è in **tre commit** su `arena/01a0abd9` (83b8eb4 port, c25a5e6 i due difetti trovati misurando, 8f0adb7 ripristino di `source_status.parquet`), PR #46 aggiornata e verde; l'anteprima è pronta e servita su **porta 8081** (`/home/user/anteprima-scheda/`): `index.html` = la scheda di Betis–Getafe generata dal build, `piena.html` = Parma–Genoa con la card piena, `approvata.html` = la card scritta a mano che l'utente ha approvato, `confronto.html` = il prima/dopo. Ora che il merge è fatto, la branch `arena/01a0abd9` porta solo gli aggiornamenti documentali successivi (§9.14 e questo STATO), che vanno in una **PR di sola documentazione** da far fondere all'utente.

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0abd9`, ventiquattresimo giro) — **verifica totale del progetto e del sito su richiesta dell'utente, e le due card che l'utente aveva dichiarato inutili rifatte** (misure, regole e prima/dopo in [`docs/24`](24_bollettino_e_mercato_2026-09-16.md)). Direttiva dell'utente, riportata a verbale: «ultime dalle società così com'è fatto è inutile per me che ho bisogno di avere informazioni importanti»; e in più: navigare il sito pubblicato come una persona, fare uno studio quantitativo, qualitativo e visivo, verificare che non si sia perso nulla. **Misure prima (sito pubblicato, 70 schede).** *Mercato*: 1.115 righe, **630 (56%) etichettate «rinnovo di contratto»** — erano trasferimenti veri (`contract` mappato su rinnovo), **28%** delle righe fuori dalla finestra in corso (stagione 2025-26), 132 importi resi come «—», scelta delle 4 voci per data e non per importo. *Ultime dalle società*: **535 voci, 58 duplicati nella stessa pagina, 8 pagine con lo stesso titolo in entrambe le colonne**, 37,8% di titoli di servizio sul corpus `news.parquet` (6.877 righe: dirette, «risultati in diretta, testa a testa e formazioni», pronostici, pagelle), ordinamento per parole chiave con **la data crescente a parità** (la voce «più recente» era la più vecchia), nessuna categoria, nessun legame con i nostri dati. **Dopo.** Mercato: finestra **ricavata dai dati** (gap ≤21 giorni) con gli estremi pubblicati, importi leggibili in italiano (`12,5 M€`, `850 k€`, `prestito`, `gratuito`, `importo non noto`), bilancio sui soli importi pubblicati con il numero dei non noti, ordine per importo, e **«Già in campo»** (quali arrivi sono nella distinta di questa partita): 1.049 righe, **0 fuori finestra, 0 «—»**. Bollettino: filtro e categoria dichiarata, finestra vera `kickoff−12g`, gate di soggetto, dedup condiviso fra le due colonne, **un soggetto per categoria**, **recupero a 45 giorni** con etichetta «fuori finestra» e imbuto stampato («37 titoli esaminati · 0 pertinenti: qui le più recenti»): **135 voci, 0 di servizio, 0 duplicati in pagina, 0 coppie sullo stesso soggetto**, 66/140 colonne vuote **dichiarate** (erano 91/140 prima del recupero, cioè 65%). **Due difetti trovati misurando**: (a) **27 voci su 162 (17%) erano «Come acquistare i biglietti …»** — la card dichiarava di escludere le pagine di servizio e non lo faceva; (b) il filtro «un soggetto per categoria» applicato in lettura teneva **la voce più vecchia** del doppione (le righe arrivano in ordine di data crescente), quindi va applicato dopo l'ordinamento. **Verifiche**: suite **356 passed** (353 → +3), `fda build` exit 0 (376/2.364/7.478), `verify_site` **0 problemi · 92.271 controlli** (89.448+; `[20]` ora controlla anche: nessuna voce di servizio, un soggetto per categoria, recupero dichiarato, etichetta «fuori finestra»), ruff **173 = baseline, 0 rilievi nuovi** sui 5 file toccati. **Studio visivo/igiene** su 4.131 pagine (123 MB): **107.563 link interni, 0 rotti**, 0 id duplicati, sitemap 4.130 URL tutti risolti, 0 pagine senza `title`/`description`/`h1`/`lang`/`viewport`, 0 immagini senza `alt`, **141 tabelle larghe (≥8 colonne) senza scroll orizzontale → 0** (tabella «I giocatori che decidono» e «Accuratezza per mercato» dentro `tablewrap`), contrasti testo/fondo 5,15-16,11:1 (AA). Il sandbox **non ha browser** (Chromium bloccato come GitHub Pages): nessuno screenshot, il limite è dichiarato in `docs/24` §0. **Navigazione manuale del sito pubblicato**: home, `prossime`, `risultati`, `accuratezza`, `stato`, `info`, `giocatori/index`, una scheda pre-partita per intero, un 404 voluto — nessuna sezione vuota o rotta. **Prossimo passo**: PR aperta verso `main` — **il merge lo fa l'utente** (regola D); il sito pubblicato continua a mostrare le card vecchie finché la PR non è fusa. Residuo dichiarato: 66 colonne su 140 senza notizie utili perché la fonte, per quelle squadre, in 45 giorni pubblica solo dirette e pronostici (esperimento con query a operatori fatto e scartato: restituisce articoli di mesi prima).

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0ab67`, ventitreesimo giro) — **verifica totale del progetto su richiesta dell'utente, e il fix che mancava: anche lo scoreboard ESPN entra in backoff** (diagnosi, misure e prova end-to-end in `docs/23` §5). **Il progetto è sano, misurato ora e non presunto**: suite **347 passed** (343 a inizio turno), `fda build` exit **0** con **376/2.364/7.478**, `verify_site` exit **0** con **0 problemi · 89.448 controlli**, ruff **173** = baseline (0 nuove), `git status --porcelain` vuoto, **0 file eliminati** negli ultimi 40 commit, nessun TODO/FIXME reale nel codice; i 3 file citati nei docs e inesistenti (`sofascore.py`, `test_sofascore.py`, `test_collect.py`) sono riferimenti storici a codice rimosso di proposito il 2026-09-09. **PR #43 fusa dall'utente** (`uamisjd`) alle **18:03:11Z**, merge commit **`6404a74`**: registrato qui come chiede la regola D («dopo il merge l'agente aggiorna STATO con il commit di merge e indica il prossimo passo»), perché il giro precedente si chiudeva ancora con «PR aperta verso `main`». Nessuna deroga da scrivere in `docs/13` §9: il merge non è dell'agente. **Il primo daily post-merge è verde** (run **`35131980208`**, 14m06s): gate **`verify_site` success**, deploy Pages **18:23** (sha `6404a74`), dati committati **`c969b6b`** (raccolta 18:09-18:22 UTC). Confermate dal vivo le voci che il ventiduesimo giro lasciava «da verificare»: **8 righe `espn:*` SOSPESO con 0 richieste** (prima: 76 fallimenti consecutivi e 1 richiesta), CSS esterno servito (`site/assets/site.css`, 4.131 pagine, 125 MB), **0 tooltip «/90′»** sulle quote, **2.825** schede giocatore con marcatori ◇/◎. Il daily **schedulato** `35131059680` risulta rosso perché è partito ~9 minuti **prima** che il fix entrasse in `main`: ha eseguito il codice vecchio, non è un difetto nuovo. **Il difetto nuovo, trovato grazie alla riga separata introdotta dal §3: lo scoreboard ESPN era l'unica fase ESPN fuori dal backoff.** Misurato per la prima volta: **HTTP 403 su 7 leghe su 7**, 1 richiesta ciascuna, **0 righe di dati**; e in `data/processed/` **non è mai esistita** una tabella `espn_events`, `espn_team_stats` o `espn_standings` → ESPN non ha mai portato una riga in produzione. La frase «lo scoreboard, che risponde, resta attivo» (`docs/22` §3 e docstring di `backoff.py`) era un'**assunzione**, non una misura, ed è ora annotata come smentita. Costo: **7 richieste a run = 35 al giorno (~1.050 al mese) per zero righe**, più **7 righe rosse «ERRORE»** a ogni run in *Stato fonti* (`ok=False, warn=False`) — lo stesso rumore identico che P1.9 aveva tolto alla classifica perché «rendeva invisibile ogni guasto nuovo». **Fix**: `sospensione(store, f"espn scoreboard:{lg.key}", "espn scoreboard")` prima della chiamata in `collect.py`, `"espn scoreboard"` aggiunto a `_WARN_NON_BLOCCANTE` (degrado coperto da FotMob → AVVISO), testo della `note` pubblicato corretto; **`scripts/verify_site.py` non è cambiato**, perché l'invariante **[28]** è già generica su qualunque riga «SOSPESO» (motivo + piano di ritentativo + 0 richieste). Nessun impatto sui contenuti: nessuna sezione del sito legge `espn_events`/`espn_team_stats`, e l'unico riferimento a una tabella ESPN è `analysis.py:601` (`espn_standings`, riserva della classifica il cui primario è FotMob, **132** righe). **Prova end-to-end sul caso reale** (righe vere del run 18:22 + 4 run falliti per lega fino alla soglia + la riga di pausa che `collect_league` scriverebbe, poi build e verificatore; Parquet ripristinato e albero pulito): `fda build` exit 0, `verify_site` **0 problemi · 89.455 controlli** (+7), *Stato fonti* con **15 righe ESPN tutte SOSPESO con 0 richieste · 0 righe in ERRORE** (prima: 7 rosse). Suite **347 passed** (+4: serie propria della fase, indipendenza delle due fasi, integrazione con contatore delle richieste, costo a regime ora parametrizzato su **entrambe** le fasi). **PR #39 chiusa come superata** (decisione dell'utente, 18:46Z): solo documentazione, **CONFLICTING/DIRTY** dalle 13:07, e il suo contenuto (deroga di merge di PR #38) è già in `main` come `docs/13` **§9.11** — la sua §9.10 avrebbe duplicato il numero già assegnato alla deroga di PR #35. Commento con le ragioni sulla PR. **Regola A5 applicata**: in `STATO.md` erano arrivati **10 giri** (66,9 KB), quindi i giri dal **tredicesimo al ventesimo** sono archiviati in [`STATO_archivio_2026-09-16.md`](STATO_archivio_2026-09-16.md) e qui restano gli ultimi 3 + i link. **P2.5 chiuso**: indice completo dei **27 file di `docs/`** (23 documenti numerati — il 09 non è mai esistito — + briefing + `STATO.md` + 2 archivi) nel briefing §6, albero `src/fda/` aggiornato (`backoff.py`, `diagnostics.py`, `site/rates.py`, `sources/news.py`, `sources/openmeteo.py`, `models/` completo), «`tests/` 20+ test» → **347**, limitazione «ESPN standings 403 cronico e isolato» corretta in «ESPN 403 su tutte e tre le fasi, tutte in backoff». **Da monitorare, non difetti**: (a) **5 partite del 19/09 hanno perso la panchina probabile** (101 righe `sub`) — tutte `lineup_type=predicted` a 3 giorni dalla gara, e oggi le **25** partite senza panchina sono tutte `predicted` mentre le **351** con panchina sono `standard`/`lastStarting11`; `sources/fotmob.py` non è cambiato fra PR #41 e PR #43, quindi è la fonte che ritira la panchina probabile e la policy «snapshot autorevole» (`replace_by="match_id"`, motivata in `store.py`) propaga la perdita: da riguardare se il numero cresce; (b) **sonda dei fallback (P1.10)**: *Stato fonti* dice «nessuna registrazione», il passo è in `lab.yml` (cron lunedì 05:30) ed è arrivato in `main` solo oggi → primo esercizio **lunedì 21/09** o dispatch manuale dell'utente (il token dell'agente dà 403 sul dispatch); (c) la serie dello scoreboard parte dal run `35131980208`, quindi la sospensione scatta dopo `BACKOFF_FAILS = 5` run (~2026-09-17): da confermare in *Stato fonti* (righe `espn scoreboard:*` SOSPESO con 0 richieste e richieste ESPN del run da 7 a 0). **PR #44 fusa dall'agente su deroga esplicita dell'utente** («Please merge the pull request», 2026-09-16; regola D, eccezione con deroga): merge commit **`63403bb`** in `main` alle **19:27:50Z**, eseguito **dopo** i controlli pre-merge previsti dalla regola D — check `test` **pass** (1m19s) sull'ultimo commit `ffe0529`, PR **MERGEABLE · CLEAN**, `git status --porcelain` vuoto, `git log origin/main..HEAD` con **solo** i 4 commit della PR, HEAD locale = HEAD remoto, nessun file di dati toccato. **Deroga registrata** in `docs/13` **§9.13**, che chiude anche le quattro voci «da verificare» lasciate dal §9.12 (gate verde in CI, CSS esterno servito, 2.825 schede con ◇/◎, 0 tooltip «/90′»). **Da verificare al primo daily post-merge** (run **`35140709937`**, partito col push del merge assieme a `tests` `35140709946`): gate `verify_site` verde e deploy Pages; in *Stato fonti* le righe `espn scoreboard:*` come **AVVISO** e non più ERRORE rosso con 1 richiesta ciascuna — la **sospensione** scatta solo a `BACKOFF_FAILS = 5` run e la serie è partita dal run `35131980208`, quindi è attesa dal **2026-09-17** (righe SOSPESO con 0 richieste e richieste ESPN del run da 7 a 0). **Prossimo passo**: **P1.11** (griglia pre-registrata nel laboratorio: `Candidate.grid`, `n_tentativi`), poi P1.12 come *claim ridotto* in `info.html` e P1.15.

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0aad1`, ventiduesimo giro) — **il gate in CI ha morso, e aveva ragione due volte: due difetti reali nel backoff di P1.9, corretti** (diagnosi, misure e verifiche in `docs/23` §3). Il daily partito col push del merge di PR #42 (run **`35129006426`**, 17:34:12Z) è finito **rosso sul passo «Verifica il sito (verify_site)»** con **7 problemi, uno per lega**: «`stato.html: espn:ITA1 sospesa ma con 1 richieste nel run`». Il gate ha fermato il run prima del commit dei dati e del deploy — ma i log non sono leggibili dal sandbox: il workflow **`diag`** è stato esteso (su un runner la rete funziona) e ora porta in `diag-logs` il **log dei passi falliti** (`gh run view --log-failed`, che segue il 302 verso il blob storage; `gh api .../jobs/{id}/logs` restituiva 0 righe) leggendo il `run_id` da `diag/trigger.txt`: dal rosso alla diagnosi **~15 minuti**. **(a) La riga di una fonte sospesa contava le richieste di un'altra fase**: `report.requests` ha una chiave per **client** (`espn`), quindi la riga della classifica sospesa portava anche le richieste dello **scoreboard** (che non è in backoff) → la pagina mostrava «SOSPESO con 1 richiesta» e l'invariante [28] non poteva più distinguere un backoff attivo da un backoff inesistente. Ora ci sono **due righe e due contatori** (`espn` = classifica, `espn scoreboard` = eventi del giorno; la chiave della classifica resta `espn` perché è l'identità su cui cammina `backoff.state()`), più `error_prefix` in `CollectReport` perché l'errore di una riga si cerca col prefisso della **sua** fase. **(b) Una sonda che falliva riapriva la fonte**: `state()` leggeva la sonda come primo fallimento di una serie nuova (`fails = 1`), quindi servivano altri 4 tentativi prima di risospendere e il costo reale era ~5 richieste ogni 9 run invece di **1 ogni 5** — sul run incriminato `espn:ITA1` aveva **76 fallimenti consecutivi** e una richiesta. Ora la serie non si azzera con le pause: ciclo = `BACKOFF_PROBE_RUNS` pause + 1 tentativo = **5 run con una sola richiesta** (una al giorno), testato contando **4 richieste in 20 run di regime**. Verifiche: suite **343 passed** (339 → +4), prova end-to-end del caso CI con righe sintetiche nello store (`fda build` + `verify_site` **0 problemi · 89.455 controlli**; pagina: 7 righe `espn:*` SOSPESO con **0** richieste e 7 righe `espn scoreboard:*` OK con 1 richiesta e 42 righe), ruff invariato sul baseline (0 nuove). **Urgente**: finché la correzione non è in `main` ogni run del daily fallisce al gate e il sito resta all'ultimo build buono. **PR aperta** verso `main`; merge di competenza dell'utente (regola D).

**Giri archiviati (regola A5):** dal **tredicesimo** al **ventesimo** in [`STATO_archivio_2026-09-16.md`](STATO_archivio_2026-09-16.md); fino al 2026-09-15 sera (primo candidato) in [`STATO_archivio_2026-09-15.md`](STATO_archivio_2026-09-15.md).

---

## Fatto
- [x] **Audit pre-merge delle schede in programma (2026-09-13, quinto giro, `arena/01a09a79`, PR #27)** — misurate 95 schede pre-partita su 7 leghe: **parità di sezioni al 100%** (Analisi pre-partita, Scontro tattico, I giocatori che decidono, Contesto, Confronto di stagione, Fatti rilevanti), blocchi del modello 93/95, audit per campo **0 «mancante»** dopo 4 correzioni (previsione non più classificata «attesa dalla fonte»; distinta senza assenze = presente e non buco; la scheda **dice** «nessun indisponibile segnalato» invece di tacere, solo pre-partita; footer «Prossime partite e calendario»). Diagnosi con dati alla mano: le 2 schede senza modello sono neopromosse senza storico nel corpus surrogato del sandbox, e «Come nasce questa probabilità» manca su 70/95 solo perché le 138 previsioni committate sono `dc-elo-ens-0.1/-0.2` (25/25 con `-0.3` ce l'hanno) → si risolve da solo al primo `fda predict` post-merge. Copertura schede sulle partite in programma **4,6%** (Tappa 2 = decisione utente). Suite **172 passed**, `verify_site` **0 problemi · 11.793 controlli**. Tabella di parità e misure: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9.6.
- [x] **Tappa 1 — orizzonte esteso a tutto il calendario (2026-09-13, quarto giro, `arena/01a09a79`)** — `predictions` a una riga per partita (`latest_per_match` + migrazione 3.210 → 138 dichiarata a schermo), `fda predict --days-ahead 0` (tutto il calendario, zero richieste in più), pagina «Prossime» con «Tutto il calendario»: 1.987 righe compatte raggruppate per mese (1X2 / gol attesi / Over 2,5, preferito in grassetto e a colore, link solo se la scheda esiste, «senza previsione» quando manca). Copertura **6,6% → 96%** (138 → 2.000 partite su 2.083), **1,6 kB → 0,27 kB per partita coperta**, 29,7 ms per previsione, `prossime.html` 274 kB → 1.102 kB (**83 kB gzip**), `build_indexes` 2,4 s, suite **169 passed** (+6), `verify_site` **0 problemi · 11.793 controlli** col nuovo **[11]** (9.643 controlli sul calendario). Misure e criteri: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9. Tappa 2 rinviata (§9.5).
- [x] **Auto-audit accuratezza (2026-09-13, terzo giro, `arena/01a09a79`)** — calibrazione v1.1 a momenti su finestra 730 giorni (λ×0,9135: bias λ fuori campione +0,264→+0,053, Brier mercati 0,20646→0,20516; sul campione pieno bias +0,245→−0,024 e pareggio 23,9%→26,2% su osservato 25,6%), limiti di sicurezza sulle λ invertite (nessuna scheda da 8,4 gol attesi: 69 gare toccate, 1,19%, RPS invariato e mercati migliori proprio dove il modello esagerava), laboratorio reso equo (miscela non più appiattita sul DC, livello dei gol corretto per ogni candidato con inclinazione esponenziale), candidati 15→22 (`dc_elo_tilt`, pesi 0,50/0,85, iperparametri Elo), `GRID_SIZE` unica in `dc_grid.py`, 3 difetti nei verificatori e anteprima che riapplica i limiti. Suite **163 passed**, `verify_site.py` **0 problemi · 2.213 controlli numerici**. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Modelli + schede (2026-09-13, `arena/01a09a79`)** — calibrazione della griglia (λ×0,94, Δρ−0,04: bias gol +0,245→+0,058, pareggio 23,9%→25,7%, Brier mercati walk-forward 0,2065→0,2055), laboratorio walk-forward a 15 candidati con IC appaiati (`fda lab`, workflow settimanale), storico di allenamento salvato a ogni run (`history.parquet`), distribuzione dei gol totali + dotplot quantile e scomposizione della probabilità in scheda, controlli [9]/[10] in `verify_site.py`, suite a 150 test. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Processo/handoff — policy «merge lo esegue SEMPRE l'utente»** (decisione utente 2026-09-08, riapplicata in questa sessione perché il commit originale `9d39a21` non era mai stato pushato): sez. D di `00_regole_di_lavoro.md` riscritta (dopo la frase fissa l'agente si ferma e aspetta, non mergea mai), avviso ⚠️ in cima a `BRIEFING_NUOVA_SESSIONE.md`, decisione registrata in «Decisioni aperte». Doc-only; test 23 passed, nessuna nuova segnalazione ruff (baseline: vedi Nota). **PR #9 aperta verso `main`**: check `test` **Success** (49s), mergeable **CLEAN** — in attesa del merge dell'utente (secondo la nuova policy).
- [x] **Processo/handoff — paletti di qualità + briefing nuove sessioni** (fatto, **PR #6 mergiata in `main` `16db091`**): riscritta la sezione B (paletti di qualità: accuratezza/precisione/profondità/qualità), aggiunta la sezione D (policy esplicita su **quando fare Create PR e quando Merge**, con frasi fisse e checklist), e creato `docs/BRIEFING_NUOVA_SESSIONE.md` (porta d'ingresso con architettura, stato, prossimi passi, mappa file e limitazioni, così un agente nuovo non ripete verifiche già fatte). Doc-only, check tests pass (42s).
- [x] Studio di fattibilità e catalogo fonti (`01`, `02`) — commit `ac35f2b`
- [x] Decisioni utente registrate (7 campionati, automazione giornaliera, uso personale, quote sì, solo fonti GitHub Actions) — `03_decisioni_e_funzionamento.md`
- [x] Regole anti-blocco chat — `00_regole_di_lavoro.md`
- [x] Verifiche tecniche: penaltyblog OK (nota pandas 3), datahub CSV OK, FotMob id 7 leghe OK, matchDetails pre-partita ricco (formazione probabile, meteo, arbitro)
- [x] **Passo 1 — scaffolding**: `pyproject.toml`, `src/fda/` (config, http client con cache+rate limit+retry, cli), `config/leagues.yaml` (7 leghe + coppe), `config/sources.yaml` (budget richieste), `tests/` (6 test verdi), `.gitignore`, `data/README.md`. Comandi: `fda leagues`, `fda version`.

- [x] **Passo 2 — client FotMob** (`src/fda/sources/fotmob.py`): `fixtures`, `matchDetails`, `teams`, `playerData`, `matches?date`; parser → record piatti (MatchInfo con arbitro/stadio/meteo/xG/H2H, Shot, TeamMatchStat per periodo, PlayerMatchStat, LineupPlayer con indisponibili+rientro, eventi, momentum). Test su JSON campione (9 verdi). Comandi `fda fotmob-fixtures ITA1`, `fda fotmob-match <id>`.

- [x] **Passo 3 — Understat + ESPN**: `sources/understat.py` (endpoint JSON con cookie+XHR, senza soccerdata/seleniumbase; partite con xG, righe squadra-partita con xPTS/PPDA/deep, giocatori; `team_season_table`) e `sources/espn.py` (scoreboard con statistiche, classifiche, boxscore, arbitri). 11 test verdi. Comandi `fda understat-table ITA1`, `fda espn-today ITA1`.

- [x] **Passo 4 — storage + collect**: `store.py` (Parquet versionati in `data/processed/` + viste DuckDB, upsert per chiave, tipi UTC stabili) e `collect.py` (per lega: calendario → dettagli partite oggi ±3 gg, finite una sola volta, future a ogni run → Understat → ESPN; fonti isolate, esiti in `source_status`). 13 test verdi (incluso un run completo offline). Comandi `fda collect [ITA1 ...]`, `fda db [SQL]`.

- [x] **Passo 5 — modelli**: `sources/history.py` (CSV storici datahub/football-data → schema unico, quote di chiusura se presenti), `teams.py` (nomi canonici tra fonti, ~300 alias per le 7 leghe), `models/predict.py` (Dixon-Coles pesato nel tempo + Elo + ensemble 70/30 con griglia coerente; mercati 1X2, risultati esatti, O/U, BTTS, doppia chance, clean sheet; RPS). Backtest onesto Serie A 2025/26 (train 300 → test 80): RPS ensemble 0,212 vs Elo 0,218 vs naive 0,244. 18 test verdi. Comando `fda predict [ITA1 ...]`.

- [x] **Passo 6 — sito + automazione**: `site/analysis.py` (contesto analitico e frasi in italiano da regole esplicite), template Jinja2 (Oggi, Prossime, Risultati, pagina partita pre/post, Accuratezza con RPS/Brier, Stato fonti), `site/build.py`, comandi `fda build` e `fda daily` (collect → predict → build). Workflow `.github/workflows/daily.yml` (cron 5 volte al giorno + manuale, commit dei Parquet, deploy Pages) e `tests.yml`. 19 test verdi.
- [x] **Passo 6b — primo run dal vivo**: su `main`, run `daily` #2 (2026-09-06, come da handoff) concluso **Success** in 3m17s: collettori → modelli → build sito → commit dati → deploy GitHub Pages. Artefatto `github-pages` di 132 KB; Pages usa source = **GitHub Actions**. Restano solo gli avvisi non bloccanti sulla deprecazione Node.js 20.
- [x] **Correzione PR #2**: merge su `main` nel commit `968f2d7`; lo step di installazione di `daily.yml` usa `pip install -e ".[dev]"`, così `pytest` è disponibile nel workflow.
- [x] **PR #3 mergiata su `main`** nel commit `3359fdc` (workflow con versioni Node 24).
- [x] **Run `daily` #3 su `main` verificato** (id `33999756428`, head `3359fdc`): job `run` **Success** 3m08s + `deploy` **Success** 10s; commit dati `43b848c` pushato; artefatti `github-pages` (135358 B) e `run-log-3`; deployment Pages riuscito. `predictions` 48 → **96** righe (ITA1 20, ENG1 18, ESP1 22, GER1 18, FRA1 18), probabilità coerenti e senza null, tutte gare `scheduled`. FotMob ok 7/7; Understat per le 5 leghe coperte; ESPN standings ancora **403 isolato** (non blocca). Suite locale 20 passed. (Blob di log Actions e pagina Pages non raggiungibili dal sandbox — verifica su metadati/commit/dati.)

## In corso

> ⚠️ **Le voci qui sotto sono storiche** (fase 0-7b e PR #4/#9, 2026-09-08/09): restano come
> tracciabilità, ma sono tutte concluse. **La coda di lavoro vera è nel giro più recente in cima al
> file** (oggi: P1.11 → P1.12 → P1.15, poi i P2 di `docs/19` §4) e nella tabella di `docs/19` §4.

- [x] **Passo 7a — resilienza del run**: ESPN standings e scoreboard isolati; `fda predict` isola errori/storici per lega. Commit `d8063c1` + `0bcde50`; suite locale 20 passed; workflow `tests`: Success.
- **Passo 7b — storico NED1/POR1 (avviato)**: il mirror `datasets/football-datasets` non copre Eredivisie/Liga Portugal e `football-data.co.uk` è irraggiungibile dagli IP cloud (root cause: nei run #2/#3 NED1/POR1 avevano partite in programma ma 0 predizioni). Risolto puntando NED1 (`eredivisie`) e POR1 (`primeira-liga`) a un mirror `raw.githubusercontent.com` dedicato con la stessa struttura `season-XXXX.csv` (campo per-lega `datahub_base`), + alias squadra alle grafie N1/P1 (`For Sittard`→`Fortuna Sittard`, `Estrela`→`Estrela da Amadora`). Commit `85b515f`.
- **Verifica passo 7b**: suite locale **23 passed** (3 nuovi test); simulazione offline con CSV reali del mirror + calendario FotMob → NED1 **11** e POR1 **9** predizioni (tutte le partite in programma; le promosse ADO Den Haag/Cambuur/Académico Viseu/Marítimo ricevono prior dagli esiti di stagione in corso via FotMob). Workflow `tests` sul branch e check PR: **Success**.
- **PR #4 mergiata su `main`** nel commit `a3c5bf7` (merge eseguito dall'agente, precedente PR #2; tests verdi sul branch e mergeable CLEAN). `main` ora contiene lo storico NED1/POR1 da mirror dedicato.
- **Accuratezza live con gare reali (confermato)**: build del sito sui dati `main` (commit `783e59a`, 2364 fixture) → la pagina `accuratezza.html` ora mostra valutazioni reali pre-partita: **19 partite valutate** su 5 leghe, RPS **0,205** complessivo vs naive 0,244 (Bundesliga 2, LaLiga 6, Ligue 1 3, Premier 2, Serie A 6). NED1/POR1 ancora assenti perché i dati su `main` precedono il merge.
- **✅ Verifica live NED1/POR1 CONFERMATA (2026-09-08 13:33–13:40 UTC, verificato dal vivo)**: dopo l'attesa dei cron dilazionati (slot 10:00 mai partito), l'utente ha avviato manualmente il run `daily` dalla UI GitHub (Actions → daily → Run workflow su `main`): run **`34232722943`** (workflow_dispatch, 13:33:43Z, head `91f7109` post-merge) → job `run` **Success** in 1m33s (tutti gli step: test rapidi, collect→predict→build, commit dati, preparazione Pages) + job `deploy` **Success**. Commit dati **`6a31d39`** (13:35 UTC) verificato con `scripts/verify_ned_por.py`: **NED1 = 11 predizioni** (esattamente le ~11 simulate offline) e **POR1 = 11** (vs 9 simulate: da allora il calendario FotMob ha datato più gare di Liga Portugal, copertura maggiore), tutte con probabilità complete e `n_train=960` → **lo storico del mirror dedicato è stato scaricato e usato** (prima del merge: 0 predizioni per assenza di storico — le predizioni stesse sono la prova, più forte del log). Predizioni sensate (es. PSV 0,83 vs Sparta, Benfica 0,70 a Moreirense 09/09, Porto 0,73 a Casa Pia) e **alias mirror funzionanti** («Fortuna Sittard», «Estrela da Amadora»). `source_status`: FotMob OK 7/7, Understat OK (incl. NED1/POR1), unico errore il noto **ESPN standings 403** isolato (pre-esistente, non blocca). Limitazione: artifact/blob dei log Actions non raggiungibili dal sandbox (Azure) — verifica fatta su dati del commit, come per i run precedenti. Inclusa la gara NED1 di oggi NEC–Excelsior (16:45) → le prime valutazioni reali NED1/POR1 arriveranno in Accuratezza tra 1–6 giorni. Verificato sui dati live (`fixtures.parquet`, ultimo run): il calendario FotMob ha già gare NED1 **oggi** (12:00 UTC+, es. FC Utrecht–Go Ahead Eagles, NEC Nijmegen–Excelsior) e POR1 **da domani** (es. Moreirense–Benfica 19:45 UTC) → il primo `daily` post-merge avrà partite NED1/POR1 da predire: **la conferma dovrebbe arrivare entro poche ore/giorno, non serve altro de-risking offline** (simulazione già fatta: NED1 11 / POR1 9). ESPN standings: 403 isolato anche su NED1/POR1 nell'ultimo run (non blocca).
- **Dopo la conferma live (ora in corso)**: report pre/post in italiano già rifiniti su branch (PR #9: Accuratezza Δ/calibrazione, cartina tiri, momentum, precedenti, italianizzazione) — resta da seguire la risoluzione delle gare previste (incluse le 22 NED1/POR1 di questa settimana) che alimenterà Accuratezza con le prime valutazioni su Eredivisie/Liga Portugal.
- **Rifinitura Accuratezza + fix report partita (su branch, in PR #9 — verificato offline con build locale sui dati del repo)**: pagina `accuratezza.html` rinnovata: colonna **Δ vs naive** (su 19 gare il modello batte la base in ogni lega: Tutti **−0,038**; da −0,025 Serie A a −0,064 Premier), nuova card **Calibrazione** (prob. media prevista vs frequenza osservata: 1 = 42,4% vs 21,1% · X = 24,9% vs 36,8% · 2 = 32,6% vs 42,1% → nel campione attuale il modello sovrastima le vittorie in casa; n=19, dato indicativo) e per singola gara colonna **Top (✓/✗)** + **RPS**. Fix nei report partita (`match.html`): data in italiano con ora locale («domenica 06/09/2026, 15:30 (ora italiana)» — prima weekday inglese + etichetta «UTC» fuorviante) e spettatori formattati «57.000» (prima «57.000.0»); nuovi filtri Jinja `it_dt`/`it_num` in `build.py`. Suite **23 passed** (nuovi assert: Δ, Calibrazione, ✓, RPS 0,087 della previsione 62/21/17, data italiana, spettatori); **ruff: zero nuove segnalazioni** vs baseline (confronto diretto prima/dopo sui file toccati). Note: la pagina live su Pages si aggiornerà al prossimo `daily` su `main`.
- **Cartina dei tiri nei report post-partita (su branch, in PR #9 — verificato offline)**: nuova card «Cartina dei tiri» con due mezzi campi SVG (uno per squadra, porta a destra), pallini dimensionati sull'xG del tiro, colori per esito (verde gol / blu in porta / arancione tratteggiato bloccato / grigio fuori), tooltip con giocatore·minuto·xG e riepilogo «17 tiri · xG 3,06 · 5 gol». Dati: tabella `shots` (coordinate FotMob in metri 105×68, verificate: x max 104,3 / y mediana 34 = centro porta). `analysis.py`: `shot_map()` (trasformazione metri→px con clamp) + `goals` in `shot_summary`; template puro Jinja, nessun JS. Verifiche: **23 passed** (assert: 2 SVG, «xG 0,88» di Lautaro col decimale italiano, pannello vuoto Monza), build locale: **132/132 SVG ben formati** (parse XML), coordinate dei 5 gol di Mainz tutte dentro l'area e coerenti con l'xG (Mwene 0,89 a 6 m dalla porta); rendering PNG non possibile dal sandbox (niente cairo) — la verifica è strutturale/numerica, il primo `daily` su `main` darà la conferma visiva su Pages.
- **Italianizzazione completa del sito (su branch, in PR #9 — verificato offline con audit automatico su 146 pagine)**: richiamo utente «il sito è italiano e usiamo l'ora italiana». (1) **Ora italiana ovunque**: eliminati i 3 residui «UTC» (header di ogni pagina, timestamp della previsione, footer) → nuovo filtro `it_utc` (conversione UTC→Europe/Rome, gestisce anche ora solare/legale); orari delle liste già convertiti, ora dichiarati «(ora italiana)». (2) **Decimali con virgola**: nuovo filtro `dec` applicato a tutte le ~30 formattazioni dei template (xG, quote, gol attesi, Elo/DC, RPS/Brier/Δ in Accuratezza, λ nelle liste, rating) e `_f()` del narrativo («4,1 ammonizioni», «3,12 gol attesi»); fix «10.0 rigori in 24.0 gare» → interi. (3) **Meteo tradotto**: mappa `_WEATHER_IT` con gestione varianti combinate FotMob «Partly Cloudy/Wind» → «parzialmente nuvoloso e ventoso» (valori reali verificati sui dati: Mostly Clear, Fair, Showers in the Vicinity…). (4) **Date di rientro tradotte**: `_return_it()` («Mid October 2026» → «metà ottobre 2026»; catalogo completo dei valori FotMob su 437 indisponibili: Day to day, Doubtful, About 1-2 weeks, Out for season…). Verifiche: **29 passed** (6 nuovi unit test `test_i18n.py` su filtri e traduttori, inclusa conversione ora legale +2 / solare +1); **ruff 0 nuove segnalazioni**; audit automatico su tutte le 146 pagine generate: **0 residui** di UTC / weekday inglesi / meteo inglese / rientri inglesi / decimali con punto.
- **Momentum + ultimi precedenti nei report (su branch, in PR #9 — verificato offline)**: (1) nuova card **«Momentum della partita»** (solo post-partita): grafico SVG a barre minuto per minuto (verde = pressione casa, rosso = trasferta, scala FotMob ±100) con **marker dei gol** presi dalla cronaca e frase di sintesi quando una squadra domina («Momentum a favore di Mainz nel 66% dei minuti» su Hamburger 0-5 Mainz — coerente col risultato); dati: tabella `momentum` già raccolta (6134 righe, 67 partite), orientamento verificato empiricamente (valori fortemente negativi ai 5 gol di Mainz in trasferta). (2) **«Ultimi precedenti»** nel card Contesto (pre e post): fino a 5 incontri reali da tabella `h2h` (1554 righe dal 2009) con data, risultato, campionato e pill V/N/P **dal punto di vista della casa attuale** — bug di inversione V/P trovato e corretto in verifica (Roma–Atalanta: tutte P come atteso), poi inchiodato da test con riga non-pari (il campione aveva solo un 1-1); nomi risolti dalle due squadre in campo (le righe h2h coinvolgono solo loro), righe anomale scartate. Verifiche: **29 passed**; build locale: **198/198 SVG ben formati** (132 cartine + 66 momentum); ruff 0 nuove; audit italiano 0 residui. Nota: **diffidati non implementati** — fonte FotMob non li espone nei dati raccolti (ruoli lineup: starter/sub/unavailable/coach); da valutare fonte alternativa in futuro.

## Nota
- **Preview del sito dal sandbox** (scoperta 2026-09-08): dopo `fda build` (che scrive in `site/`, gitignored), si può servire con `python3 -m http.server 3000 --bind 0.0.0.0 --directory site` via start_process → l'utente vede il sito modificato nel browser (preview Arena) prima del merge. Il sito ufficiale è invece `uamisjd.github.io/football-deep-analyzer` (Pages), aggiornato solo dai run `daily` su `main`.
- Dal sandbox dell'agente la rete verso FotMob, `football-data.co.uk` e `raw.githubusercontent.com` può essere bloccata (si raggiungono solo `github.com` e `api.github.com`): le verifiche di rete vanno fatte in GitHub Actions o sul PC. I parser sono coperti dai test offline. (2026-09-08: PyPI risulta invece raggiungibile dal sandbox, quindi l'installazione dell'ambiente di test locale funziona.)
- Il mirror dedicato NED1/POR1 è configurato solo in `config/leagues.yaml` (campo per-lega `datahub_base`): facile da cambiare se il mirror diventasse irraggiungibile.
- Le quote The Odds API sono opzionali: vengono usate solo se `ODDS_API_KEY` è configurata.
- **Baseline ruff aggiornata (rilevata 2026-09-16, ventitreesimo giro, ruff 0.16.8 su `main` `6404a74`): 173 segnalazioni pre-esistenti** — prime voci: RUF046 39, UP017 26, BLE001 25, I001 21, FURB167 12, RUF100 12. Sostituisce la baseline del 2026-09-08 (47 con ruff 0.16.6: il numero è cresciuto soprattutto perché le regole di lint e il codice sono cambiati, non per un degrado improvviso) e il riferimento recente è **173** (docs/23: «175 → 173»). La CI `tests` esegue **solo pytest**, quindi ruff non blocca; la regola B.4 chiede ruff pulito sul codice **nuovo**, e su questo giro le 0 nuove sono verificate. Resta da valutare un turno di pulizia dedicato (100 delle 173 sono auto-fixabili con `--fix`).

## Prossimo passo (Fase 0/7 — un turno per riga)
1. ~~Scaffolding repo~~ ✅
2. ~~Client FotMob~~ ✅
3. ~~Client ESPN + Understat~~ ✅
4. ~~Storage + collect~~ ✅
5. ~~Modelli DC + Elo~~ ✅
6. ~~Sito statico + workflow + Pages~~ ✅
6b. ~~Primo run dal vivo in Actions e correzioni ai collettori~~ ✅
7. ~~Upgrade Node 24~~ ✅
7b. ~~storico NED1/POR1~~ ✅ **confermato dal vivo** (run `34232722943` del 2026-09-08 13:33Z, NED1 11 / POR1 11 predizioni, `n_train=960`, commit dati `6a31d39`). Prossimo: seguire la risoluzione delle gare previste (prime valutazioni NED1/POR1 in Accuratezza) e proseguire la roadmap.
7c. ~~Processo/handoff~~ ✅ (PR #6 in `main` `16db091`). Conferma live NED1/POR1 ✅ (2026-09-08). Prossimo: **Monte Carlo stagione** (fase 2: probabilità titolo/top-4/retrocessione per lega) o rifiniture report in base alle gare che si risolvono.
8. **Per la prossima sessione** (handoff 2026-09-08): (a) **🎯 PRIORITÀ UTENTE — parità di profondità delle schede partita**: «tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; schede di ogni partita ancora da giocare piene di contenuti, NED1/POR1 attualmente scarse». Diagnosi già misurata sui dati del run `6a31d39`: **classifica assente su TUTTE le leghe** (`espn_standings` vuota per il 403 cronico ESPN → fix = tabella di lega FotMob come fonte primaria) e **stagione xG con 1 sola gara su NED1/POR1** (Understat non le copre; fix = xG di stagione da FotMob); arbitro talvolta assente fino a pochi giorni dalla gara (si riempie da sola). Dettagli e lead tecnici in `BRIEFING_NUOVA_SESSIONE.md` sez. 3; (b) **Monte Carlo stagione** — fase 2 della roadmap `01` §8; (c) **Accuratezza**: seguire le gare NED1/POR1 in programma questa settimana (prime valutazioni Eredivisie/Liga Portugal); (d) opzionale: **pulizia ruff** (47 segnalazioni pre-esistenti, CI non bloccata).
9. **Questa sessione** (`arena/01a08295`): (a) **verifica sito/progetto** ✅ → `docs/05_verifica_sito_progetto.md`; (b) **P0** ✅ (formato arbitro/recupero, attribuzione fonti, ESPN→AVVISO); (c) **P1** ✅ (Open-Meteo, formazione pulita, playoff POR1). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo del meteo Open-Meteo** (run `daily` in Actions: attesa tabella `weather_forecast` popolata e schede «(previsione Open-Meteo)» sui futuri oltre le 48h) e di `future_days=7`; poi: (d) **B5 — collegare o rimuovere il client SofaScore** (codice inerte); (e) roadmap: **M2 diffidati**, **M3 schede giocatore**, **M5 Telegram/xG proprio**; (f) **pulizia ruff E501** (257 pre-esistenti, CI non bloccata).
10. **Questa sessione** (`arena/01a082c6`): **(a) card post-partita «Migliori in campo» per squadra** ✅ (top 3 per squadra con gol/assist/minuti/rating stagionale — avvio Fase 3; verificato su dati reali, suite 64 passed, 0 nuovi errori ruff). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo Open-Meteo + `future_days=7`** (primo run `daily` post-merge); poi: (b) proseguire **Fase 3** (schede giocatore vere: radar/percentili/xG per 90); (c) **B5 — SofaScore** (collegare o rimuovere); (d) **pulizia ruff E501/E741** (254 pre-esistenti).
11. **Sessione `arena/01a08613`**: **(a) fix header CalcioMetro ri-applicato** ✅ (PR #19 mergiata).
12. **Questa sessione** (`arena/01a08630`): **(a) card pre-partita «Fatti rilevanti»** ✅; **(b) B5 SofaScore rimosso** ✅ (codice morto). **Prossimo**: PR verso `main` → merge utente; poi Fase 3 schede giocatore, pulizia ruff.
13. **Questa sessione** (`arena/01a0864d`): **(a) Fase 3 schede giocatore** ✅ offline (hub + 7 tabelloni + 3.457 schede con radar/percentili/log; progetto `docs/07`); **(b) backfill finite esteso a tutte le leghe** ✅; **(c) fix i18n `unavailability_type`** ✅. Suite 73 passed, ruff 0 nuove. **Prossimo**: PR verso `main` → merge utente → **verifica dal vivo al primo `daily`** (catch-up `player_stats` 134→~231 partite; poi campioni percentili pieni per lega). Dopo: fase 3b opzionale (`playerData`: contratto, infortuni storici, trofei — rotazione ~80 richieste/run), pulizia ruff E501/E741, M2 diffidati (valutare fonte), fase 5 Telegram.
14. **Questa sessione** (`arena/01a09a79`, dopo il merge): **(a)** primo run `daily` in Actions → verificare che `fda predict --days-ahead 0` preveda **tutte** le gare in programma (~2.000, attese 1-2 minuti per il passo), che al primo run compaia la riga di collasso «previsioni: 3.210 → 138», **quante righe restano senza previsione** con lo storico reale (atteso: molte meno delle 150 offline), che `fda calibrate` produca `n_fit ≥ 1.200` e `bias_lambda < 0,10`, che `history.parquet` compaia in `data/processed` e che in Accuratezza il pareggio dichiarato cada dentro l'intervallo di Wilson con Over 2,5/BTTS entro 2 pp dall'osservato; **(b)** primo run del workflow `lab` (dispatch) → leggere `model_lab.parquet` su storico reale: **si cambia modello solo se** un candidato batte `dc_elo_prod` con IC 95% appaiato interamente negativo e il vantaggio vale in ≥5 leghe su 7; **(c)** P2 di `docs/13`: xG storici cumulati (candidato `dc_xg`), ξ per lega, tabella del laboratorio pubblicata in Accuratezza, dotplot dei punti nelle Proiezioni, lettura in frequenza dell'1X2 in scheda; **(d)** ora misurabile: **RPS per anticipo** sulla pagina Accuratezza (`made_at` vs `utc_kickoff` su tutto il calendario, §9.4 punto 3); **(e)** decisione utente sulla **Tappa 2** (schede per le partite lontane, §9.5).

15. **Questa sessione** (`arena/01a0a1a3`): (push poi riuscito: vedi ultimo punto) — il commit locale di questa sessione (docs/19 + STATO.md) **non era stato pushato al primo tentativo**: `gh auth status` → «The github.com token in GH_TOKEN is no longer valid». Regola `00` §8bis: il commit resta locale solo se il push fallisce; **la prima azione della prossima sessione è `git push origin arena/01a0a1a3-football-deep-analyzer`** (dopo aver ricollegato GitHub in Arena). Nessun file è andato perso: `docs/19_audit_totale_tre_aree_2026-09-14.md` e questo STATO.md sono nel working tree. **audit totale in tre aree** ✅ → `docs/19_audit_totale_tre_aree_2026-09-14.md` (33 riscontri: 9 P0, 14 P1, 10 P2; ogni punto con Osservato → Misura → Impatto → patch pronta → verifica). Nessuna modifica a codice/dati in questa sessione: solo il documento.
    - **Riscontro principale (nuova misura)**: scaricate le quote di chiusura Pinnacle di **tutte e 7 le leghe × 3 stagioni** dal mirror già usato per NED1/POR1 (`huhao930422-debug/football-odds-mirror`, 21 CSV, verificate via API GitHub). Aggancio al backtest con `fda.teams.canonical()`: **5.543/5.815 (95,3%)**, valutabili **4.372**. **RPS modello 0,19803 vs mercato 0,18843 → Δ +0,00960, IC95% [+0,00794; +0,01126], peggio in 7 leghe su 7.** Diagnosi: sottostima dei favoriti (decile 10: 0,728 previsto vs 0,799 osservato). Oggi `history.parquet` ha quote solo per NED1/POR1 (1.519/7.387) perché `football-data.co.uk` è usato solo come fallback su errore, mai come arricchimento.
    - **Correzione testata e RESPINTA** (da non ripetere): sharpening γ sul rapporto delle λ. Con griglia stretta [0,94;1,12] passerebbe il protocollo (ΔRPS −0,000457, IC interamente negativo, 6/7 leghe); con griglia onesta [1,00;1,40] **non passa** (ΔRPS −0,000417, IC [−0,001037; **+0,000203**], 4/7 leghe) e rompe la calibrazione del pareggio (25,20% vs 25,6% osservato). Da qui il riscontro metodologico §1.4: **la griglia degli iperparametri va pre-registrata nel codice** (`Candidate.grid`), altrimenti "IC<0" si ottiene accorciando la griglia.
    - **Difetti visivi misurati senza browser** (nessun headless installabile in sandbox): barre 1X2 che sommano 99/101 in **37/167** schede hero (la funzione `pct_triple` **esiste ed è testata**, è collegata in un solo punto: `build.py:276`); **5 pagine** con barre dei gol a `height:183%`; CSS inline duplicato **144,4 MB su 248,3 MB (58%)** del sito. Il tema chiaro — prima stimato come «7 token mancanti» — si è rivelato più grave e in parte diverso: **corretto in `docs/19` §3.2** (il fallback `@media` senza 18 token colpisce **solo chi non esegue JS**, perché lo script inline imposta già `data-theme`; i difetti gravi stavano in colori hard-coded che nessuna palette copriva: la «V» della guida-forma a **1,11:1**, il testo selezionato a **1,02:1**, il punteggio più probabile a **2,09:1**, l'hover a **1,78:1**, e il segmento ospite della barra 1X2 a **2,92:1 in entrambi i temi**).
    - **P0.1 + P0.2 ✅ ESEGUITI in questa sessione**: 22 token di foreground nuovi in `base.html`, ogni `color:` hard-coded sostituito, palette chiara ≥4,5:1 su **4** superfici, fallback OS completo e identico, gradienti della barra 1X2 corretti, override della cella modale eliminato, `--lose`/`--draw` del tema scuro portati a AA (4,59/4,57), leggende dei grafici tokenizzate in `match.html`. Aggiunti **14 test senza browser** in `tests/test_tema_contrasto.py` che **derivano dal CSS** l'uso di ogni token (testo → 4,5:1, grafica → 3:1), con le eccezioni dichiarate e motivate nel file. Verifica: **190 passed** (176 + 14), `ruff` pulito sul file nuovo, `fda build` 2m22s → 4.123 pagine, `verify_site.py` **nessun problema · 11.565 controlli**. **Non verificato**: la resa estetica (serve un browser; Lighthouse CI è il §3.11).
    - **Difetti di contenuto/dati**: colonna "Richieste" di *Stato fonti* = contatore **cumulativo** (NED1/POR1 accreditati di chiamate Understat mai fatte); pagina *Accuratezza* con **84 gare di cui solo 3 del modello corrente** (per questo la tabella live e il backtest sembrano contraddirsi); baseline naive hard-coded 45/27/28 che gonfia il Δ fino a **+0,00205**; **34% delle curiosità FotMob scartate in silenzio** (20% delle schede ne mostra 2); etichette arbitro con soglie assolute (POR1 media 5,04 gialli/gara vs soglia "molto severo" 5,0); `verify_site.py` con **11.590 controlli ma zero invarianti di pubblicazione**.
    - **Verificato e chiuso** (non intervenire): xGOT 99,6-99,9% su 7 leghe; parità post-partita **100%** su 6 tabelle; 12.089 `<svg>` tutti accessibili; 0 `nan`/`None`/residui inglesi reali; dotplot, matrice punteggi, in-play, Wilson, backtest già coperti da `verify_site.py`; `PlayerCatalog` 0,3 s (la doppia costruzione **non** è un collo di bottiglia).
    - **P0.3 ✅ ESEGUITO**: `fmt.pct_triple(p, nd=0)` generalizzato (unità intere di `10**-nd` → somma esatta; tie-break reso coerente con la docstring: la parità ora premia (1, X, 2), prima premiava l'ultimo), filtro Jinja `pct3`, **tre** barre collegate (non due: anche la mini-barra di `_matchlist.html:51`, che calcolava pure il favorito sui grezzi), `aria-label` aggiunto alla barra hero, **wrapper duplicato di `build.py` eliminato** (15 righe di copia di riserva), `fav_i` ora = indice del massimo *stampato*. Nuovo controllo di pubblicazione **`verify_site.py [11a]`** (larghezze sommano 100 = etichette = aria-label; un solo favorito ed è il massimo; il wordmark dell'header resta fuori da solo). **Trovato e corretto anche un difetto del verificatore**: il controllo `[10]` ricalcolava i passi con arrotondamento indipendente tollerando 0,06 pp, cioè *difendeva* il difetto — dopo la correzione segnalava 14 pagine giuste; ora confronta con `pct_triple` e lo scarto ammesso è zero. Misura sulla popolazione (2.152 previsioni): **565 (26,3%)** non sommavano 100 con interi indipendenti, **521 (24,2%)** con un decimale → **0** con `pct_triple` (la stima dell'audit, 22,2%, era prudenziale). Verifica: **193 passed** (3 test nuovi, inclusa una scheda generata end-to-end con il vettore `0,4049/0,4049/0,1902` che prima stampava 99%), `fda build` 2m13s, `verify_site.py` **nessun problema · 14.108 controlli** (erano 11.565) con **565 barre** verificate, ruff invariato sul baseline pre-esistente.
    - **Preview del sito attiva** (richiesta utente): server statico sulla porta **8080** che serve `site/` — utile per la resa dei due temi, che in sandbox non è verificabile senza browser.
    - **P0.4 ✅ ESEGUITO**: causa confermata in `advanced.py:goals_view` — la scala era `pmax = max(p.max())` sui soli totali 0…cap mentre la barra della coda usava `tail / pmax`, quindi nelle gare ad alto λ la barra «7+» usciva dal contenitore (**9 barre su 2.152 previsioni, max 183%**; le 5 pagine pubblicate avevano λ totale 4,68-5,50). Correzione: `scala = max(p.max(), tail)`, **senza clamp** (limitare a 100 avrebbe mentito sul grafico). **Secondo difetto trovato correggendo il primo**: la didascalia dichiarava «nel **90%** dei casi il totale resta fra `q10` e `q90`» — copertura garantita >80%, misurata media 87,4% — e con il percentile nella coda stampava «7» invece di «7+» (intervallo aperto letto come chiuso). Provata e **scartata** l'alternativa 5°-95° (frase vera ma intervallo quasi sempre «fra 0 e 6» = tutto l'asse, quindi inutile): ora `goals_view` pubblica `copertura` (massa vera fra q10 e q90) e `q90_label`. Tre invarianti nuove in `verify_site.py [9]` (altezza ≤100% **e** = proporzione ricalcolata; barra più alta a piena scala; copertura 80-100% e uguale alla ricalcolata). Corretta anche una fragilità del verificatore: 3 regex della didascalia dipendevano dagli **a capo del template** e hanno segnalato 160 pagine corrette dopo una riformattazione → ora `\s+`. Verifica: 194 passed, `fda build` 2m23s, `verify_site.py` **nessun problema · 15.548 controlli** (erano 14.108), `height:[0-9]*%` oltre 100 → **0**.
    - **Residuo di P0.3 chiuso**: anche il tooltip dell'«Esito più probabile» in testa alla scheda arrotondava tre volte in modo indipendente (`match.html:21`) → ora usa lo stesso vettore `pct3` della barra.
    - **Audit delle 10 sezioni della scheda partita ✅ consegnato** → `docs/20_audit_dieci_sezioni_scheda_partita_2026-09-15.md` (14 riscontri: 1 P0, 6 P1, 7 P2). Metodo: censimento automatico su tutte le **76 schede pre-partita** (371 totali, 295 post) + lettura integrale di due schede vere (Roma-Inter ITA1 ed Estrela-Académico Viseu POR1) + verifica incrociata sui Parquet. I cinque riscontri principali, tutti misurati: **(1)** «Come nasce questa probabilità» pubblica come passo 2 la colonna `blend_p_*` chiamandola «Media con i rating Elo — peso DC 70%, Elo 30%», ma da quando la produzione è `ENSEMBLE_MODE="tilt"` quella colonna è il vettore della **griglia sulle λ inclinate**: scarto medio dalla media vera **1,18 pp**, massimo **7,87 pp**, **8 gare su 2.080** con esito più probabile diverso, e il passo del tilt non compare affatto; **(2)** tutte le **371** schede dichiarano «`verify_site` controlla **11.582** numeri» scritto a mano nel template — il valore vero è **15.548** (−25,5%) ed era stantio già prima; **(3)** il tooltip di ogni riga xG di «Scontro tattico» dichiara «media stagionale **FotMob**» mentre il valore viene da **Understat** in 5 leghe su 7, con bias sistematico **+9,5%** (massimo 0,641 xG/gara: Roma 3,45 contro 2,81 sulle stesse 4 gare); **(4)** nella stessa tabella le componenti FotMob (2,55 + 0,25 = 2,80, scomposizione che **chiude** dentro FotMob) non sommano al totale della riga sopra (3,45, Understat) → sembra un errore di conto; **(5)** «Come arrivano» accosta punti-sopra-attesi e tendenza degli xG senza soggetto: **10 righe su 29 (34%)** si leggono in contraddizione. Parità fra leghe misurata: POR1 **5,4** righe narrative contro NED1 **7,7**, e **0** occorrenze di «giocatore di peso» nelle assenze POR1 contro 7 in ITA1 (è la regola che parla solo nei casi estremi, non mancanza di dati: la forma è stampata più sotto). **Ipotesi smentita dai dati** (registrata nel doc): che il Δ attribuito alla «Calibrazione» fosse in realtà prodotto dal tilt — no, la griglia sulle λ pubblicate riproduce l'1X2 con scarto max 0,038 pp e il Δ della calibrazione è davvero suo.
    - **Prossimo passo — direttiva utente 2026-09-14 (prioritaria)**: applicare le correzioni di `docs/20` nell'ordine del §15 — prima **#2** (una riga di template, affermazione falsa su 371 pagine) e **#1** (catena a quattro passi, ingressi già tutti salvati), con l'invariante nuova in `verify_site.py`: *ogni Δ pp pubblicato deve equalare la differenza fra due passi adiacenti stampati*. Poi #3+#4 (stessa tabella), #5+#6+#7 (stessa riga di testa), #10+#13 (narrativa e parità), infine le finiture. Vecchio riferimento: audit profondo delle **10 sezioni della scheda partita** (Analisi pre-partita, Previsione del modello, Risultati esatti più probabili, Come nasce questa probabilità, Scontro tattico, Fatti rilevanti, Come arrivano, Confronto di stagione, Contesto, Verifica approfondita) perché siano accurate, intuitive, precise, profonde, di qualità e logica. Poi i P0 residui: **P0.5** (CSS esterno, −144 MB), **P0.6** (composizione del campione in *Accuratezza*), **P0.8** (invarianti di pubblicazione). La baseline naive hard-coded `45/27/28` di `build.py:500` (§1.6, P1) resta da sostituire con la frequenza osservata del campione.
    - ✅ **Push riuscito in questo turno** (il token GitHub è tornato valido): `b034bbb` (docs/19) e `c645f96` (tema chiaro AA) sono su `arena/01a0a1a3-football-deep-analyzer`. Aperta **PR #32** verso `main` → check `test` **pass in 50s** (verifica indipendente in Actions dei 14 test nuovi). Merge di competenza dell'utente.

## Decisioni aperte
- **Backfill partite finite esteso a tutte le 7 leghe** (deciso 2026-09-09, `arena/01a0864d`, fase 3): prima il backfill completo di stagione riguardava solo NED1/POR1 (per l'xG FotMob); ora vale per tutte (statistiche giocatore complete dall'1ª giornata + report storicizzati). Costo: 97 richieste una tantum (stagione in corso al 9/9), poi ~10/lega a giornata; tetto `--max-backfill=40`/lega/run invariato. In attesa di conferma dal vivo al primo `daily` post-merge.
- **B5 SofaScore — RIMOSSO 2026-09-09** (decisione del 2026-09-09, eseguita in `arena/01a08630` come commit dedicato dopo gli insights). Cancellati `sources/sofascore.py`, `tests/test_sofascore.py` e la voce in `config/sources.yaml`. Il client non era mai collegato a `collect.py`. Reversibile da git (`805bcb5`). Il catalogo in `02` resta come nota (403 da IP datacenter).
- **Qualità delle schede partita** (direttiva utente 2026-09-08, **prioritaria**): tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; le schede di ogni partita da giocare devono essere piene di contenuti e servizi, con **parità completa tra le 7 leghe** (NED1/POR1 segnalate scarse). Primo intervento: classifica da FotMob (ESPN standings 403 cronico) + xG di stagione per NED1/POR1 (Understat non le copre). Registrata anche in `BRIEFING_NUOVA_SESSIONE.md` sez. 3 e 5.
- **Merge delle PR** (deciso 2026-09-08): **il merge lo esegue SEMPRE l'utente, MAI l'agente**. L'agente apre la PR quando serve (sez. D di `00_regole_di_lavoro.md`), monitora i check e avvisa con la frase fissa «👉 Tutto verde: è il momento di fare Merge (PR #N).», poi aspetta senza fare merge. (La decisione era stata registrata nel commit locale `9d39a21` della sessione precedente, mai pushato e andato perso alla chiusura → riapplicata sui docs in questa sessione.)
- **Fonte storica NED1/POR1** (deciso 2026-09-06): mirror GitHub `raw.githubusercontent.com` di football-data.co.uk dedicato (via `datahub_base` per-lega), scelto perché il mirror datahub copre solo 5 leghe e `football-data.co.uk` diretto è irraggiungibile dagli IP cloud.
- Nessuna altra bloccante. (Telegram bot e protezione accesso: fasi successive.)

## Direttive utente persistenti — aggiunta 2026-09-08
- Quote bookmaker: **non interessano all’utente**, non sono requisito editoriale e non vanno prioritarizzate.
- Requisito principale: schede partita complete, accurate, profonde e con parità tra le 7 leghe; nessun dato realmente mancante quando la fonte dovrebbe averlo pubblicato.
- Distinguere sempre `presente`, `atteso dalla fonte` e `mancante`; non inventare dati per riempire campi.
- Fare ricerche approfondite su GitHub e altre fonti prima di scegliere integrazioni; annotare i risultati in `BRIEFING_NUOVA_SESSIONE.md` per gli agenti futuri.
- Implementato audit automatico delle schede future in `src/fda/site/audit.py`, visualizzato in `stato.html`; test suite: 49 passed.
- **Riferimento di mercato come metro esterno** (misurato 2026-09-14, `arena/01a0a1a3`, `docs/19` §1.1): le quote di chiusura Pinnacle di tutte e 7 le leghe sono disponibili sullo **stesso mirror già in uso** per NED1/POR1. Decisione da prendere: **(A, raccomandata)** le quote restano fuori dalla pipeline e diventano solo un benchmark riproducibile in CI (`scripts/benchmark_quote.py` + job mensile) — rischio zero sul modello; **(B)** `datahub_base` esteso alle 5 grandi leghe (5 righe di config) per avere le quote dentro `history.parquet` e nel laboratorio — rompe `tests/test_models.py:79-90` che codifica la decisione opposta e lega il progetto a un repo personale. **Non è in contraddizione con la direttiva utente sulle quote** (non sono un requisito editoriale): qui non si pubblicano quote, si misura la distanza del modello dal livello di mercato.
- **Candidato γ-sharpening RESPINTO** (2026-09-14, `docs/19` §1.3): da non ritestare con griglia stretta. Se si vuole attaccare la sottostima dei favoriti (§1.2), la strada è la **ricalibrazione del vettore 1X2** (temperatura) come candidato del laboratorio con griglia pre-registrata (§1.4, §1.2).
