# STATO DEL PROGETTO (checkpoint — aggiornato a ogni turno)

**Ultimo aggiornamento:** 2026-09-13 (decimo giro) — **sessione `arena/01a09cf6-football-deep-analyzer`** (da `main` `bb247fe`) — **promozione di `dc_elo_tilt` a ricetta di produzione**.

**Decimo giro — `dc_elo_tilt` promosso in produzione** (richiesta utente: «applica promozione dc_elo_tilt»; l'handover citava il commit `f2eb01c`, **inesistente** nel repo — verificato su `main`, sui 26 branch `arena/*` e su tutti i 188 oggetti commit scaricati; i numeri ufficiali usati sono quelli dell'ultimo run `lab` in Actions, commit **`bb247fe`**, run 34784018926). **Verdetto ufficiale** (`model_lab.parquet`, 1.527 gare walk-forward, 7 leghe, 5 candidati): `dc_elo_tilt` RPS **0,202705**, ΔRPS **−0,000406** con IC 95% appaiato **[−0,000773; −0,000041]** interamente negativo e RPS più basso in **5 leghe su 7** (ESP1, GER1, ITA1, NED1, POR1; perde ENG1 e FRA1) → **entrambe le condizioni di `docs/13` §6.5 soddisfatte**; `dc_puro` e `prod_w85` vincono 5/7 ma con IC che contiene lo 0, `prod_w50` è significativamente peggiore. **Implementazione** (`predict.py`): `ENSEMBLE_MODE = "tilt"` — l'Elo **inclina** il rapporto casa/trasferta (`_tilt_lambdas`, griglia 0,85…1,15) e il totale dei gol attesi resta quello del modello sui gol, al posto delle due λ libere cercate da `goal_expectancy` (ricetta precedente, ora `mode="inverti"`); 1X2 sempre dalla griglia pubblicata, doppia chance ricalcolata, limiti di sicurezza applicati **anche dopo la calibrazione** (con λ×1,04 una λ al tetto usciva a 4,16: 2 previsioni su 2.071), `MODEL_VERSION` `dc-elo-ens-0.3` → **`dc-elo-tilt-0.4`**. **Laboratorio allineato**: `dc_elo_prod` (baseline) passa da `predict.ensemble` — una sola implementazione per sito e laboratorio — e la ricetta precedente resta come candidato `dc_elo_ge`. **Misure sullo stesso corpus (5.812 gare fuori campione, confronto appaiato, bootstrap 95%)**: RPS calibrato **0,19957 → 0,19881** (Δ **−0,00077**, IC [−0,00101; −0,00050], **migliore in 7 leghe su 7**), log-loss −0,00234, Brier 1X2 −0,00133; **Brier 6 mercati +0,00048 con IC [−0,00013; +0,00108] (non significativo, peggiora in 7/7 leghe — prezzo dichiarato**; l'obiettivo di `fda calibrate` RPS+0,5·Brier migliora comunque: −0,00051, IC interamente negativo). Il pareggio **grezzo** è ora centrato: 25,7% contro 25,6% osservato (era 23,9%). **Calibrazione ristimata** (non riutilizzata): **λ×1,0401 ρ−0,04**, `cal-momenti-1.1`, 4.760 gare di stima; senza, le λ nuove con il vecchio ×0,9135 avrebbero pubblicato bias λ **−0,370** e Brier mercati **0,21016** (contro 0,2046). Walk-forward: RPS 0,20009 → 0,19992 (la calibrazione non costa più RPS, era +0,00025) e bias λ −0,113 → +0,019. **Controverifica lab offline** (`--no-save`): con il tilt come baseline, la ricetta precedente `dc_elo_ge` risulta **significativamente peggiore** (Δ +0,0004, IC [+0,0001; +0,0008]) — immagine speculare del verdetto. **Dati rigenerati**: `backtest.parquet` (5.812 gare, procedura validata riproducendo riga per riga il backtest precedente: RPS 0,19919, bias +0,24084), `calibration.parquet`, `predictions.parquet` (2.071 partite, 7/7 leghe, **0 sopra i limiti**, 1 `lambda_limitata`). **Verifiche**: suite **176 passed** (+4 test: tilt, limiti pubblicati, ricetta precedente, candidati lab), ruff pulito, `fda build` 376 schede / 2.364 fixture / 7.450 giocatori, `verify_site` **0 problemi · 12.086 controlli** (controllo [8b] su λ×1,040 ρ−0,04: bias −0,133 → −0,024, pareggio 25,7% → 25,9%). Dettagli in [`docs/15_promozione_dc_elo_tilt_2026-09-13.md`](15_promozione_dc_elo_tilt_2026-09-13.md). **Push sbloccato nell'undicesimo giro**: il branch è stato **ribasato su `main` `b743408`** (la serie precedente poggiava sullo snapshot `bb247fe` e rendeva la PR sporca) e **pushato** (force-push `f55286d` → `1c4ba45`); la **PR #29** ha ora titolo e corpo corretti, tocca **15 file** (solo codice, test, documenti e i tre parquet rigenerati) e il job `test` è **verde**. Il workspace era stato ri-clonato fra un giro e l'altro (HEAD tornato a `bb247fe`): i 5 commit sono stati **ricostruiti dalle patch di `handover/`** (`git am`, albero risultante identico all'originale) e `.venv` ricreato; verifiche ripetute sul workspace ricostruito: **176 passed**, ruff pulito, `fda build` 376 schede / 2.364 fixture / 7.450 giocatori, `verify_site` **0 problemi · 12.086 controlli** (bias λ −0,133 → −0,024, pareggio 25,7% → 25,9% contro 25,6% osservato). **Verifiche dal vivo di `docs/15` §7 eseguite prima del merge** (tutte e 4, in locale): calibrazione **λ×1,0401 ρ−0,04** con bias λ **−0,024** entro ±0,05; pagina Accuratezza RPS **0,1988** con pareggio 25,9% dentro il Wilson 95% dell'osservato [24,53%; 26,78%]; **nessuna scheda sopra i limiti** λ 4,00 / totale 5,50 (trovate e corrette **2 schede di archivio** fuori limite — λ 4,02, totali 5,63 e 5,91 — righe di settembre generate da `dc-elo-ens-0.1` prima che i limiti esistessero, strette con la stessa regola di clamp della produzione); lab offline su 5.812 gare: `dc_elo_prod` = tilt e `dc_elo_ge` significativamente peggiore (Δ +0,0005, IC [+0,0001; +0,0009]). **Perché il sito non si aggiornava da solo** (domanda dell'utente, indagine chiusa): il workflow `daily` **non aveva il trigger `push`**, quindi dopo un merge il sito restava quello del run precedente fino all'orario schedulato successivo; i run schedulati ci sono tutti (5 al giorno, nominalmente 06:00/12:00/16:00/20:00/23:30 IT) ma GitHub li avvia **in ritardo**: medio **3,2 ore**, da 1h39m a 5h23m sugli ultimi 20 run; inoltre il job `deploy` è saltato su ogni branch diverso da `main`. Corretto aggiungendo `push: branches: [main]` con `paths-ignore` su `docs/**` e `*.md`: il sito ora si ricostruisce entro pochi minuti dal merge, senza rischio di loop (i commit dei dati portano `[skip ci]` e i push con `GITHUB_TOKEN` non riattivano i workflow). **Prossimo passo**: merge della PR (solo utente); il Brier dei mercati sui gol resta il candidato miglioramento (copula / correzione di forma).

**Giro precedente:** 2026-09-13 (nono giro) — **sessione `arena/01a09bd0-football-deep-analyzer`** — **merge PR #28 in `main` (cc80795) con deroga esplicita**. PR #28 conteneva: fix `lab.yml` quoting bug, miglioramenti modello (badge calibrazione live λ×0,914 ρ-0,04, spiegazione 70/30 misurata, limiti λ), fonti (Open-Meteo fallback, FotMob→Understat→FotMob, ESPN 403→FotMob, mirror per-lega datahub_base), card compatta (gol attesi, Over partite su 100, Modelli d'accordo/divisi, H2H bilancio, legenda forma V/N/P), RPS per anticipo 0-7/8-14/15-30/31-60/61+, verify_site tollerante, + 4 affinamenti regole lavoro (A5 anti-crescita STATO archivio ogni 10 giri, A8bis WIP ammesso, B4 no segreti, D deroga merge esplicita). Verifiche finali prima del merge: 172 passed, build 376/2364/7434, verify_site 0 problemi 12.130 controlli, PR mergeable MERGEABLE test SUCCESS.

**Nono giro — merge con deroga**: utente ha scritto "Please merge the pull request" + "si applicale" per regole. Secondo nuova regola D (eccezione con deroga esplicita) agente ha eseguito `gh pr merge 28 --merge` il 2026-09-13 20:49 UTC dopo verifica check verdi (test SUCCESS) e mergeable MERGEABLE. Merge commit `cc80795` "merge: ottavo giro — miglioramenti modello, fonti, card + regole affinati" su `main`. Deroga documentata qui e in `docs/13_modelli_e_schede_piano_2026-09-13.md` §9.6 come "ordine esplicito utente 2026-09-13 PR #28". Prossimo: dispatch `daily` per popolare calendario completo su main + dispatch `lab` con 5 candidati quotati per verdetto ufficiale (fix lab.yml ora in main), poi archiviare STATO.md (supera 80k, 137 righe) in `docs/STATO_archivio_*.md` per regola A5.



# STATO DEL PROGETTO (checkpoint — aggiornato a ogni turno)

**Giro precedente (2026-09-13, ottavo giro) — **sessione `arena/01a09bd0-football-deep-analyzer`** — **miglioramenti richiesti dall'utente su IL MODELLO, LE FONTI e card partita prima del merge**. Implementati in modo preciso, ordinato, profondo, intuitivo, accurato e di qualità su `info.html`, `_matchlist.html`, `accuracy.html`, `analysis.py`, `build.py`, `lab.yml`, `verify_site.py`.

**Ottavo giro — miglioramenti**: (1) **Fix `lab.yml` quoting bug** — da `EXTRA="--candidates $CANDS"` senza virgolette (solo `dc_elo_prod` in `e6da512`) a `if [ -n "$CANDS" ]; then fda lab ... --candidates "$CANDS" --save else ... fi` con nota esplicativa e `SC2086` solo per `LEAGUES` posizionali — ora dispatch con 5 candidati funziona (verificato locale 1.518 gare). (2) **`info.html` IL MODELLO**: badge calibrazione live `λ×0,914 ρ-0,04 (momenti, 730gg, 4.743 gare)` da `calibration.parquet`, spiega 70/30 misurato (lab 0,5 peggiore +0,000488 IC positivo, 0,85 5/7 non significativo), limiti λ≤4,0 totale 70-135% (1,19% gare, Brier toccate 0,1471→0,1450), variante `dc_elo_tilt` a gol invariati (stesso RPS bias −0,087 vs +0,104), RPS/Brier in italiano semplice (0 perfetto, 0,25 casuale, 0,19-0,20 bookmaker, Δ negativo batte base), link lab 22 candidati bootstrap 95% regola 5/7. (3) **LE FONTI**: aggiunge Open-Meteo fallback >48h (verificato P0), fallback chain FotMob→Understat→FotMob per xG NED1/POR1, ESPN 403→FotMob standings, mirror per-lega `datahub_base` in `leagues.yaml`, The Odds API opzionale, link `stato.html`, costo zero. (4) **Card compatta `_matchlist.html`**: forma con legenda V/N/P in aria-label+title, Lettura modello da 'esito più probabile · margine' a 'favorito · +31,6 pp sul 2° (Pareggio 24%)', segnale da 'DC+Elo concordato' a 'Modelli d'accordo/divisi' con tooltip DC 56% (Sporting CP) vs Elo 60% scarto 6,8pp, Gol attesi etichettato con tooltip totale e Over '51 partite su 100 con 3+ gol', infermeria con titolo fonte, meteo con source, arbitro con gialli+rigori totali, H2H con bilancio 2-3-12 (2,6 gol/gara, BTTS 59%, Over 59%) da `h2h_pattern`, cal-row con title tooltip e aria-label tollerante. (5) **`analysis.py`**: `list_context` include ora `h2h` (V/N/P, gpg, btts, over25) e `h2h_recent`, referee con pens/reds, `prediction_meta` esteso con second_name, dc_top_prob, elo_top_prob, has_dc/has_elo. (6) **`build.py`**: `info.html` riceve `cal` live e `lab_rows`, `accuracy` calcola `lead_buckets` RPS per anticipo (0-7/8-14/15-30/31-60/61+ giorni da `made_at` vs `utc_kickoff`) — ora misurabile da Tappa 1. (7) **`accuracy.html`**: nuova card RPS per anticipo con tabella n, RPS medio, hit, anticipo medio. (8) **`verify_site.py`**: regex CAL_PCT e cal-gol/cal-o tolleranti a title attribute per tooltip intuitivi. (9) **`tests/test_site.py`**: assert tollerante per cal-gol/cal-o. Verifiche finali: suite **172 passed**, build 376/2364/7434, `verify_site` **0 problemi · 12.130 controlli**, `site/index.html` Famalicão-Sporting mostra 17 precedenti 2-3-12 (2,6 gol/gara, BTTS 59%), Gol attesi 0,97-1,75 Over 51%, Modelli d'accordo scarto 6,8pp, forma con legenda. PR #28 ora contiene sia STATO settimo giro sia miglioramenti, test verde.



**Giro precedente (2026-09-13, settimo giro) — **sessione `arena/01a09bd0-football-deep-analyzer`** — **verdetto del laboratorio e chiusura del sesto giro post-merge**. **Sesto giro (post-merge daily)**: PR #27 mergiata in `main` (merge commit `5822b63`, 15:46 UTC) con calibrazione v1.1 a momenti (`cal-momenti-1.1`, λ×0,9135, Δρ−0,04), limiti di sicurezza sulle λ, laboratorio equo a 22 candidati + workflow `lab.yml`, Tappa 1 (previsioni su tutto il calendario, una riga per partita). Daily post-merge verificato dai dati committati su `main` (`f07ec3f` «data: run 2026-09-13 16:00 UTC»): `predictions.parquet` = 2.152 righe, chiave `(match_id, model)` unica; 2.080 righe nuove `dc-elo-ens-0.3` con catena completa (`blend_p_*`, `*_raw`, `calibration_*`) e timbro `cal-momenti-1.1` (n_fit 4.743, estimator momenti, window 730 giorni, λ×0,9135); orizzonte fino al 30/5/2027, 7/7 leghe; promosse coperte (Elversberg–Bayern 18/18/64, Schalke, Estrela, Viseu) — in `f07ec3f` 0 partite senza previsione nel calendario completo (vs 150 offline surrogato); `lambda_limitata` 62 righe (2,9%); bias λ walk-forward +0,053 (da +0,264, griglia lasciava +0,128) e su campione pieno −0,024; pareggio previsto 26,2% vs osservato 25,6% (entro Wilson); Over 2,5 Δ1,7 pp ≤2 pp; Brier mercati 0,20516 (da 0,20646). Build locale su `26c5151` (secondo daily 17:24 UTC): `site/stato.html` Mancante 2, Presente 1.018, Atteso 215 su 95 schede (95×13=1.235 campi), 93 senza campi scaduti — coincide con correzioni del quinto giro (Mancante 4→2); calendario completo 1.987 partite in 8 mesi, 0 senza previsione. Deploy Pages: job `deploy` verde in entrambi i daily (run `34766831566` 15:51 UTC su `5822b63` e `34770832760` 17:11 UTC su `e6da512`). Riga di collasso «previsioni: 3.210 righe → 138 (una per partita)» non scaricabile dal sandbox (blob storage Azure bloccato, `gh run view --log` EOF), ma verificata indirettamente: a `5822b63` `predictions.parquet` = 3.291 righe (23 versioni per partita), a `f07ec3f` 2.152 righe uniche per `(match_id, model)` — il codice `latest_per_match` in `predict.py` dichiara il collasso e la chiave `store.TABLE_KEYS["predictions"]` ora è `(match_id, model)`.

**Settimo giro — verdetto del laboratorio (priorità)** — sessione `arena/01a09bd0-football-deep-analyzer` (da `main` `26c5151`). L'utente ha dispatchato il workflow `lab` con 5 candidati (`dc_elo_prod dc_elo_tilt prod_w50 prod_w85 dc_puro`, step_days 90, min_train 800, tutte le leghe). Il workflow ha prodotto **un solo candidato** (`dc_elo_prod`) in `data/processed/model_lab.parquet` (8 righe: 1 ALL + 7 leghe, n=1.516, RPS 0,203373, commit `e6da512` «lab: confronto modelli 2026-09-13 16:26 UTC»). Causa: bug di parsing in `lab.yml` — `EXTRA="--candidates $CANDS"` senza virgolette: con `CANDS="dc_elo_prod dc_elo_tilt ..."` la shell espande in `--candidates dc_elo_prod dc_elo_tilt ...` e Typer prende solo `dc_elo_prod` come valore di `--candidates`, mentre gli altri 4 token finiscono in `league_keys` (argomento posizionale) che non corrisponde a nessuna lega → walk-forward gira solo sul baseline. Verificato su codice `cli.py:459-461` (`candidates.split()`) e `lab.yml:43`. **Verdetto ufficiale sul parquet committato**: nessun confronto possibile → si resta su `dc_elo_prod` per regola `docs/13` §5-§6 (serve ΔRPS con IC 95% interamente negativo E ≥5 leghe con RPS più basso).

**Riproduzione locale corretta** (stesso `history.parquet` 7.373 righe, 7 leghe, `cal-momenti-1.1` λ×0,9135, `--candidates "dc_elo_prod dc_elo_tilt prod_w50 prod_w85 dc_puro"` quotato): n=1.518 gare valutate walk-forward. Risultati ALL: `dc_elo_tilt` RPS 0,202850 Δ−0,000397 IC [−0,000769; −0,000035] (interamente negativo, migliore_in 56,9%), `dc_puro` 0,202926 Δ−0,000320 [−0,000904; +0,000287], `prod_w85` 0,203048 Δ−0,000198 [−0,000493; +0,000106], `dc_elo_prod` 0,203247 baseline, `prod_w50` 0,203735 Δ+0,000488 [+0,000065; +0,000895] significativamente peggiore. Bias λ: tilt −0,087, puro −0,087, w85 +0,014, prod +0,104, w50 +0,244; Brier mercati: tilt 0,202917, puro 0,202739, w85 0,201923 (migliore), prod 0,202335, w50 0,203864. Per lega vs baseline (RPS più basso = vittoria): `dc_elo_tilt` batte baseline in 5/7 leghe (ESP1 0,201323<0,201620, GER1 0,194111<0,194367, ITA1 0,203528<0,204712, NED1 0,198500<0,198625, POR1 0,180714<0,181037; perde in ENG1 e FRA1), `dc_puro` 5/7 (ESP1, FRA1, GER1, ITA1, NED1), `prod_w85` 5/7 (ESP1, FRA1, GER1, ITA1, NED1), `prod_w50` 1/7 (ENG1). Quindi **in locale** `dc_elo_tilt` soddisferebbe entrambe le condizioni (IC negativo + 5 vittorie), ma **non è ufficiale** finché il workflow non viene corretto (quotare `"$CANDS"`) e rieseguito in Actions: il verdetto vale solo sul parquet committato dal workflow (avvertenza 4). **Decisione**: restare su `dc_elo_prod` fino a nuovo lab ufficiale; aprire fix per `lab.yml` e riprogrammare dispatch con 5 candidati (o tutti i 22). **Tappa 2** (schede per ~2.000 partite lontane, +17 min build, contenuti in gran parte segnaposto) resta rinviata — consiglio precedente confermato: non ora, priorità a stabilizzare laboratorio e RPS per anticipo. **Deroga merge PR #27**: eseguito dall'agente su ordine esplicito utente (policy `00` sez. D: merge solo utente, deroga puntuale registrata in `docs/13` §9.6) — resta deroga puntuale, non regola permanente. **Prossimo**: (c) «RPS per anticipo» in Accuratezza (ora misurabile da `made_at` vs `utc_kickoff` su tutto il calendario) — implementazione dopo verdetto ufficiale.

**Giro precedente (2026-09-13, quinto giro) — **sessione `arena/01a09a79-football-deep-analyzer`**, **PR #27** aperta verso `main`. **Audit pre-merge richiesto dall'utente («c'è altro da verificare? le schede delle partite in programma hanno le giuste sezioni e contenuti?»): misurato, non opinato — 95 schede pre-partita su 7 leghe, 13 campi per scheda, e 4 correzioni fatte subito.** Risultato: **parità completa di sezioni** nelle 7 leghe su Analisi pre-partita, Scontro tattico, I giocatori che decidono, Contesto, Confronto di stagione, Fatti rilevanti (**100% ovunque**); i blocchi del modello (Previsione / Risultati esatti / Matrice / Quanti gol) su **93/95**; audit per campo **0 «mancante»** dopo le correzioni (attesi dalla fonte: formazioni 66, arbitro 75 = finestre reali di pubblicazione). **Le due eccezioni hanno una causa sola e non è il codice**: (a) **2 schede senza modello** (`Schalke 04–Elversberg`, `Estrela da Amadora–Académico Viseu`: neopromosse senza storico nel corpus surrogato del sandbox — in produzione ricevono il prior dalla stagione in corso, da ricontare dopo il merge); (b) **«Come nasce questa probabilità» solo su 25/95** perché le **138 previsioni committate sono di due versioni fa** (`dc-elo-ens-0.1`/`-0.2`, senza i campi `blend_p_*`/`*_raw`/`calibration_*`): verificato riga per riga **25/25 con `dc-elo-ens-0.3` hanno la sezione, 0/138 con riga vecchia** → il primo `fda predict` post-merge la porta su **tutte** le schede insieme con la calibrazione v1.1 e i limiti sulle λ. **Correzioni**: (1) `audit.py` — la previsione **non è un dato «atteso dalla fonte»** (`from_source = False`): con `--days-ahead 0` il modello non aspetta nessun editore, quindi se manca è **mancante** (prima i 2 buchi sparivano dal conto); (2) `audit.py` — distinta pubblicata e zero assenze = **presente**, non buco (4 squadre su 105 schede: Telstar, Excelsior, PSG, Moreirense → `stato.html` Mancante 4 → **2**, Presente 1.142 → **1.146**); (3) `match.html` — il silenzio non è un'informazione: «Nessun indisponibile segnalato nella distinta pubblicata dalla fonte (formazione probabile/ufficiale)», **solo pre-partita** perché sulle finite la fonte riporta le assenze una volta su due (133 sì / 138 no su 271) e lì il silenzio è ambiguo; (4) `base.html` — footer «Prossimi 7 giorni» → «**Prossime partite e calendario**». **Compatibilità verificate sullo stesso giro**: tutti i lettori di `predictions` tollerano una riga per partita (`build_accuracy`, `analysis.prediction`, `verify_site` [3]/[8], `anteprima_scheda.py` che scrive in `/tmp/preview_data` e **non** in `data/processed`); `daily.yml` `timeout-minutes: 40` contro ~11 minuti di run + ~1 minuto per l'orizzonte esteso; crescita dati committati 218 kB → ~550 kB a run (+~1,6 MB/giorno, ~11% di `data/processed`). **Copertura schede sulle partite in programma: 95 su 2.082 = 4,6%** — il resto ha la riga compatta del calendario: è la **Tappa 2** (§9.5), decisione utente. **Verifiche**: suite **172 passed** (+3 test: previsione mai «attesa», nessun indisponibile non è un buco, la frase in scheda con e senza distinta), `ruff --select F,E9` pulito, build 162 s (376 schede / 2.364 fixture / 7.394 giocatori), `verify_site` **0 problemi · 11.793 controlli**. Dettaglio e tabella di parità: `docs/13` §9.6. Merge solo utente.

**Giro precedente nella stessa sessione (2026-09-13, quarto giro — Tappa 1) — **sessione `arena/01a09a79-football-deep-analyzer`** (da `main` `e15a1f0`). **Tappa 1 dell'estensione dell'orizzonte — approvata dall'utente con «procedi» e implementata: il modello copre tutto il calendario, non solo i prossimi 7 giorni.** Diagnosi di partenza misurata sui dati locali: **138 partite previste su 2.083 in programma (6,6%)**, `predictions.parquet` con **3.210 righe = ~23 versioni per partita** (nessuna pagina le usava) e «Prossime» ferma a 84 link. **(1) Una riga per partita**: `store.TABLE_KEYS["predictions"]` passa da `(match_id, model, made_at)` a `(match_id, model)` e il nuovo `models/predict.latest_per_match` tiene l'ultima previsione per (partita, modello) — usato sia come **migrazione** (`fda predict` collassa le versioni accumulate e lo dichiara: «previsioni: 3.210 righe → 138 (una per partita)») sia come garanzia a regime; `build_accuracy` sceglieva già l'ultima previsione pre-partita, quindi la pagina Accuratezza non cambia. **(2) Tutto il calendario**: `fda predict --days-ahead 0` (nuovo default, `fda daily` compreso) prevede ogni gara `scheduled` con calcio d'inizio futuro — **zero richieste in più** (l'elenco arriva dalla chiamata `fixtures` per lega che `fda collect` fa già; il modello usa solo squadre e storico), mentre restano legate alla finestra `future_days` le raccolte *per partita* (formazioni, infermeria, meteo, arbitro) che la fonte pubblica solo a ridosso della gara. **(3) Pagina «Prossime» estesa**: le righe ricche dei 7 giorni restano invariate (schede comprese) e sotto compare «**Tutto il calendario**» — navigazione per mese, un `<details>` per mese col primo aperto, una riga compatta per partita (data e ora italiane · gara e campionato · 1X2 col preferito in grassetto e a colore · gol attesi · Over 2,5); filtri e ricerca della toolbar valgono anche lì e il link alla scheda compare **solo se la pagina esiste**. **Misure [offline dal sandbox, storico surrogato da `h2h`: i numeri di gioco non sono decisionali]**: copertura **138 → 2.000 partite su 2.083 (96%)**; nel calendario oltre i 7 giorni **1.837 righe con previsione e 150 senza** (7,5%: storico surrogato insufficiente, la riga lo scrive invece di lasciare buchi); costo **29,7 ms per partita** → 1.862 previsioni in **55 s** (~5 minuti al giorno su 5 run, dentro il budget del `daily`); `predictions.parquet` **1,6 kB → 0,27 kB per partita coperta** (218 kB/138 → 549 kB/2.000: il vecchio file comprimeva bene perché era 23 copie quasi identiche, 69 B/riga contro 469 B/riga delle stesse righe collassate); `build_indexes` **2,4 s** (le righe compatte non chiamano `analysis.list_context`), build completo 146 s invariato; `prossime.html` **274 kB → 1.102 kB** (+430 B per riga, **83 kB gzip**), `index.html`/`risultati.html` invariati. **Cura dei dettagli**: `build.pct_triple` (resto massimo) garantisce un 1X2 che somma **sempre** 100 anche dove il lettore non ha contesto; preferito marcato due volte (grassetto + colore, WCAG 1.4.1); decimali italiani ovunque; il dettaglio per partita dei limiti sulle λ scende a `DEBUG` e `predict_matches` scrive **un solo riepilogo** col tasso di intervento («toccate 29 partite su 2.084, 1,39%»). **Nuovo controllo [11] in `scripts/verify_site.py`**: 1X2 = 100, `aria-label` = testo visibile, grassetto = preferito e coerente con `cal-fav-*`, gol attesi in formato italiano, data/ora valide, mesi in ordine con etichetta coerente, conteggio dichiarato = righe stampate, navigazione = sezioni. **Verifiche**: suite **169 passed** (+6 test), `ruff --select F,E9` pulito su `src`/`tests`/`scripts`, `verify_site` sulla build locale completa **0 problemi · 11.793 controlli** (di cui [11] **9.643 su 1.987 righe**); anteprima servita su porta 8000 da una build su copia dello store in `/tmp/prev` (`data/processed` **non** toccato; `model_lab.parquet` surrogato **non** committato, come da decisione del terzo giro). **Effetto dell'orizzonte sulla qualità: offline non è misurabile** (RPS 0,2027 / 0,2138 / 0,1934 / 0,1943 per 0-7 / 8-14 / 15-30 / 31-60 giorni di anticipo, non monotono e dentro il rumore, SE ≈ 0,03 per bucket): con tutto il calendario previsto saranno i dati veri a misurarlo (`made_at` + `utc_kickoff`), ed è il prossimo passo sulla pagina Accuratezza. **Tappa 2** (schede per le partite lontane: ~2.000 pagine in più, **+17 minuti** di build stimati, contenuto in gran parte segnaposto) **rinviata — serve decisione utente**, dettagli in `docs/13` §9.5. **Push sbloccato** (GitHub riconnesso in Arena): i commit della sessione (`84e9212`, `0799003`, `bde3388`, `3c2dd8e`, `fd2ab6e`, `8425158`) sono su `arena/01a09a79-football-deep-analyzer` e la **PR #27** verso `main` è **aperta, `MERGEABLE`, check `test` verde** (1m4s, run `34764649430`) — **merge solo utente**. Calibrazione v1.1, limiti sulle λ, laboratorio e Tappa 1 restano da confermare sul primo run `daily` in Actions dopo il merge (§9.4).

**Giro precedente nella stessa sessione (2026-09-13, terzo giro) — **sessione `arena/01a09a79-football-deep-analyzer`** (da `main` `e15a1f0`). **Auto-audit richiesto dall'utente («c'è altro che deve essere più accurato?»): 6 difetti trovati, misurati e corretti — due erano nelle modifiche del giro precedente.** Piano, misure e criteri in [`docs/13_modelli_e_schede_piano_2026-09-13.md`](13_modelli_e_schede_piano_2026-09-13.md) (§3.1-3.3, §5.1-5.3). **(1) Calibrazione v1.1 a momenti** (`cal-momenti-1.1`): il moltiplicatore non esce più da una griglia di punteggio — che su una funzione obiettivo piatta sceglieva 0,94 quasi a caso fra 0,93 e 0,95 — ma dall'**uguaglianza dei momenti sugli ultimi 730 giorni** → **λ×0,9135** (Δρ −0,04 invariato, `estimator`/`window_days` salvati e mostrati). Misurato sul backtest reale (5.812 gare fuori campione, 7 leghe): bias λ walk-forward **+0,264 → +0,053** (la griglia lasciava +0,128), Brier mercati walk-forward **0,20646 → 0,20516**, RPS 1X2 0,20056 → 0,20081 (+0,0003: è il prezzo dichiarato, l'1X2 è al pavimento informativo e la pagina Accuratezza mostra anche il confronto «senza calibrazione»), sul campione pieno bias λ **+0,245 → −0,024**, pareggio **23,9% → 26,2%** (osservato 25,6%), Brier 9 mercati **0,2025 → 0,2017**. Finestra scelta misurando la **non stazionarietà** del bias (m per semestre 0,963/0,935/0,881/0,917/0,902/1,034); limiti di sicurezza 0,85–1,05; avviso se il bias residuo resta > 0,10; `as_row` a **6 decimali** (a 4 il giro parquet→memoria non restituiva lo stesso numero); `n_fit` dichiara ora le gare **di stima** (4.743) e non il campione (5.791) — la scheda scrive «λ × 0,91 (momenti, ultimi 730 giorni) stimata su 4.743 gare fuori campione». Calibrazione **per lega misurata e scartata** (Brier 0,20510 contro 0,20514 globale = rumore, 7 parametri in più su campioni da 300-900 gare). **(2) Limiti di sicurezza sulle λ invertite** (`predict._clamp_lambda`): `goal_expectancy` risolve due λ libere **senza vincoli** e sulle previsioni pubblicate arrivava a **Barcellona-Racing Santander 6,17+2,25 = 8,4 gol attesi** (Over 2,5 al 99%, matrice centrata sul 5-1) — mentre il modello sui gol da solo non supera mai 3,84 per squadra né 4,73 totali e l'inversione arriva a 5,09/6,77 (rapporto 1,14 medio, 1,28 al p95, 1,55 max). Ora il totale resta fra il 70% e il 135% di quello del modello sui gol, ogni λ ≤ 4,0, tetto assoluto 5,5, **inclinazione casa/trasferta conservata**; quando il limite lega si pubblica l'1X2 della griglia limitata (scheda coerente) e la riga lo dichiara (`lambda_limitata`). Misurato: tocca **69 gare su 5.791 (1,19%)**, in tutte e 7 le leghe (POR1 21, NED1 15, ITA1 9, GER1 7, FRA1 7, ENG1 6, ESP1 4), λ max 5,09→**4,00** e 6,77→**5,49**, RPS/log-loss **invariati**, bias λ +0,245→+0,242; sulle gare toccate Brier 9 mercati 0,1471→**0,1450**, log-loss 0,5676→**0,5664**, bias λ +0,791→**+0,561** (+0,395→+0,184 dopo calibrazione). **(3) Laboratorio reso equo — 2 bug che falsavano il confronto**: `_apply_calibration` ricostruisce una griglia τ dalle λ e le miscele dichiaravano le λ del DC → con una calibrazione attiva **`mix_50` diventava identico a `dc_puro` riga per riga** (verificato: 405/405 gare, p_home, mercati e λ_total uguali), cioè il candidato che doveva misurare la miscela non misurava niente; e le **famiglie non-DC non ricevevano alcuna correzione** (confrontate grezze contro una baseline calibrata: correggendo il livello, il loro RPS sul corpus surrogato passa da 0,2058 a **0,1968**). Ora ogni candidato corregge il **proprio** livello di gol sulle gare che ha già valutato (`self_calibrate`, soglia 100 gare, colonna `scala_media` nel riepilogo, `--no-self-calibrate` per il confronto con la ricetta pubblicata) tramite **inclinazione esponenziale** p′(i,j) ∝ p(i,j)·θ^(i+j) (`_tilt_total`), che per Poisson coincide con λ·θ e per le altre famiglie sposta la media **senza trasformare una binomiale negativa in un Dixon-Coles**; le miscele dichiarano le λ della propria matrice e `_mix_grids` riceve gli stessi limiti di sicurezza della produzione (su un vettore irraggiungibile — pareggio al 9,5% con Elo degenerato — l'inversione scappava a 8,9 gol attesi). **(4) Candidati 15 → 22**: `dc_elo_tilt` (l'Elo **inclina** il rapporto casa/trasferta senza gonfiare i gol attesi: sul surrogato stesso RPS della produzione, 0,1996 contro 0,1997, con bias λ −0,186 invece di +0,155 — è la correzione **strutturale** del difetto che oggi la calibrazione tampona globalmente), `prod_w50`/`prod_w85` (il peso 0,7 della media non era mai stato misurato), `elo_k10`/`elo_k40`/`elo_hfa40`/`elo_hfa80` (iperparametri Elo mai tarati: k 10 risulta **significativamente peggiore**, IC [+0,0040; +0,0110]; k 40 è il migliore sul surrogato ma con IC che contiene lo 0). **(5) `GRID_SIZE = 11` unica in `dc_grid.py`**: `calibrated_prediction` pubblicava da una griglia 10×10 mentre la calibrazione era stimata su 11×11 (scarto ~4e-7 su una probabilità: piccolo, ma si ottimizzava una superficie e se ne mostrava un'altra). **(6) Verificatori e anteprima**: 2 falsi positivi nel controllo [7] (previsto arrotondato esattamente sul bordo dell'intervallo di Wilson), il controllo [9] **non catturava l'ultima colonna del dotplot** (difetto nascosto finché la coda era sempre vuota: con la nuova calibrazione alcune partite hanno punti nella colonna «7+»), `st.close()` chiamato su un dict per ombreggiamento del nome nel ciclo [10], e `scripts/anteprima_scheda.py` ora riapplica i limiti di sicurezza alle previsioni già pubblicate (336 righe) così l'anteprima mostra ciò che produrrebbe `fda predict`. **Verifiche**: suite **163 passed** (0 warning), `ruff --select F,E9` pulito su `src`, `tests`, `scripts`; `scripts/verify_site.py` **0 problemi · 2.213 controlli numerici superati** (backtest ricalcolato RPS 0,1997 = pagina; [8b] λ×0,914 ρ−0,04 `cal-momenti-1.1`, bias gol +0,245 → −0,023, pareggio 23,9% → 26,2% su osservato 25,6%; [9]/[10] 138 ciascuno); sito di anteprima 376 schede / 2.364 fixture / 7.394 giocatori. **Nessuna verifica live dal sandbox** (mirror irraggiungibili): calibrazione v1.1, limiti sulle λ e laboratorio vanno confermati dal primo run in Actions; `model_lab.parquet` prodotto sul corpus surrogato **non** è stato committato di proposito (numeri nei doc, non nei dati). **Push ancora bloccato** (token GitHub non valido: `gh auth status` «token in GH_TOKEN is no longer valid») → commit locali `bde3388`, `3c2dd8e` + questo in attesa di riconnessione GitHub in Arena. Merge solo utente.

**Giro precedente nella stessa sessione (2026-09-13, secondo giro) — numeri poi corretti dal terzo giro: calibrazione λ×0,9135 a momenti su finestra 730 giorni invece di λ×0,94 su griglia, 22 candidati invece di 15, suite a 163 test invece di 150, e limiti di sicurezza sulle λ invertite:** **sessione `arena/01a09a79-football-deep-analyzer`** (da `main` `e15a1f0`). **Modelli più precisi + schede più leggibili**: piano, ricerca e misure in [`docs/13_modelli_e_schede_piano_2026-09-13.md`](13_modelli_e_schede_piano_2026-09-13.md). **Diagnosi misurata sul backtest fuori campione (5.812 gare, 7 leghe)**: l'1X2 è solido (RPS **0,1993** contro Elo 0,2027 e naive 0,2315) ma **tutto ciò che dipende dai gol è gonfiato** — λ totali **3,107 vs 2,862 osservati (+8,6%, bias positivo in 7 leghe su 7)**, pareggio dichiarato 23,9% vs 25,6%, Over 2,5 59,0% vs 54,8%, BTTS 59,1% vs 55,0%. **Causa meccanica isolata**: `ensemble()` ricalcola le λ con `goal_expectancy` sulle probabilità 1X2 mescolate con Elo e le rigonfia del 15-20% (la τ di Dixon-Coles non c'entra: scarto griglia/λ dichiarata 0,0003). **Intervento 1 — calibrazione della griglia** (`models/calibration.py`, `fda calibrate`, tabella `calibration`): si sceglie su griglia il paio (λ×m, Δρ) che minimizza RPS+0,5·Brier mercati con valutazione walk-forward a 4 fold; sul backtest reale **m=0,94, Δρ=−0,04** → bias λ **+0,245 → +0,058**, pareggio **23,9% → 25,7%** (osservato 25,6%), Brier mercati in walk-forward **0,2065 → 0,2055**, RPS 1X2 invariata (+0,0001, entro il rumore): la correzione agisce su λ/ρ e **ripubblica l'intera previsione dalla griglia corretta**, quindi 1X2, doppia chance, risultati esatti, Over/Under, BTTS, porte inviolate e matrice in scheda restano coerenti; sotto 1.200 gare di backtest non si corregge nulla; `fda daily` ora è collect → **calibrate** → predict → backtest → simulate → build e degrada al modello grezzo se la calibrazione fallisce; `predict` scrive nella riga `lambda_scale`, `rho_shift`, `calibration_version`, i valori grezzi (`lambda_*_raw`, `rho_raw`, `blend_p_*`) e le nuove `dc_p_*`. **Intervento 2 — laboratorio modelli** (`models/lab.py`, `fda lab`, tabella `model_lab`, workflow `lab.yml` settimanale/dispatch): **15 candidati** (DC puro e varianti ξ/shrinkage, Poisson, Poisson bivariata, binomiale negativa, zero-inflazionata, copula di Weibull, Elo, Pi-ratings, miscele DC+Elo nella griglia 50/85, produzione) confrontati walk-forward con **bootstrap appaiato al 95%** sullo stesso insieme di partite, tabella per lega e stacking convesso; primo giro dal sandbox su corpus **surrogato** ricostruito da h2h (`scripts/corpus_da_h2h.py`, 4.803 gare, distorto: −10% di gol) → 783 gare valutate in 59 s, baseline `dc_elo_prod` RPS 0,2036, **nessun candidato significativamente diverso (tutti gli IC contengono lo 0)**: la scelta va presa sul primo giro in Actions con `history.parquet` reale, che da ora `fda simulate` salva a ogni run (snapshot per lega). **Intervento 3 — schede**: due blocchi nuovi derivati solo dai numeri già pubblicati, «**Quanti gol, in pratica**» (distribuzione dei gol totali in «partite su 100» con somma esattamente 100 + **quantile dotplot a 20 punti**, 1 punto = 5 partite su 100, moda/mediana/intervallo 10-90%) e «**Come nasce questa probabilità**» (catena modello sui gol → media con Elo → calibrazione, con Δ in punti percentuali e la nota «λ × 0,94 stimata su 5.812 gare fuori campione»; i passi non tracciati non compaiono). **Difetti trovati e corretti strada facendo**: λ NaN dello store producevano matrice/distribuzione di NaN (ora i blocchi spariscono); le righe per mercato di Accuratezza non pubblicavano `(k/n)` e `verify_site` ricostruiva k dalla percentuale arrotondata → **2 falsi problemi**; decimale col punto nella nota della calibrazione (→ `dec`/`int_it`). **Verifiche**: suite **150 passed** (+32 test: calibrazione, laboratorio, viste della scheda, storico salvato), `ruff --select F,E9` pulito, `fda build` 376 schede / 2.364 fixture / 7.394 giocatori, `scripts/verify_site.py` **4.088 pagine · 0 problemi** con i nuovi controlli **[9]** (138 distribuzioni dei gol ricalcolate) e **[10]** (138 scomposizioni della probabilità). Anteprima statica servita su porta 8000 (build su copia dello store in `/tmp/preview_data` con la calibrazione applicata: `scripts/anteprima_scheda.py`, `data/processed` non toccato). **Nessuna verifica live dal sandbox** (mirror irraggiungibili): calibrazione e laboratorio vanno confermati dal primo run in Actions. **Nota di sessione**: il push del terzo commit è fallito per token GitHub scaduto (`gh auth status`: «token in GH_TOKEN is no longer valid») → commit locale in attesa di riconnessione GitHub in Arena. Merge solo utente.

**Sessione precedente (`arena/01a09a3b`, PR #26):** 2026-09-13 — **sessione `arena/01a09a3b-football-deep-analyzer`**, PR **#26** OPEN, `MERGEABLE`, check `test` verdi. Ricerca + implementazione UX quantitativa per «Oggi» e schede partita. Snapshot locale misurato: **21 partite oggi, 7/7 campionati, 21/21 previsioni, 42/42 squadre con classifica, 21/21 meteo e arbitro, 20/21 H2H utilizzabili**. Nuovo documento: [`docs/12_oggi_schede_ux_quantitativa_2026-09-13.md`](12_oggi_schede_ux_quantitativa_2026-09-13.md). Implementati: riepilogo giornata, filtri stato/lega/ricerca, card partita a tre livelli con forma/classifica/assenze/meteo/arbitro/H2H, hero della scheda, indice sticky, margine a una cifra decimale e concordanza DC–Elo, vento quando presente, correzione narrativa del pareggio più probabile. **Verifiche completate:** `pytest` **118 passed**; build **376** schede, **2364** fixture, **7392** giocatori; `scripts/verify_site.py` **4087 pagine, 1860 controlli numerici, 0 problemi**. Pulite due righe snapshot obsolete (`Nottm Forest`/`Frankfurt`) e reso `season_sim` sostitutivo per lega. Preview statica avviata su porta 3000; resta controllo manuale a 375 px. **Diagnosi aggiornamenti:** il `daily` GitHub Actions `34749412140` del 13/09 09:23 UTC ha completato collect/predict/build ma ha fallito nel passo «Commit dei dati aggiornati», quindi Pages è stato saltato e la produzione è rimasta alla build 01:14. La correzione del commit/rebase con retry è inclusa nella PR #26. Merge solo utente.

**Turno precedente (dettaglio storico — UX quantitativa Oggi/schede):** 2026-09-12 (terzo giro) — **sessione `arena/01a094e1-football-deep-analyzer`**, PR **#23** (`OPEN`, head `a49ca95` → aggiornato con questo giro, `MERGEABLE`; merge solo utente). **Completato il resto del piano di `docs/11_partite_oggi_miglioramenti.md`: post-partita, statistiche di dettaglio, accuratezza per mercato.** (1) **Cronaca con assist e tipo di gol**: «⚽ Højlund · assist di Di Lorenzo · 0-2» + «di testa»/«rigore»/«punizione diretta»/«rovesciata» — **534 assist su 752 gol** (71%), nome risolto dalla distinta della stessa partita nel 100%, tipo su 143 gol (gli autogol non ripetono «autogol»); (2) **Primo e secondo tempo**: xG, tiri, in porta, possesso, angoli, grandi occasioni per periodo — **239/239** partite finite, 7/7 leghe; (3) **I portieri**: parate, **gol prevenuti**, errori→gol, rigori parati — **239/239**, portiere identificato da `saves`/`goals_prevented` e validato **478/478** con ruolo 0 in distinta (la prima versione prendeva i difensori: van Ewijk al posto di Raya); (4) **Dati fisici** condizionali (distanza km, sprint, metri in sprint, più veloce con km/h) — **30 partite**, card solo quando i dati esistono; (5) **Statistiche di dettaglio**: le altre **23 voci** FotMob (tiri da dentro/fuori area, xG azione manovrata vs palle inattive, duelli a terra/aerei, intercetti, rinvii, tiri bloccati, dribbling, cross, lanci, passaggi per metà campo, legni, fuorigioco) su due colonne — **239/239**; (6) **Accuratezza per mercato** su `accuratezza.html`: 9 mercati (Over 1,5/2,5/3,5, BTTS, doppie chance 1X/12/X2, porte inviolate) con previsto vs osservato, **Brier**, Brier della frequenza di base, Δ e scelte indovinate. **Esito pubblicato anche se scomodo**: il modello batte la frequenza di base solo su doppia chance 1X (Δ −0,0223) e porta inviolata in trasferta (Δ −0,0139); sugli altri mercati il Brier è peggiore del riferimento e la tabella ne mostra la causa (Over 2,5 dichiarato 56,0% contro 64,3% osservato: sottostima dei gol, coerente con lo shrinkage già corretto nel codice ma non ancora nelle previsioni pubblicate). **Difetti/correzioni di questo giro**: quirk **pandas 3** — con colonne di dtype `str` l'indicizzazione `df[tupla]` dà KeyError anche quando le etichette esistono → sempre liste; identificazione del portiere; regex del verificatore che non tollerava l'a capo fra le celle e confrontava i nomi degli assist con la grafia globale invece di quella della partita (Uriel/Uriël van Aalst, Josko/Joško Gvardiol). **Verifiche**: suite **106 passed** (+5 test post-partita); `ruff --select F` pulito; `fda build` → 347 partite + 2364 fixture + 7388 giocatori; `scripts/verify_site.py` → **4056 pagine · 0 problemi · 1855 controlli numerici** (nuovo passo `[6]`: **534 assist** e **239 split 1T/2T** ricontrollati contro `events`/`team_stats`). **Aperto**: rileggere la tabella dei mercati dopo il primo `fda predict` con la rete; metriche fisiche limitate dalla fonte (30 partite).

**Turno precedente (dettaglio storico, stessa sessione — card pre-partita):** 2026-09-12 (secondo giro) — **sessione `arena/01a094e1-football-deep-analyzer`**, PR **#23** aperta su `main` (merge solo utente, policy sez. D). **Profondità delle partite «oggi» implementata** (piano `docs/11_partite_oggi_miglioramenti.md`, priorità utente **giocatori e assenze**). **Card nuove nelle schede pre-partita**: (1) **«Come arrivano»** — ultime gare con **xG contro xGA**, risultato, avversario, esito, xG/xGA per gara, **punti fatti contro xPTS**, PPDA, split casa/trasferta, tendenza (Understat su 38 squadre di oggi, fallback FotMob `team_stats` su 14: NED1/POR1; avversario recuperato dal calendario **191/191 righe**; sotto 3 gare con xG la card non compare); (2) **«I giocatori che decidono»** — **(xG+xA) per 90** con minuti, gol, assist, xG, xA, occasioni create e media voto su **7/7 leghe** (FotMob `player_stats`), soglia di minutaggio **relativa** (40% dei minuti del più impiegato, min 90′) e esclusione di chi non ha alcuna riga xG/xA (non è «a zero»: è senza dato); sotto resta la classifica per media voto di stagione; (3) **«Infermeria pesata»** — ruolo, motivo, rientro, **minuti di stagione**, gol+assist, **xG+xA per 90** e marchio «titolare» per ogni assente, con in testa quanti titolari abituali mancano e quanti xG+xA a partita si perdono (248 assenti oggi: **99 con ruolo**, **81 con statistiche**); (4) **«Precedenti completi»** — tutti i precedenti in archivio (media 19, max 43) con V/N/P dal punto di vista della casa attuale, gol a gara, **BTTS%**, **Over 2,5%**, risultati più frequenti, astinenza da pareggio e arco temporale con date complete (**103 schede verificate**); (5) **arbitro a confronto con la media del campionato** (gialli e falli per gara, n. designazioni); (6) **fatti rilevanti da 3 a 5** per partita. **Liste**: ogni riga futura mostra **«Infermeria: <casa> N assenti · <trasferta> M assenti»**. **Difetti corretti strada facendo**: **19 — ruoli spostati di uno** (`POSITION_NAMES` partiva da 1, la codifica FotMob `usualPosition` parte da **0**: misurato su 616 formazioni, il valore 0 è il portiere in 600/600; ogni distinta era etichettata col ruolo precedente e il portiere restava senza etichetta) → mappa ricentrata 0-3 + `POSITION_ID_ROLE` per i `positionId` tattici tenuta solo con ≥20 titolari concordi nel 90% dei casi + fallback sullo storico distinte per gli assenti; **`pivot_table(dropna=False)` faceva il prodotto cartesiano** dei livelli dell'indice (18 giocatori → 324 righe con nomi incrociati: le statistiche di stagione finivano sul giocatore sbagliato) → `groupby().unstack()`; plurali («1 clamorose»), intestazione «Precedenti (40)», nuovo filtro `it_dt_full`. **Verifiche**: suite **101 passed** (+9 in `tests/test_oggi_depth.py`: codifica ruoli, catena di risoluzione, trend Understat e fallback FotMob, precedenti con riga anomala scartata, soglia relativa, infermeria pesata, arbitro, chiavi di `build()`); `ruff --select F` pulito; `fda build` → 347 partite + 2364 fixture + 7388 giocatori; `scripts/verify_site.py` → **4056 pagine · 0 problemi · 1082 controlli numerici** (nuovo passo `[5]`: **648 ruoli**, **108 infermerie**, **103 archivi di precedenti** ricontrollati contro le tabelle). **Resta dichiarato**: post-partita (assist sui gol 535/753, split 1T/2T 478/478, portieri, metriche fisiche condizionali 12,5%), le 27 chiavi di `team_stats` non ancora a schermo, accuratezza per mercato (dettaglio in `docs/11_…` sez. F5).

**Turno precedente (dettaglio storico, stessa sessione):** 2026-09-12 — **sessione `arena/01a094e1-football-deep-analyzer`** (da `main` `cb68f5a`). **Verifica profonda di sito e progetto + correzioni** (dettaglio in `docs/10_verifica_sito_2026-09-12.md`). Nuovo strumento versionato **`scripts/verify_site.py`** (contenuto + collegamenti + ricalcolo numerico su tutte le pagine, con test): **4056 pagine · 0 problemi · 140 controlli numerici** (prima: 1254 problemi di contenuto e 14 difetti di calcolo/dati). **Correzioni principali**: (1) **τ di Dixon-Coles con λ scambiate** — `advanced.dixon_coles_grid` e `create_dixon_coles_grid` di penaltyblog applicano `1+λρ`/`1+μρ` a celle invertite rispetto a `DixonColesGoalModel.predict` (1,3 pp sull'1X2; ricostruzione corretta a 2,8e-17) → modulo condiviso `models/dc_grid.py`; (2) **shrinkage DC** verso la media di lega `f = w/(w+8)` (neopromosse al bordo ±2,5: Dortmund–Paderborn λ 1,02–0,19), gauge invariante, `MODEL_VERSION` → `dc-elo-ens-0.2`; (3) **doppia chance** ricalcolata come somma del 1X2 mediato (era incoerente nel 14,3% delle previsioni, fino a 1,14 pp); (4) **cronaca**: punteggio ricostruito (FotMob dà il punteggio *prima* del gol, 226/226), autogol non ribaltato (22/22), gol duplicati scartati; (5) **tiri in porta** = (gol o parata) ∧ non bloccato ∧ non autogol → 476/478 contro `ShotsOnTarget` (era 94,8%); (6) **snapshot per partita**: `upsert(..., replace_by="match_id")` + chiave `lineup` senza ruolo → formazioni da 13-19 nomi (573 coppie duplicate) e sostituzioni perse (6,5/partita invece di ~10) risolte alla radice; rinfresco delle finite già archiviate con `COLLECT_SNAPSHOT=2` (6/lega/run, ~42 richieste); (7) **contenuti**: `nan` in 292 schede giocatore → «ruolo n.d.», 1098 concordanze («1 gare») → filtro `it_plural`, 173 decimali col punto → virgola, meteo/situazioni/competizioni/xG tradotti; distinta a 11 (11 esatti in 472/478 usando il voto di partita, nessun taglio arbitrario sulle future). **Verifiche**: suite **91 passed** (+10 test); `fda build` 347 partite + 2364 fixture + 7388 giocatori; `scripts/verify_site.py` 0 problemi. **Aperto (dichiarato)**: le previsioni pubblicate cambiano solo al primo `fda predict` con la rete (verificare λ Paderborn e parametri DC non più a ±2,5); le finite archiviate guariscono in ~1-2 giorni di run. **Prossimo**: PR verso `main` → merge utente (policy sez. D).

**Sessione precedente (dettaglio storico, `arena/01a092ab`):** 2026-09-12 — **sessione `arena/01a092ab-football-deep-analyzer`** (da `main` `7ab51fe` = data run 20:19 UTC; PR #1–#21 mergiate, nessuna PR aperta). **Laboratorio analitico sulle schede partita** (progetto `docs/08_laboratorio_analitico.md`): (1) **matrice dei punteggi** 0–5 dalla griglia Dixon-Coles (λ + ρ, coda 6+ dichiarata); (2) **scontro tattico** (attacco/difesa DC, xG/xGA, xG azione vs palle inattive da FotMob su 235/235 finite 7/7, PPDA/deep Understat); (3) **corsa xG** SVG dai tiri (autogol esclusi); (4) **qualità dei tiri** (xG/tiro, split situazioni tradotte, gol−xG, gol−xGOT); (5) **probabilità in-play** ricostruite dopo ogni gol (λ residue ∝ tempo, Poisson indipendente sul residuo — ρ non scalato). Nessuna fonte nuova. **Verifiche (offline, dati `7ab51fe`)**: suite **81 passed** (8 test `test_advanced.py`); ruff F,E pulito su codice nuovo; `fda build` → **343 partite**; 0 leak `RegularPlay`/`FromCorner`; 0 `nan`; matrice 101/101 con previsione; corsa+qualità 235/235 finite con tiri; WP 23/23 finite con previsione e gol. **Prossimo**: PR verso `main` → merge utente.

**Sessione precedente (dettaglio storico, `arena/01a0864d`):** 2026-09-09 (sera) — **sessione `arena/01a0864d-football-deep-analyzer`** (da `main` `99ba9ee` = data run 13:10 UTC; PR #1–#19 tutte mergiate, nessuna PR aperta). **Fase 3 — schede giocatore, completata offline** (progetto in `docs/07_fase3_giocatori.md`): (1) **backfill esteso a tutte le 7 leghe** (prima solo NED1/POR1): ogni partita finita di stagione viene scaricata una sola volta — catch-up misurato **97 richieste una tantum**, poi ~10/lega a giornata, nel budget 600/run; (2) **modulo `src/fda/site/players.py`** (`PlayerCatalog`): aggregazioni di stagione da `player_stats` (chiave assente = 0 eventi, semantica FotMob verificata), per-90, media voto ponderata sui minuti, **percentili lega+ruolo** (soglia ≥90′, min 8 pari-ruolo con dato, stat «lower is better» capovolte), **radar SVG** a 6 assi per ruolo (P/D/C/A mappati da `usual_position_id`, inchiodato per incrocio con Understat su 1.539 giocatori), log partite con V/N/P dal punto di vista della squadra e link ai report; (3) **pagine**: `giocatori/index.html` (hub 7 leghe), `giocatori/<LEGA>.html` (tabellone con filtro testo+ruolo, JS vanilla), `giocatori/<id>.html` (3.457 schede: anagrafica, stagione totali+per-90, radar, barre percentili con N pari-ruolo, log; segnaposto onesto per chi non è sceso in campo, avviso «campione ridotto» sotto 270′); nav «Giocatori», link dalle card «da tenere d'occhio» e «Migliori in campo», metodologia in `info.html`; (4) fix i18n trovato in verifica: **`unavailability_type` («injury»/«suspension») non era tradotto** nelle schede partita → mappa `unavailability_it()` condivisa; (5) le partite finite fuori finestra ora hanno pagina (archivio). **Verifiche (offline, dati reali del run `99ba9ee`)**: suite **73 passed** (8 nuovi test `test_players.py`: per-90, soglie, inversione, geometria radar, V/N/P, segnaposto; e2e esteso); **ruff 0 nuove segnalazioni** (17 pre-esistenti su righe vecchie); `fda build` → **211 partite + 3.465 giocatori + 7 pagine lega in ~70 s** (103 MB); audit i18n su tutte le 3.465 pagine: **0 residui** (settimane/mesi/meteo/ruoli/decimali-punto; «Jonas Wind» è un nome, non meteo). Esempio verificato: radar portiere J. Robles (Estoril) con 9 barre, 0 n.d. **Da verificare dal vivo dopo il merge** (primo `daily`): `player_stats` 134 → ~231 partite (catch-up 97), percentili con popolazione piena per lega (ora big-5 con ~11 pari-ruolo perché il backfill big-5 parte dal merge), `source_status` senza errori nuovi. **Prossimo**: PR verso `main` → merge utente.

**Sessione precedente (dettaglio storico, `arena/01a08630`):** 2026-09-09 (pomeriggio) — **sessione `arena/01a08630-football-deep-analyzer`** (da `main` `7173639` = data run 12:36 UTC, post merge PR #19). **Turno 1 — card pre-partita «Fatti rilevanti» dagli `insights` FotMob**. Tabella già raccolta (1274 righe / 211 match) ma mai mostrata: testi inglesi a template. Catalogo misurato: 11 template squadra + 4 H2H + 5 giocatore. Si traducono solo i fatti oggettivi (streak, gol recenti, testa-a-testa, capocannoniere); hype («most shots on target», «most clean sheets in the competition», penalità, medie) e testi sconosciuti sono **scartati** (niente inglese a schermo). `translate_insight()` + `select_insights()` (max 3, al più uno per coppia kind×squadra) + `MatchAnalysis.match_insights()`; card in `match.html` solo pre-partita. **Verifiche**: suite **67 passed** (+2); ruff F pulito sul nuovo codice; `fda build` 145 match; sui dati reali **77/77** future con insights hanno la card (ITA1 10 · ENG1 10 · ESP1 17 · GER1 9 · FRA1 9 · NED1 11 · POR1 11), 1–3 fatti (media 2,83), **0 leak inglesi**, 0 card nei post-partita. Esempio: Athletic–Elche «Elche: non vince da 6 partite». **Turno 2 — B5 SofaScore rimosso** (commit dedicato): cancellati client, test e voce yaml (codice mai collegato). Suite **65 passed** (67 − 2 test SofaScore). **Prossimo**: PR verso `main` → merge utente.

**Sessione precedente (dettaglio storico, `arena/01a08613`):** 2026-09-09 (pomeriggio ~13:30) — **sessione `arena/01a08613-football-deep-analyzer`** (da `main` `de89576` = merge PR #18). **Turno 1 — fix header CalcioMetro ri-applicato (era nel commit locale `8806236` della sessione chiusa, mai pushato → perso)**. Diagnosi della causa radice sul CSS di `main`: la riga dell'header riusa la classe `.bar`, e gli stili generici della barra-probabilità (`.bar{height:36px;overflow:hidden}` + `.bar .h{background:gradient}`) **sanguinavano** su di essa → logo/wordmark tagliati (content-box fissa 36px + overflow nascosto) e «riquadro verde» sul wordmark. Fix in `base.html`: `header .bar{padding:24px 0; min-height:84px; height:auto; overflow:visible}`, blocco protettivo post-barra-probabilità `.brand .h/.brand span/footer .fbrand ... {background:none; padding:0}`, `-webkit-tap-highlight-color:transparent` su `.brand` e `footer .fbrand` (coerente anche nel footer); commenti CSS che spiegano il perché. **Verifiche**: suite **65 passed**; `fda build` ok (145 match + 2364 fixture, 7 root); **verifica strutturale della cascata CSS su 12 dichiarazioni in conflitto** su HTML generato reale (header .bar vince su .bar generico; wordmark senza sfondo; barre-probabilità invariate: height 36px e gradiente intatti su tutte le 152 pagine); 0 occorrenze dei vecchi valori in `site/`. Rendering visivo non possibile dal sandbox (download Chromium bloccato: solo HEAD passa) → verifica visiva finale sulla preview Arena/live dopo il merge. **Periferiche verificate**: (1) **meteo Open-Meteo**: l'ultimo run `daily` in Actions (2026-09-09 11:38 UTC, commit dati `de89576`) ha il passo attivo — `source_status` con `openmeteo:ITA1..POR1`, `ok=True`, `requests=0` → nessun errore: 0 richieste perché il fallback scatta solo sui futuri senza meteo FotMob (verifica concordante: nessuna scheda futura sguarnita) — chiusa la «verifica dal vivo al primo daily»; (2) tabella `insights` oggi **1255 righe / 211 match** (cresciuta da 824/142 con i nuovi run), pronta per l'Obiettivo 2. **Prossimo**: PR verso `main` (questa sessione) → merge utente; poi Obiettivo 2: card insights in `match.html` (PR separata).

**Sessione precedente (dettaglio storico, `arena/01a082e0`):** 2026-09-09 (mattina ~11:15) — **sessione `arena/01a082e0-football-deep-analyzer`** (da `main` `723ad7f`). **Brand redesign «CalcioMetro» reso ufficiale** (dopo «mi va bene così, rendilo ufficiale»): design system completo nel template `base.html` (Calcio `#2ee59d` + Metro `#e7b23c`, fondo scuro non-nero `#0c1118`, un solo accento `#28c893`, logo **CM 3D ≥ 52px** in header/footer/favicon, brand nel title, `user-select:none`, selezione trasparente), info page, footer migliorato. **Verifiche**: suite **65 passed**; ruff **nessun nuovo errore** sul nuovo codice (`scripts/render_preview.py` pulito; `build.py` HEAD 2→0, `analysis.py` 4→4, `test_site.py` 1→1 invariati); `fda build` su dati reali → **145 pagine match + 2364 fixture**, 7 pagine root, **7 voci nav su tutte e 7 le pagine**, nessuna sezione rimossa (confronto HEAD vs working: tutti i file cresciuti/uguali, zero riduzioni; sezioni `h1/h2` invariate o arricchite — `match.html` +1 «Giocatori da tenere d'occhio»). **Anteprime ufficiali salvate in `docs/preview/`** (cartella **versionata**, non toccata da `fda build` che fa `rmtree(site/)`): `header-preview.svg`/**`home-preview.svg`** (**vettoriali fedeli = UFFICIALI**, con tutte le 7 voci nav) + `header-preview.png`/`home-preview.png` (raster di supporto, font di ripiego perché Sora non scaricabile da GitHub LFS). **Nota**: `site/` è gitignored e viene rigenerato dal build → le anteprime NON vanno messe lì (spariscono). **Prossimo**: PR verso `main` → merge utente (sempre l'utente, policy sez. D).

**SEGNATO PRIMA DEL MERGE (utente: «se devi appuntare qualcosa prima del merge fallo») — da fare nel prossimo turno, PR dedicata:**
- **🎯 PRIORITÀ — integrare gli `insights` nelle schede partita** ✅ **CHIUSA 2026-09-09** (sessione `arena/01a08630`): card «Fatti rilevanti», 77/77 future, 0 inglese. Dettaglio in cima a questo file. Piano originale (per traccia): (unico gap di profondità reale: dati già raccolti, mai mostrati). Stato verificato oggi: tabella `insights` **824 righe / 142 partite** (`team_id`, `player_id`, `text`, `match_id`), popolata in `fotmob.py` (parser con campo `facts["insights"]`) e salvata da `collect.py` via `store.upsert`. **NON è usata da nessun template** (grep `insights` in `analysis.py`/`templates/` → 0 occorrenze). I testi sono **in inglese** e **pro-squadra** (es. «Atalanta haven't lost to Roma in their last 8 meetings», «Have scored 5 goals in their last 5 matches»). **Come farlo bene** (requisito «giusto, accurato, preciso e di qualità»): (1) tradurre in italiano con mappa su pattern prevedibili (troveremo varianti: «haven't lost in N matches», «scored N goals in last N», «won their last N», «unbeaten in N», «failed to score», «clean sheet in N»…) con fallback che **non** mostra testo non tradotto; (2) **selezionare solo fatti oggettivi e rilevanti** (no hype/marketing), max ~3 per partita, con priorità a forma recente, streak, testa-a-testa e gol; (3) legare il fatto alla squadra corretta (`team_id`) mostrando es. «**Atalanta**: non ha perso contro la Roma negli ultimi 8 incontri»; (4) aggiungere la card in `match.html` (solo pre-partita, come «Giocatori da tenere d'occhio») e il metodo in `analysis.py` (es. `match_insights()`), filtrando `player_id` (insight su singolo giocatore → gestire con nome o scartare se non mappabile); (5) test dedicati (traduzione, selezione, squadra-mostrata, degradazione a lista vuota) + assert e2e; (6) **verifica su dati reali**: nessun testo inglese residuo, massimo 3 per scheda, distribuzione su tutte le 7 leghe.
- **Verifica dal vivo Open-Meteo + `future_days=7`** ✅ **CHIUSA 2026-09-09** (evidenza sul run Actions 11:38 UTC, commit dati `de89576`): `source_status` ha `openmeteo:ITA1..POR1` con `ok=True` e `requests=0` su tutte e 7 le leghe → il passo è **attivo e senza errori** dopo il merge PR #16; 0 richieste perché il fallback è progettato per scattare solo sui futuri senza meteo FotMob e oggi **tutti i 73 futuri ≤7 gg hanno già il meteo FotMob** (`match_info.weather_desc` valorizzato al 100%) → tabella `weather_forecast` a 0 righe è il comportamento corretto, non un guasto. Da riverificare solo se una scheda futura resterà senza meteo FotMob a T-48h.
- **Decidere B5 SofaScore**: client + test presenti ma mai collegato in `collect.py` (codice morto) → collegare o rimuovere.

**Turno 1 — verifica completa sito/progetto + audit prossimi passaggi** (richiesta utente «dimmi cosa c'è ancora da fare, naviga e verifica»). Navigato il sito generato (`fda build`: 141 pagine partita + 2364 fixture), ispezionato `src/fda/`, query su `data/processed/*.parquet`, `gh pr list`, ricerca web mirata. **Misurato**: suite **64 passed**; `ruff --select F` **pulito** (restano **216** `E501`/`E741`, baseline ~254); parità confermata **73/73** partite nei prossimi 7 gg con previsione su **tutte le 7 leghe**; audit **0 «mancante»** su 73 partite (490 presente + 459 atteso); `fotmob_standings` 132 righe; `season_sim` 132; Accuratezza **20 gare** su 6 leghe, RPS **0,2045** vs naive 0,2385 (Δ −0,034). **Risultato principale**: la direttiva «parità di profondità, nessuna lega di serie B» è **raggiunta e verificata**. Report completo salvato in **`docs/06_verifica_audit_2026-09-08.md`**. **Prossimi passi (per priorità)**: (1) 🔴 **verifica dal vivo Open-Meteo + `future_days=7`** al primo `daily` post-merge (la tabella `weather_forecast` **non esiste ancora** nei dati: il run in repo è del 20:31 UTC, precedente al merge PR #16 delle 20:33); (2) 🟠 **B5 SofaScore** (client + test ma mai collegato in `collect.py` → codice morto, decidere collegare/rimuovere), **M2 diffidati** (fattibile da `events` + soglie per lega, non implementati), **M3 schede giocatore** (dati pronti: `understat_players` 1954 con xG/xA, `player_stats` 127.880); (3) 🟡 igiene: tabella `insights` (824 righe) raccolta ma mai mostrata, formazioni con alcuni rating mancanti, pulizia ruff `E501/E741`, snellimento `STATO.md`. Nessuna decisione bloccante spetta all'utente.

**Sessione precedente (dettaglio storico, `arena/01a082c6`):** 2026-09-08 (sera ~23:00) — **sessione `arena/01a082c6-football-deep-analyzer`** (da `main` `ca5cd89` = merge PR #16). **Turno 1 — card post-partita «Migliori in campo» per squadra (avvio Fase 3, schede giocatore)**: la vecchia riga piatta (3 migliori assoluti, senza squadra né contesto) è sostituita da una **card dedicata** con il **top 3 di ciascuna squadra** per rating partita FotMob, arricchito con **gol, assist, minuti e rating di stagione** (tutti dati già raccolti e presenti nel repo: 134 partite finite, 3237 titolari con rating, `player_stats` con 67 chiavi). In `analysis.py`: `top_players()` riscritto (`{"home": [...], "away": [...]}`, esclusi i non scesi in campo — ruolo diverso da starter/sub, degrada a lista vuota senza rating, nessun dato inventato), `self.player_stats` caricato in `__init__`. Template `match.html`: card a due colonne al posto della riga piatta. Test: nuovo `test_top_players_per_team_with_goals` (split, merge gol/assist/minuti, unavailable esclusi, degradazione) + assert dedicati nell'e2e. **Verifiche (offline, su dati reali del repo)**: suite **64 passed** (era 63); ruff `--select F` **0 errori** su tutto il package; E501+E741 **254** (baseline 257 → nessuna nuova, 3 in meno); `fda build` su dati reali (141 pagine): **68/68 pagine post-partita con la card**, distribuite su **tutte le 7 leghe** (Serie A 10 · LaLiga 11 · Premier 10 · Bundesliga 9 · Ligue 1 9 · Eredivisie 10 · Liga Portugal 9); **0 «nan»** standalone (le occorrenze sono sottostringhe di parole reali: «segnano», «Fernandes»…); audit invariato **0 «mancante»** su 73 partite. Esempio reale Genoa–Como (1-4): Assane Diao 9,2 · 2 gol · 84' · stagione 7,29. **Prossimo**: PR verso `main` → merge utente; poi **verifica dal vivo Open-Meteo + `future_days=7`** al primo run `daily` post-merge.

**Sessione precedente (dettaglio storico, `arena/01a08295`):** 2026-09-08 (sera) — **sessione `arena/01a08295-football-deep-analyzer`** (da `main` `bd1f80e`). **Verifica completa sito/progetto + fix P0/P1** (richiesta utente «controlla stato fonti e poi inizia con i lavori»). **(1) Verifica** navigata e misurata, salvata in `docs/05_verifica_sito_progetto.md` (commit `5b0a151`): sito live OK su Pages, **73/73 previsioni sui prossimi 7 giorni su tutte le 7 leghe**, classifica FotMob **7/7** (132 righe), audit schede **0 «mancante»** su 73 partite, 5 run `daily` consecutivi verdi. **(2) Fix P0** (commit `55539ca`): riga arbitro intera («18 rigori (33 gare)» invece di «18.0 … (33.0 …)» in 70 pagine), recupero «45+1'» (non «45+1.0'», 92 occorrenze), attribuzione fonti corretta (rimossa Open-Meteo dal footer quando non implementato, classifica attribuita a FotMob), **ESPN standings 403 → AVVISO giallo** (non più ERRORE rosso) in «Stato fonti»: ora **14 OK + 7 AVVISO, 0 ERRORE**. **(3) Fix P1** (commit `7b1a42c`): **meteo previsionale Open-Meteo** (nuovo client `src/fda/sources/openmeteo.py` — gratis, senza chiave, previsioni orarie 16 gg, mappa WMO→italiano; passo in `collect_league` per i futuri entro **7 giorni** con coordinate stadio FotMob → tabella `weather_forecast`, **fallback solo se FotMob non ha ancora il meteo**, fonte dichiarata «(previsione Open-Meteo)»; `fda daily` ora raccoglie `future_days=7` così le coordinate coprono tutta la settimana «prossime»), **formazione probabile pulita** (titolari elencati anche tra gli indisponibili esclusi: Bella-Kotchap/Oulaï nel Venezia, Thorisson nel Telstar), **playoff Liga Portugal** (retrocessione POR1 **2→3**: 2 dirette + playoff della 16ª, con nota aggiornata in `stagione.html`). Suite **63 passed**, ruff `--select F` **0 errori** (E501/E741 restano baseline pre-esistente: 257). Verifiche offline su dati reali (`fda build`, 141 pagine): 0 residui «.0», 0 «+X.0'», formazioni corrette, nota Retro corretta. **Da verificare dal vivo in Actions dopo il merge**: meteo Open-Meteo (dal sandbox `api.open-meteo.com` è irraggiungibile; il parser è coperto da fixture offline) e l'aumento `future_days=7` del collect.

**Sessione precedente (dettaglio storico, `arena/01a08232`):** 2026-09-08 — **sessione `arena/01a08232-football-deep-analyzer`** (da `main` `8f44c05`). **Turno 1 — fix xPTS FotMob (obiettivo unico di sessione)**: in `season_xg()` (`src/fda/site/analysis.py`) il fallback FotMob (`if not self.info.empty`) ora calcola **xPTS e punti reali dalle partite finite**: per ogni gara finita della squadra le λ = xG di casa/trasferta (`home_xg`/`away_xg` in `match_info`) → P(V casa), P(N), P(V trasferta) con `scipy.stats.poisson`, xPTS casa = 3·P(V casa)+P(N) e xPTS trasferta = 3·P(V trasferta)+P(N); punti reali 3/1/0 dai gol reali. Nuovo metodo statico `_poisson_xpts(lh, la)` → `(xPTS casa, xPTS trasferta)`; nel return del fallback `"xpts": round(xpts_total, 1), "pts": pts_total` al posto di `None` (resta `None` — riga xPTS assente come prima — solo se mancano i gol reali). Import `numpy as np` e `from scipy.stats import poisson` in cima al file; scipy è già dipendenza di `penaltyblog` (presente in CI con `pip install -e ".[dev]"`), `pyproject.toml` non toccato. Nuovo test `test_season_xg_fotmob_calcola_xpts` (4 gare finite → `pts=7`, `xpts=5.9`; metodo statico verificato contro griglia Poisson indipendente su 7 coppie di λ, simmetria a λ uguali < 1e-6, degradazione `None` senza gol). Verifiche (offline, sandbox): suite **47 passed** (era 46); ruff `--select F,E` su `analysis.py` **76 = baseline 78 → 0 nuove** (solo E501 pre-esistenti; 2 in meno grazie ai refolw), `tests/test_site.py` invariato; `fda build` sui dati reali del repo (141 pagine): **nessun «nan»** (le 67 occorrenze della stringa sono sottostringhe di parole: «segnano», «Fernandes», «vicinanze»…), tutte le **94 righe «Stagione (FotMob…)» ora mostrano «· xPTS … vs … punti reali»** (prima mai: xpts era None) e il narrativo «rispetto agli xPTS» scatta su squadre NED1/POR1 su dati reali (es. Fortuna Sittard +6,4 · AZ Alkmaar +4,3 · FC Utrecht −3,8). Nota: **la «regola permanente sez. F» in `docs/00_regole_di_lavoro.md` non esiste** (verificato: il file finisce alla sezione E sia su questo branch sia su `origin/main`) → non toccata, da chiarire con l'utente. Stato: commit `d9d1258` pushato, **PR #14 aperta verso `main`** (in attesa di merge utente secondo policy sez. D). **Turno 2 — sezione F aggiunta (richiesta utente prima del merge)**: `docs/00_regole_di_lavoro.md` ora contiene «## F. Parità di qualità delle schede partita (direttiva permanente)»: principio delle 7 leghe alla pari, tabella dei **12 contenuti obbligatori pre-partita** (previsione modello, forma recente, xG stagione, xPTS vs punti reali, PPDA, confronto di stagione, indisponibili, formazione probabile, riposo, arbitro, meteo, precedenti H2H) con fonte primaria e fallback, tabella dei **6 contenuti obbligatori post-partita** (lettura partita, statistiche chiave, cronaca, tiri e occasioni, cartina tiri SVG, momentum) e **checklist obbligatoria pre-PR** (`fda build` senza errori, audit automatico, confronto big-5 vs NED1/POR1, zero nan, test verdi + ruff pulito). **Turno 3 — regola permanente sull'ordine di lavoro (richiesta utente prima del merge)**: in sezione D di `00_regole_di_lavoro.md` aggiunto il blocco «Verifica dell'ordine di lavoro — PRIMA del merge»: prima della frase fissa di merge l'agente deve verificare in STATO/briefing/richieste utente se c'è altro lavoro importante da aggiungere al progetto che deve entrare su `main` nello stesso giro e, se c'è, farlo subito nella PR aperta — dopo il merge richiederebbe una nuova sessione (nuovo branch + nuova PR). Vale per ogni sessione futura.

**Sessione precedente (dettaglio storico, `arena/01a08176`):** 2026-09-08 (**sessione `arena/01a08176-football-deep-analyzer`**, da `main` `5ef4290` = post-merge PR #10 + commit dati run 14:32. **Turno 1 — hotfix ricostruito**: il commit `394759d` («hotfix max_backfill + pipefail + test», 41 passed) della sessione precedente non fu mai pushato ed è **perso col sandbox** — verificato: assente da tutti i ref locali, remoti e `refs/pull/*`, clone fresco con reflog solo clone+checkout, `fsck` senza oggetti pendenti. **Diagnosi con prove sui dati del run**: il run `daily` dispatch `34238795120` (14:31 UTC, head `02eda11`, primo dopo il merge PR #10) è FALLITO allo step «Prepara il sito per Pages» (exit 2, `deploy` saltato) perché il collect è morto su NED1 nel nuovo backfill 2b: `source_status` ferma a FRA1 14:32:29 (nessuna riga NED1/POR1), `fotmob_standings` solo big-5 (96 righe), 0 partite storiche scaricate, 12 partite NED1 in `match_info` tutte della finestra. Root cause: in `_fetch()` (`collect.py`) `bundle_to_dicts` + `store.upsert` stavano **fuori da `_safe`** → una partita storica anomala uccideva l'intero run; aggravante: pipe `fda daily | tee | tail` **senza `pipefail`** → step «Run giornaliero» ✅ falso e commit dati comunque eseguito. Verifiche locali sui dati del run (`5ef4290`): `fda build` riesce (141 pagine → il problema era nel collect, non nel build), suite **40 passed** (= main pre-hotfix, coerente col «41» = 40+test nuovo). **Fix**: `_save()` dentro `_safe` come `fotmob save <id>` (partita guasta isolata e visibile in `source_status`, il run prosegue) + `set -euo pipefail` nello step «Run giornaliero» di `daily.yml` + test di regressione offline `test_collect_survives_bad_match_save` (crash simulato sul salvataggio di una partita: il run completa, l'altra partita è salvata). **Turno 2 — audit + card «Confronto di stagione»**: audit misurato sulle **86 gare da giocare entro 10 gg** (dati `5ef4290`): classifica presente 100% big-5 / **0% NED1-POR1** (run morto prima del passo 2c → si sblocca col solo merge hotfix + 1 run); arbitro e meteo **0% su tutte** (FotMob li pubblica a T-24/48 h → segnaposto onesti); xG3 via partite FotMob 0% ovunque (big-5 coperte da Understat; NED1/POR1 dal backfill post-hotfix); formazioni: ENG1 0% (pubblicate a ridosso), altre leghe 5-23%. **Nuova card pre-partita «Confronto di stagione»** su tutte le 7 leghe: tabella lato-a-lato (posizione, punti, punti/gara, V-N-P, GF/GC per gara, DR) + righe derivate **Attacco/Difesa × media campionato** (dal GF e GC per gara della lega intera, stessa tabella) con il valore migliore evidenziato; degrada con «—» se una squadra non è in tabella, scompare se nessuna delle due; sostituisce la vecchia riga «Classifica: X° con Y punti». Refactor `h2h` in `_h2h_core`/`h2h_list`/`h2h_stats` + nuova sintesi sui precedenti (**gol/gara e % entrambe a segno** sui 5 mostrati) + segnaposto onesti «Arbitro da definire» / «previsione pubblicata a ridosso della gara» quando mancano in pre-partita. Suite **42 passed** (+`test_season_compare`), ruff 0 nuove (confronto senza numeri di riga: output identico). Verificato anche su dati reali (`fda build`, es. Venezia–Fiorentina: attacco 0,49× vs 0,24× media lega). Nota tool confermata: 3 edit fantasma («success» senza applicazione) — riverifica con grep sempre. **Turno 3 — Monte Carlo di stagione (fase 2 roadmap)**: nuovo modulo `models/season_sim.py` — per lega: fit DC+Elo su storico datahub+correnti (helper `build_hist` estratto e riusato da `fda predict`, fonte unica), probabilità 1X2 per ogni gara restante (ensemble 70/30), poi N stagioni simulate campionando i **punti dalla griglia DC completa** (ρ compreso → tie-break su differenza reti simulata); squadre senza storico DC (neopromosse) simulate con λ neutre di lega (nota esplicita sul sito). Output tabella `season_sim` (chiave lega+squadra): punti attesi, media posizione, P titolo / top-4 / retro (regole per lega: POR1=2, NED1=3 con playoff, altre=3). Nuovo comando `fda simulate [--sims]` + integrato in `fda daily` tra predict e build (fallimento isolato, il sito esce comunque); pagina **`stagione.html` «Proiezioni»** (nav aggiornata; tabella per lega: PG, Pt, Pt attesi, media pos., % 1°, % Top-4, % Retro, virgole decimali; segnaposto onesto finché `season_sim` è vuota). Verifiche: **44 passed** (+2 test simulazione: invarianti somme 1/4/3 e determinismo a seed fisso; squadra-neopromossa senza storico); ruff **0 nuove** (season_sim e test puliti; noqa mirati B008/BLE001 su cli, pattern pre-esistente). Da sandbox: `fda simulate` degrada pulito senza mirror («storico insufficiente», saltata per lega); **primo giro reale in Actions dopo il merge** (stima runtime: fit ~1s/lega + sampling vettorizzato ~10k×300 gare ≈ pochi secondi/lega). Prossimo: Prossimo: merge utente PR #11 → dispatch ×2 (backfill NED1/POR1 ~69 gare a 40/run) → `scripts/verify_standings.py --ref origin/main`; poi candidata: **meteo previsionale Open-Meteo** (fonte gratuita senza chiave, previsioni ~16 gg: riempirebbe il vuoto meteo 0% su TUTTE le future; richiede client nuovo + verifica live in Actions). Sessione precedente (`arena/01a08157`, PR #10): (nuova sessione su branch `arena/01a08157-football-deep-analyzer`, da `main` `3a4303f` = merge PR #9; `main` allineato a `origin/main`, worktree pulito. **Turno 1 — setup + diagnosi**: ambiente installato (`pip install -e ".[dev]"`, PyPI ok), suite **29 passed**, ruff **47 segnalazioni = baseline pre-esistente** (nessuna nuova). Diagnosi parità schede **rimisurata sui dati locali**: nessun file `espn_*` (classifica assente ovunque), Understat solo 5 leghe a 2,0–4,1 gare/squadra, fallback FotMob con 67/230 finite (→ ~1 gara su NED1/POR1). Creato `docs/04_parita_schede.md` con disegno Fix A (classifica da endpoint FotMob `leagues` → nuova tabella `fotmob_standings`) e Fix B (xG stagione NED1/POR1: opzione risposta `leagues` o matchDetails di tutte le finite). Prossimo: fixture + parser + test offline (Fix A). **Turno 2 — mappatura endpoint `leagues`**: da `pseudo-r/Public-FotMob-API` (VERIFIED) + `tommhe14/fotmob-wrapper` via api.github.com — risposta con `details`, `table[].data.table.all[]` e righe `{idx,name,id,played,wins,draws,losses,scoresStr,goalConDiff,pts}`; il wrapper usa `/data/leagues` (stesso base_url nostro, senza x-mas) e `/data/tltable` come endpoint dedicato (fallback candidato, struttura ignota). Divergenza lista-vs-dict sul nodo `table` → parser difensivo. Nessun xG di squadra nella risposta → Fix B = opzione 2 (matchDetails di tutte le finite nelle leghe senza Understat, endpoint già verificati). Tutto annotato in `docs/04_parita_schede.md`; formato reale da confermare in Actions. **Turno 3 — parser tabella FotMob**: `FotMobStandingRow` + `parse_league_table()` in `fotmob.py` (gestisce nodo `table` lista e dict, `scoresStr`→gol, fallback differenza reti, righe senza id scartate, ordinamento per `idx`; `league_raw()` con `season` opzionale) + 2 fixture campione + 2 test. Suite **31 passed**, ruff: 0 nuove segnalazioni sui file toccati (6 pre-esistenti invariate). Prossimo: merge utente della PR parità-schede → dispatch manuale `daily` dalla UI → verifica dati live. **Turno 4 — Fix A integrato**: passo 2b in `collect_league` (tabella `fotmob_standings`, chiave lega+squadra, errore esplicito se tabella vuota/inattesa così resta visibile in `source_status`; TTL `leagues: 6` in config), `analysis.standing()` FotMob-prima/ESPN-riserva (template invariato), nuovo comando `fda fotmob-table` per debug dal PC. Test: collect offline con tabella (Inter 1°/9pt), isolamento guasto `leagues`, preferenza FotMob + fallback ESPN in `standing`. Suite **33 passed**, ruff **47 = baseline esatta** (0 nuove; nota: `typer.Argument("lit")` non attiva B008, solo la forma `None` — tolto `noqa` superfluo). Lezione tool: un edit su due in parallelo a volte non si applica nonostante «success» — ora riverifico sempre con grep. **Turno 5 — Fix B integrato**: passo 2b in `collect_league` (backfill finite fuori finestra solo per leghe senza Understat, recenti prima, tetto `--max-backfill=40`/run, fetch estratto in `_fetch()`); contatore `matches_backfilled` + display «N+M storiche» in `fda collect`. Test: NED1 1+1 recuperi (0 al rerun), ITA1 invariata, `season_xg()` FotMob 3,5/1,5 su 2 finite. Suite **36 passed**, ruff **47 = baseline**. Recuperato anche l'edit §3 di `docs/04` perso al turno 2 (riverifica sistematica). Fix A+B completi offline; resta la verifica dal vivo in Actions. **Turno 6 — PR**: assert e2e rendering «Classifica: 1° con 9 punti» (36 passed, ruff 47 = baseline), workflow `tests` verde sul branch, diff pulito (13 file, no dati); **PR #10 aperta** verso `main` (check `test` SUCCESS, CLEAN — merge all'utente). **Turno 7 — in parallelo al merge utente**: script `scripts/verify_standings.py` (tabelle 7/7 leghe con controlli rank/punti/giocate, copertura xG finite per lega con soglia backfill NED1/POR1, `source_status` FotMob; exit 1 se FAIL) + 4 test funzioni pure. Suite **40 passed**, file nuovi ruff-puliti. Script già provato su `origin/main`: riporta correttamente «parquet assente» pre-merge. Nota: in `verify_ned_por.py` resta un RUF100 pre-esistente (noqa S603/S607, regole non abilitate) — da pulire nel turno ruff.)

## Fatto
- [x] **Audit pre-merge delle schede in programma (2026-09-13, quinto giro, `arena/01a09a79`, PR #27)** — misurate 95 schede pre-partita su 7 leghe: **parità di sezioni al 100%** (Analisi pre-partita, Scontro tattico, I giocatori che decidono, Contesto, Confronto di stagione, Fatti rilevanti), blocchi del modello 93/95, audit per campo **0 «mancante»** dopo 4 correzioni (previsione non più classificata «attesa dalla fonte»; distinta senza assenze = presente e non buco; la scheda **dice** «nessun indisponibile segnalato» invece di tacere, solo pre-partita; footer «Prossime partite e calendario»). Diagnosi con dati alla mano: le 2 schede senza modello sono neopromosse senza storico nel corpus surrogato del sandbox, e «Come nasce questa probabilità» manca su 70/95 solo perché le 138 previsioni committate sono `dc-elo-ens-0.1/-0.2` (25/25 con `-0.3` ce l'hanno) → si risolve da solo al primo `fda predict` post-merge. Copertura schede sulle partite in programma **4,6%** (Tappa 2 = decisione utente). Suite **172 passed**, `verify_site` **0 problemi · 11.793 controlli**. Tabella di parità e misure: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9.6.
- [x] **Tappa 1 — orizzonte esteso a tutto il calendario (2026-09-13, quarto giro, `arena/01a09a79`)** — `predictions` a una riga per partita (`latest_per_match` + migrazione 3.210 → 138 dichiarata a schermo), `fda predict --days-ahead 0` (tutto il calendario, zero richieste in più), pagina «Prossime» con «Tutto il calendario»: 1.987 righe compatte raggruppate per mese (1X2 / gol attesi / Over 2,5, preferito in grassetto e a colore, link solo se la scheda esiste, «senza previsione» quando manca). Copertura **6,6% → 96%** (138 → 2.000 partite su 2.083), **1,6 kB → 0,27 kB per partita coperta**, 29,7 ms per previsione, `prossime.html` 274 kB → 1.102 kB (**83 kB gzip**), `build_indexes` 2,4 s, suite **169 passed** (+6), `verify_site` **0 problemi · 11.793 controlli** col nuovo **[11]** (9.643 controlli sul calendario). Misure e criteri: [`docs/13`](13_modelli_e_schede_piano_2026-09-13.md) §9. Tappa 2 rinviata (§9.5).
- [x] **Auto-audit accuratezza (2026-09-13, terzo giro, `arena/01a09a79`)** — calibrazione v1.1 a momenti su finestra 730 giorni (λ×0,9135: bias λ fuori campione +0,264→+0,053, Brier mercati 0,20646→0,20516; sul campione pieno bias +0,245→−0,024 e pareggio 23,9%→26,2% su osservato 25,6%), limiti di sicurezza sulle λ invertite (nessuna scheda da 8,4 gol attesi: 69 gare toccate, 1,19%, RPS invariato e mercati migliori proprio dove il modello esagerava), laboratorio reso equo (miscela non più appiattita sul DC, livello dei gol corretto per ogni candidato con inclinazione esponenziale), candidati 15→22 (`dc_elo_tilt`, pesi 0,50/0,85, iperparametri Elo), `GRID_SIZE` unica in `dc_grid.py`, 3 difetti nei verificatori e anteprima che riapplica i limiti. Suite **163 passed**, `verify_site.py` **0 problemi · 2.213 controlli numerici**. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Modelli + schede (2026-09-13, `arena/01a09a79`)** — calibrazione della griglia (λ×0,94, Δρ−0,04: bias gol +0,245→+0,058, pareggio 23,9%→25,7%, Brier mercati walk-forward 0,2065→0,2055), laboratorio walk-forward a 15 candidati con IC appaiati (`fda lab`, workflow settimanale), storico di allenamento salvato a ogni run (`history.parquet`), distribuzione dei gol totali + dotplot quantile e scomposizione della probabilità in scheda, controlli [9]/[10] in `verify_site.py`, suite a 150 test. Dettagli e criteri di accettazione: `docs/13_modelli_e_schede_piano_2026-09-13.md`.
- [x] **Processo/handoff — policy «merge lo esegue SEMPRE l'utente»** (decisione utente 2026-09-08, riapplicata in questa sessione perché il commit originale `9d39a21` non era mai stato pushato): sez. D di `00_regole_di_lavoro.md` riscritta (dopo la frase fissa l'agente si ferma e aspetta, non mergea mai), avviso ⚠️ in cima a `BRIEFING_NUOVA_SESSIONE.md`, decisione registrata in «Decisioni aperte». Doc-only; test 23 passed, nessuna nuova segnalazione ruff (baseline: vedi Nota). **PR #9 aperta verso `main`**: check `test` **Success** (49s), mergeable **CLEAN** — in attesa del merge dell'utente (secondo la nuova policy).
- [x] **Processo/handoff — paletti di qualità + briefing nuove sessioni** (fatto, **PR #6 mergiata in `main` `16db091`**): riscritta la sezione B (paletti di qualità: accuratezza/precisione/profondità/qualità), aggiunta la sezione D (policy esplicita su **quando fare Create PR e quando Merge**, con frasi fisse e checklist), e creato `docs/BRIEFING_NUOVA_SESSIONE.md` (porta d'ingresso con architettura, stato, prossimi passi, mappa file e limitazioni, così un agente nuovo non ripete verifiche già fatte). Doc-only, check tests pass (42s).
- [x] Studio di fattibilità e catalogo fonti (`01`, `02`) — commit `ac35f2b`
- [x] Decisioni utente registrate (7 campionati, automazione giornaliera, uso personale, quote sì, solo fonti GitHub Actions) — `03_decisioni_e_funzionamento.md`
- [x] Regole anti-blocco chat — `00_regole_di_lavoro.md`
- [x] Verifiche tecniche: penaltyblog OK (nota pandas 3), datahub CSV OK, FotMob id 7 leghe OK, matchDetails pre-partita ricco (formazione probabile, meteo, arbitro)
- [x] **Passo 1 — scaffolding**: `pyproject.toml`, `src/fda/` (config, http client con cache+rate limit+retry, cli), `config/leagues.yaml` (7 leghe + coppe), `config/sources.yaml` (budget richieste), `tests/` (6 test verdi), `.gitignore`, `data/README.md`. Comandi: `fda leagues`, `fda version`.

- [x] **Passo 2 — client FotMob** (`src/fda/sources/fotmob.py`): `fixtures`, `matchDetails`, `teams`, `playerData`, `matches?date`; parser → record piatti (MatchInfo con arbitro/stadio/meteo/xG/H2H, Shot, TeamMatchStat per periodo, PlayerMatchStat, LineupPlayer con indisponibili+rientro, eventi, momentum). Test su JSON campione (9 verdi). Comandi `fda fotmob-fixtures ITA1`, `fda fotmob-match <id>`.

- [x] **Passo 3 — Understat + ESPN**: `sources/understat.py` (endpoint JSON con cookie+XHR, senza soccerdata/seleniumbase; partite con xG, righe squadra-partita con xPTS/PPDA/deep, giocatori; `team_season_table`) e `sources/espn.py` (scoreboard con statistiche, classifiche, boxscore, arbitri). 11 test verdi. Comandi `fda understat-table ITA1`, `fda espn-today ITA1`.

- [x] **Passo 4 — storage + collect**: `store.py` (Parquet versionati in `data/processed/` + viste DuckDB, upsert per chiave, tipi UTC stabili) e `collect.py` (per lega: calendario → dettagli partite oggi ±3 gg, finite una sola volta, future a ogni run → Understat → ESPN; fonti isolate, esiti in `source_status`). 13 test verdi (incluso un run completo offline). Comandi `fda collect [ITA1 ...]`, `fda db [SQL]`.

- [x] **Passo 5 — modelli**: `sources/history.py` (CSV storici datahub/football-data → schema unico, quote di chiusura se presenti), `teams.py` (nomi canonici tra fonti, ~300 alias per le 7 leghe), `models/predict.py` (Dixon-Coles pesato nel tempo + Elo + ensemble 70/30 con griglia coerente; mercati 1X2, risultati esatti, O/U, BTTS, doppia chance, clean sheet; RPS). Backtest onesto Serie A 2025/26 (train 300 → test 80): RPS ensemble 0,212 vs Elo 0,218 vs naive 0,244. 18 test verdi. Comando `fda predict [ITA1 ...]`.

- [x] **Passo 6 — sito + automazione**: `site/analysis.py` (contesto analitico e frasi in italiano da regole esplicite), template Jinja2 (Oggi, Prossime, Risultati, pagina partita pre/post, Accuratezza con RPS/Brier, Stato fonti), `site/build.py`, comandi `fda build` e `fda daily` (collect → predict → build). Workflow `.github/workflows/daily.yml` (cron 5 volte al giorno + manuale, commit dei Parquet, deploy Pages) e `tests.yml`. 19 test verdi.
- [x] **Passo 6b — primo run dal vivo**: su `main`, run `daily` #2 (2026-09-06, come da handoff) concluso **Success** in 3m17s: collettori → modelli → build sito → commit dati → deploy GitHub Pages. Artefatto `github-pages` di 132 KB; Pages usa source = **GitHub Actions**. Restano solo gli avvisi non bloccanti sulla deprecazione Node.js 20.
- [x] **Correzione PR #2**: merge su `main` nel commit `968f2d7`; lo step di installazione di `daily.yml` usa `pip install -e ".[dev]"`, così `pytest` è disponibile nel workflow.
- [x] **PR #3 mergiata su `main`** nel commit `3359fdc` (workflow con versioni Node 24).
- [x] **Run `daily` #3 su `main` verificato** (id `33999756428`, head `3359fdc`): job `run` **Success** 3m08s + `deploy` **Success** 10s; commit dati `43b848c` pushato; artefatti `github-pages` (135358 B) e `run-log-3`; deployment Pages riuscito. `predictions` 48 → **96** righe (ITA1 20, ENG1 18, ESP1 22, GER1 18, FRA1 18), probabilità coerenti e senza null, tutte gare `scheduled`. FotMob ok 7/7; Understat per le 5 leghe coperte; ESPN standings ancora **403 isolato** (non blocca). Suite locale 20 passed. (Blob di log Actions e pagina Pages non raggiungibili dal sandbox — verifica su metadati/commit/dati.)

## In corso
- [x] **Passo 7a — resilienza del run**: ESPN standings e scoreboard isolati; `fda predict` isola errori/storici per lega. Commit `d8063c1` + `0bcde50`; suite locale 20 passed; workflow `tests`: Success.
- **Passo 7b — storico NED1/POR1 (avviato)**: il mirror `datasets/football-datasets` non copre Eredivisie/Liga Portugal e `football-data.co.uk` è irraggiungibile dagli IP cloud (root cause: nei run #2/#3 NED1/POR1 avevano partite in programma ma 0 predizioni). Risolto puntando NED1 (`eredivisie`) e POR1 (`primeira-liga`) a un mirror `raw.githubusercontent.com` dedicato con la stessa struttura `season-XXXX.csv` (campo per-lega `datahub_base`), + alias squadra alle grafie N1/P1 (`For Sittard`→`Fortuna Sittard`, `Estrela`→`Estrela da Amadora`). Commit `85b515f`.
- **Verifica passo 7b**: suite locale **23 passed** (3 nuovi test); simulazione offline con CSV reali del mirror + calendario FotMob → NED1 **11** e POR1 **9** predizioni (tutte le partite in programma; le promosse ADO Den Haag/Cambuur/Académico Viseu/Marítimo ricevono prior dagli esiti di stagione in corso via FotMob). Workflow `tests` sul branch e check PR: **Success**.
- **PR #4 mergiata su `main`** nel commit `a3c5bf7` (merge eseguito dall'agente, precedente PR #2; tests verdi sul branch e mergeable CLEAN). `main` ora contiene lo storico NED1/POR1 da mirror dedicato.
- **Accuratezza live con gare reali (confermato)**: build del sito sui dati `main` (commit `783e59a`, 2364 fixture) → la pagina `accuratezza.html` ora mostra valutazioni reali pre-partita: **19 partite valutate** su 5 leghe, RPS **0,205** complessivo vs naive 0,244 (Bundesliga 2, LaLiga 6, Ligue 1 3, Premier 2, Serie A 6). NED1/POR1 ancora assenti perché i dati su `main` precedono il merge.
- **✅ Verifica live NED1/POR1 CONFERMATA (2026-09-08 13:33–13:40 UTC, verificato dal vivo)**: dopo l'attesa dei cron dilazionati (slot 10:00 mai partito), l'utente ha avviato manualmente il run `daily` dalla UI GitHub (Actions → daily → Run workflow su `main`): run **`34232722943`** (workflow_dispatch, 13:33:43Z, head `91f7109` post-merge) → job `run` **Success** in 1m33s (tutti gli step: test rapidi, collect→predict→build, commit dati, preparazione Pages) + job `deploy` **Success**. Commit dati **`6a31d39`** (13:35 UTC) verificato con `scripts/verify_ned_por.py`: **NED1 = 11 predizioni** (esattamente le ~11 simulate offline) e **POR1 = 11** (vs 9 simulate: da allora il calendario FotMob ha datato più gare di Liga Portugal, copertura maggiore), tutte con probabilità complete e `n_train=960` → **lo storico del mirror dedicato è stato scaricato e usato** (prima del merge: 0 predizioni per assenza di storico — le predizioni stesse sono la prova, più forte del log). Predizioni sensate (es. PSV 0,83 vs Sparta, Benfica 0,70 a Moreirense 09/09, Porto 0,73 a Casa Pia) e **alias mirror funzionanti** («Fortuna Sittard», «Estrela da Amadora»). `source_status`: FotMob OK 7/7, Understat OK (incl. NED1/POR1), unico errore il noto **ESPN standings 403** isolato (pre-esistente, non blocca). Limitazione: artifact/blob dei log Actions non raggiungibili dal sandbox (Azure) — verifica fatta su dati del commit, come per i run precedenti. Inclusa la gara NED1 di oggi NEC–Excelsior (16:45) → le prime valutazioni reali NED1/POR1 arriveranno in Accuratezza tra 1–6 giorni. Verificato sui dati live (`fixtures.parquet`, ultimo run): il calendario FotMob ha già gare NED1 **oggi** (12:00 UTC+, es. FC Utrecht–Go Ahead Eagles, NEC Nijmegen–Excelsior) e POR1 **da domani** (es. Moreirense–Benfica 19:45 UTC) → il primo `daily` post-merge avrà partite NED1/POR1 da predire: **la conferma dovrebbe arrivare entro poche ore/giorno, non serve altro de-risking offline** (simulazione già fatta: NED1 11 / POR1 9). ESPN standings: 403 isolato anche su NED1/POR1 nell'ultimo run (non blocca).
- **Dopo la conferma live (ora in corso)**: report pre/post in italiano già rifiniti su branch (PR #9: Accuratezza Δ/calibrazione, cartina tiri, momentum, precedenti, italianizzazione) — resta da seguire la risoluzione delle gare previste (incluse le 22 NED1/POR1 di questa settimana) che alimenterà Accuratezza con le prime valutazioni su Eredivisie/Liga Portugal.
- **Rifinitura Accuratezza + fix report partita (su branch, in PR #9 — verificato offline con build locale sui dati del repo)**: pagina `accuratezza.html` rinnovata: colonna **Δ vs naive** (su 19 gare il modello batte la base in ogni lega: Tutti **−0,038**; da −0,025 Serie A a −0,064 Premier), nuova card **Calibrazione** (prob. media prevista vs frequenza osservata: 1 = 42,4% vs 21,1% · X = 24,9% vs 36,8% · 2 = 32,6% vs 42,1% → nel campione attuale il modello sovrastima le vittorie in casa; n=19, dato indicativo) e per singola gara colonna **Top (✓/✗)** + **RPS**. Fix nei report partita (`match.html`): data in italiano con ora locale («domenica 06/09/2026, 15:30 (ora italiana)» — prima weekday inglese + etichetta «UTC» fuorviante) e spettatori formattati «57.000» (prima «57.000.0»); nuovi filtri Jinja `it_dt`/`it_num` in `build.py`. Suite **23 passed** (nuovi assert: Δ, Calibrazione, ✓, RPS 0,087 della previsione 62/21/17, data italiana, spettatori); **ruff: zero nuove segnalazioni** vs baseline (confronto diretto prima/dopo sui file toccati). Note: la pagina live su Pages si aggiornerà al prossimo `daily` su `main`.
- **Cartina dei tiri nei report post-partita (su branch, in PR #9 — verificato offline)**: nuova card «Cartina dei tiri» con due mezzi campi SVG (uno per squadra, porta a destra), pallini dimensionati sull'xG del tiro, colori per esito (verde gol / blu in porta / arancione tratteggiato bloccato / grigio fuori), tooltip con giocatore·minuto·xG e riepilogo «17 tiri · xG 3,06 · 5 gol». Dati: tabella `shots` (coordinate FotMob in metri 105×68, verificate: x max 104,3 / y mediana 34 = centro porta). `analysis.py`: `shot_map()` (trasformazione metri→px con clamp) + `goals` in `shot_summary`; template puro Jinja, nessun JS. Verifiche: **23 passed** (assert: 2 SVG, «xG 0,88» di Lautaro col decimale italiano, pannello vuoto Monza), build locale: **132/132 SVG ben formati** (parse XML), coordinate dei 5 gol di Mainz tutte dentro l'area e coerenti con l'xG (Mwene 0,89 a 6 m dalla porta); rendering PNG non possibile dal sandbox (niente cairo) — la verifica è strutturale/numerica, il primo `daily` su `main` darà la conferma visiva su Pages.
- **Italianizzazione completa del sito (su branch, in PR #9 — verificato offline con audit automatico su 146 pagine)**: richiamo utente «il sito è italiano e usiamo l'ora italiana». (1) **Ora italiana ovunque**: eliminati i 3 residui «UTC» (header di ogni pagina, timestamp della previsione, footer) → nuovo filtro `it_utc` (conversione UTC→Europe/Rome, gestisce anche ora solare/legale); orari delle liste già convertiti, ora dichiarati «(ora italiana)». (2) **Decimali con virgola**: nuovo filtro `dec` applicato a tutte le ~30 formattazioni dei template (xG, quote, gol attesi, Elo/DC, RPS/Brier/Δ in Accuratezza, λ nelle liste, rating) e `_f()` del narrativo («4,1 ammonizioni», «3,12 gol attesi»); fix «10.0 rigori in 24.0 gare» → interi. (3) **Meteo tradotto**: mappa `_WEATHER_IT` con gestione varianti combinate FotMob «Partly Cloudy/Wind» → «parzialmente nuvoloso e ventoso» (valori reali verificati sui dati: Mostly Clear, Fair, Showers in the Vicinity…). (4) **Date di rientro tradotte**: `_return_it()` («Mid October 2026» → «metà ottobre 2026»; catalogo completo dei valori FotMob su 437 indisponibili: Day to day, Doubtful, About 1-2 weeks, Out for season…). Verifiche: **29 passed** (6 nuovi unit test `test_i18n.py` su filtri e traduttori, inclusa conversione ora legale +2 / solare +1); **ruff 0 nuove segnalazioni**; audit automatico su tutte le 146 pagine generate: **0 residui** di UTC / weekday inglesi / meteo inglese / rientri inglesi / decimali con punto.
- **Momentum + ultimi precedenti nei report (su branch, in PR #9 — verificato offline)**: (1) nuova card **«Momentum della partita»** (solo post-partita): grafico SVG a barre minuto per minuto (verde = pressione casa, rosso = trasferta, scala FotMob ±100) con **marker dei gol** presi dalla cronaca e frase di sintesi quando una squadra domina («Momentum a favore di Mainz nel 66% dei minuti» su Hamburger 0-5 Mainz — coerente col risultato); dati: tabella `momentum` già raccolta (6134 righe, 67 partite), orientamento verificato empiricamente (valori fortemente negativi ai 5 gol di Mainz in trasferta). (2) **«Ultimi precedenti»** nel card Contesto (pre e post): fino a 5 incontri reali da tabella `h2h` (1554 righe dal 2009) con data, risultato, campionato e pill V/N/P **dal punto di vista della casa attuale** — bug di inversione V/P trovato e corretto in verifica (Roma–Atalanta: tutte P come atteso), poi inchiodato da test con riga non-pari (il campione aveva solo un 1-1); nomi risolti dalle due squadre in campo (le righe h2h coinvolgono solo loro), righe anomale scartate. Verifiche: **29 passed**; build locale: **198/198 SVG ben formati** (132 cartine + 66 momentum); ruff 0 nuove; audit italiano 0 residui. Nota: **diffidati non implementati** — fonte FotMob non li espone nei dati raccolti (ruoli lineup: starter/sub/unavailable/coach); da valutare fonte alternativa in futuro.

## Nota
- **Preview del sito dal sandbox** (scoperta 2026-09-08): dopo `fda build` (che scrive in `site/`, gitignored), si può servire con `python3 -m http.server 3000 --bind 0.0.0.0 --directory site` via start_process → l'utente vede il sito modificato nel browser (preview Arena) prima del merge. Il sito ufficiale è invece `uamisjd.github.io/football-deep-analyzer` (Pages), aggiornato solo dai run `daily` su `main`.
- Dal sandbox dell'agente la rete verso FotMob, `football-data.co.uk` e `raw.githubusercontent.com` può essere bloccata (si raggiungono solo `github.com` e `api.github.com`): le verifiche di rete vanno fatte in GitHub Actions o sul PC. I parser sono coperti dai test offline. (2026-09-08: PyPI risulta invece raggiungibile dal sandbox, quindi l'installazione dell'ambiente di test locale funziona.)
- Il mirror dedicato NED1/POR1 è configurato solo in `config/leagues.yaml` (campo per-lega `datahub_base`): facile da cambiare se il mirror diventasse irraggiungibile.
- Le quote The Odds API sono opzionali: vengono usate solo se `ODDS_API_KEY` è configurata.
- Baseline ruff (rilevata 2026-09-08, ruff 0.16.6 con vincolo `ruff>=0.5`): **47 segnalazioni pre-esistenti su `main`** (B008 `typer.Argument`, UP, RUF, BLE, I) — nessuna introdotta da questo turno (modifiche doc-only); la CI `tests` esegue solo pytest, quindi non blocca. Da valutare un turno di pulizia dedicato (la regola B.4 richiede ruff pulito sul codice **nuovo**).

## Prossimo passo (Fase 0/7 — un turno per riga)
1. ~~Scaffolding repo~~ ✅
2. ~~Client FotMob~~ ✅
3. ~~Client ESPN + Understat~~ ✅
4. ~~Storage + collect~~ ✅
5. ~~Modelli DC + Elo~~ ✅
6. ~~Sito statico + workflow + Pages~~ ✅
6b. ~~Primo run dal vivo in Actions e correzioni ai collettori~~ ✅
7. ~~Upgrade Node 24~~ ✅
7b. ~~storico NED1/POR1~~ ✅ **confermato dal vivo** (run `34232722943` del 2026-09-08 13:33Z, NED1 11 / POR1 11 predizioni, `n_train=960`, commit dati `6a31d39`). Prossimo: seguire la risoluzione delle gare previste (prime valutazioni NED1/POR1 in Accuratezza) e proseguire la roadmap.
7c. ~~Processo/handoff~~ ✅ (PR #6 in `main` `16db091`). Conferma live NED1/POR1 ✅ (2026-09-08). Prossimo: **Monte Carlo stagione** (fase 2: probabilità titolo/top-4/retrocessione per lega) o rifiniture report in base alle gare che si risolvono.
8. **Per la prossima sessione** (handoff 2026-09-08): (a) **🎯 PRIORITÀ UTENTE — parità di profondità delle schede partita**: «tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; schede di ogni partita ancora da giocare piene di contenuti, NED1/POR1 attualmente scarse». Diagnosi già misurata sui dati del run `6a31d39`: **classifica assente su TUTTE le leghe** (`espn_standings` vuota per il 403 cronico ESPN → fix = tabella di lega FotMob come fonte primaria) e **stagione xG con 1 sola gara su NED1/POR1** (Understat non le copre; fix = xG di stagione da FotMob); arbitro talvolta assente fino a pochi giorni dalla gara (si riempie da sola). Dettagli e lead tecnici in `BRIEFING_NUOVA_SESSIONE.md` sez. 3; (b) **Monte Carlo stagione** — fase 2 della roadmap `01` §8; (c) **Accuratezza**: seguire le gare NED1/POR1 in programma questa settimana (prime valutazioni Eredivisie/Liga Portugal); (d) opzionale: **pulizia ruff** (47 segnalazioni pre-esistenti, CI non bloccata).
9. **Questa sessione** (`arena/01a08295`): (a) **verifica sito/progetto** ✅ → `docs/05_verifica_sito_progetto.md`; (b) **P0** ✅ (formato arbitro/recupero, attribuzione fonti, ESPN→AVVISO); (c) **P1** ✅ (Open-Meteo, formazione pulita, playoff POR1). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo del meteo Open-Meteo** (run `daily` in Actions: attesa tabella `weather_forecast` popolata e schede «(previsione Open-Meteo)» sui futuri oltre le 48h) e di `future_days=7`; poi: (d) **B5 — collegare o rimuovere il client SofaScore** (codice inerte); (e) roadmap: **M2 diffidati**, **M3 schede giocatore**, **M5 Telegram/xG proprio**; (f) **pulizia ruff E501** (257 pre-esistenti, CI non bloccata).
10. **Questa sessione** (`arena/01a082c6`): **(a) card post-partita «Migliori in campo» per squadra** ✅ (top 3 per squadra con gol/assist/minuti/rating stagionale — avvio Fase 3; verificato su dati reali, suite 64 passed, 0 nuovi errori ruff). **Prossimo**: PR verso `main` (merge utente) → **verifica dal vivo Open-Meteo + `future_days=7`** (primo run `daily` post-merge); poi: (b) proseguire **Fase 3** (schede giocatore vere: radar/percentili/xG per 90); (c) **B5 — SofaScore** (collegare o rimuovere); (d) **pulizia ruff E501/E741** (254 pre-esistenti).
11. **Sessione `arena/01a08613`**: **(a) fix header CalcioMetro ri-applicato** ✅ (PR #19 mergiata).
12. **Questa sessione** (`arena/01a08630`): **(a) card pre-partita «Fatti rilevanti»** ✅; **(b) B5 SofaScore rimosso** ✅ (codice morto). **Prossimo**: PR verso `main` → merge utente; poi Fase 3 schede giocatore, pulizia ruff.
13. **Questa sessione** (`arena/01a0864d`): **(a) Fase 3 schede giocatore** ✅ offline (hub + 7 tabelloni + 3.457 schede con radar/percentili/log; progetto `docs/07`); **(b) backfill finite esteso a tutte le leghe** ✅; **(c) fix i18n `unavailability_type`** ✅. Suite 73 passed, ruff 0 nuove. **Prossimo**: PR verso `main` → merge utente → **verifica dal vivo al primo `daily`** (catch-up `player_stats` 134→~231 partite; poi campioni percentili pieni per lega). Dopo: fase 3b opzionale (`playerData`: contratto, infortuni storici, trofei — rotazione ~80 richieste/run), pulizia ruff E501/E741, M2 diffidati (valutare fonte), fase 5 Telegram.
14. **Questa sessione** (`arena/01a09a79`, dopo il merge): **(a)** primo run `daily` in Actions → verificare che `fda predict --days-ahead 0` preveda **tutte** le gare in programma (~2.000, attese 1-2 minuti per il passo), che al primo run compaia la riga di collasso «previsioni: 3.210 → 138», **quante righe restano senza previsione** con lo storico reale (atteso: molte meno delle 150 offline), che `fda calibrate` produca `n_fit ≥ 1.200` e `bias_lambda < 0,10`, che `history.parquet` compaia in `data/processed` e che in Accuratezza il pareggio dichiarato cada dentro l'intervallo di Wilson con Over 2,5/BTTS entro 2 pp dall'osservato; **(b)** primo run del workflow `lab` (dispatch) → leggere `model_lab.parquet` su storico reale: **si cambia modello solo se** un candidato batte `dc_elo_prod` con IC 95% appaiato interamente negativo e il vantaggio vale in ≥5 leghe su 7; **(c)** P2 di `docs/13`: xG storici cumulati (candidato `dc_xg`), ξ per lega, tabella del laboratorio pubblicata in Accuratezza, dotplot dei punti nelle Proiezioni, lettura in frequenza dell'1X2 in scheda; **(d)** ora misurabile: **RPS per anticipo** sulla pagina Accuratezza (`made_at` vs `utc_kickoff` su tutto il calendario, §9.4 punto 3); **(e)** decisione utente sulla **Tappa 2** (schede per le partite lontane, §9.5).

15. **Questa sessione** (`arena/01a0a1a3`): ⚠️ **PUSH IN SOSPESO** — il commit locale di questa sessione (docs/19 + STATO.md, `git log -1`) **non è stato pushato**: `gh auth status` → «The github.com token in GH_TOKEN is no longer valid». Regola `00` §8bis: il commit resta locale solo se il push fallisce; **la prima azione della prossima sessione è `git push origin arena/01a0a1a3-football-deep-analyzer`** (dopo aver ricollegato GitHub in Arena). Nessun file è andato perso: `docs/19_audit_totale_tre_aree_2026-09-14.md` e questo STATO.md sono nel working tree. **audit totale in tre aree** ✅ → `docs/19_audit_totale_tre_aree_2026-09-14.md` (33 riscontri: 9 P0, 14 P1, 10 P2; ogni punto con Osservato → Misura → Impatto → patch pronta → verifica). Nessuna modifica a codice/dati in questa sessione: solo il documento.
    - **Riscontro principale (nuova misura)**: scaricate le quote di chiusura Pinnacle di **tutte e 7 le leghe × 3 stagioni** dal mirror già usato per NED1/POR1 (`huhao930422-debug/football-odds-mirror`, 21 CSV, verificate via API GitHub). Aggancio al backtest con `fda.teams.canonical()`: **5.543/5.815 (95,3%)**, valutabili **4.372**. **RPS modello 0,19803 vs mercato 0,18843 → Δ +0,00960, IC95% [+0,00794; +0,01126], peggio in 7 leghe su 7.** Diagnosi: sottostima dei favoriti (decile 10: 0,728 previsto vs 0,799 osservato). Oggi `history.parquet` ha quote solo per NED1/POR1 (1.519/7.387) perché `football-data.co.uk` è usato solo come fallback su errore, mai come arricchimento.
    - **Correzione testata e RESPINTA** (da non ripetere): sharpening γ sul rapporto delle λ. Con griglia stretta [0,94;1,12] passerebbe il protocollo (ΔRPS −0,000457, IC interamente negativo, 6/7 leghe); con griglia onesta [1,00;1,40] **non passa** (ΔRPS −0,000417, IC [−0,001037; **+0,000203**], 4/7 leghe) e rompe la calibrazione del pareggio (25,20% vs 25,6% osservato). Da qui il riscontro metodologico §1.4: **la griglia degli iperparametri va pre-registrata nel codice** (`Candidate.grid`), altrimenti "IC<0" si ottiene accorciando la griglia.
    - **Difetti visivi misurati senza browser** (nessun headless installabile in sandbox): tema chiaro da preferenze OS con **7 token mancanti** → `--accent` su bianco **2,15:1**, hover **1,78:1**; **21 coppie** sotto AA nel tema chiaro (valori corretti già calcolati); barre 1X2 che sommano 99/101 in **37/167** schede hero (la funzione `pct_triple` **esiste ed è testata**, è collegata in un solo punto: `build.py:276`); **5 pagine** con barre dei gol a `height:183%`; CSS inline duplicato **144,4 MB su 248,3 MB (58%)** del sito.
    - **Difetti di contenuto/dati**: colonna "Richieste" di *Stato fonti* = contatore **cumulativo** (NED1/POR1 accreditati di chiamate Understat mai fatte); pagina *Accuratezza* con **84 gare di cui solo 3 del modello corrente** (per questo la tabella live e il backtest sembrano contraddirsi); baseline naive hard-coded 45/27/28 che gonfia il Δ fino a **+0,00205**; **34% delle curiosità FotMob scartate in silenzio** (20% delle schede ne mostra 2); etichette arbitro con soglie assolute (POR1 media 5,04 gialli/gara vs soglia "molto severo" 5,0); `verify_site.py` con **11.590 controlli ma zero invarianti di pubblicazione**.
    - **Verificato e chiuso** (non intervenire): xGOT 99,6-99,9% su 7 leghe; parità post-partita **100%** su 6 tabelle; 12.089 `<svg>` tutti accessibili; 0 `nan`/`None`/residui inglesi reali; dotplot, matrice punteggi, in-play, Wilson, backtest già coperti da `verify_site.py`; `PlayerCatalog` 0,3 s (la doppia costruzione **non** è un collo di bottiglia).
    - **Prossimo passo consigliato** (§5 di `docs/19`): applicare **P0.1 + P0.2** (tema chiaro completo + a norma AA) con i due test automatici allegati — è l'unico difetto visivo verificabile **in sandbox** (il contrasto si calcola senza browser), ~10 righe di CSS, non tocca modello né dati. Poi P0.3 (collegare `pct_triple` alle barre) e P0.8 (invarianti di pubblicazione in `verify_site.py`).

## Decisioni aperte
- **Backfill partite finite esteso a tutte le 7 leghe** (deciso 2026-09-09, `arena/01a0864d`, fase 3): prima il backfill completo di stagione riguardava solo NED1/POR1 (per l'xG FotMob); ora vale per tutte (statistiche giocatore complete dall'1ª giornata + report storicizzati). Costo: 97 richieste una tantum (stagione in corso al 9/9), poi ~10/lega a giornata; tetto `--max-backfill=40`/lega/run invariato. In attesa di conferma dal vivo al primo `daily` post-merge.
- **B5 SofaScore — RIMOSSO 2026-09-09** (decisione del 2026-09-09, eseguita in `arena/01a08630` come commit dedicato dopo gli insights). Cancellati `sources/sofascore.py`, `tests/test_sofascore.py` e la voce in `config/sources.yaml`. Il client non era mai collegato a `collect.py`. Reversibile da git (`805bcb5`). Il catalogo in `02` resta come nota (403 da IP datacenter).
- **Qualità delle schede partita** (direttiva utente 2026-09-08, **prioritaria**): tutti i contenuti delle partite devono essere accurati, precisi, profondi e di qualità; le schede di ogni partita da giocare devono essere piene di contenuti e servizi, con **parità completa tra le 7 leghe** (NED1/POR1 segnalate scarse). Primo intervento: classifica da FotMob (ESPN standings 403 cronico) + xG di stagione per NED1/POR1 (Understat non le copre). Registrata anche in `BRIEFING_NUOVA_SESSIONE.md` sez. 3 e 5.
- **Merge delle PR** (deciso 2026-09-08): **il merge lo esegue SEMPRE l'utente, MAI l'agente**. L'agente apre la PR quando serve (sez. D di `00_regole_di_lavoro.md`), monitora i check e avvisa con la frase fissa «👉 Tutto verde: è il momento di fare Merge (PR #N).», poi aspetta senza fare merge. (La decisione era stata registrata nel commit locale `9d39a21` della sessione precedente, mai pushato e andato perso alla chiusura → riapplicata sui docs in questa sessione.)
- **Fonte storica NED1/POR1** (deciso 2026-09-06): mirror GitHub `raw.githubusercontent.com` di football-data.co.uk dedicato (via `datahub_base` per-lega), scelto perché il mirror datahub copre solo 5 leghe e `football-data.co.uk` diretto è irraggiungibile dagli IP cloud.
- Nessuna altra bloccante. (Telegram bot e protezione accesso: fasi successive.)

## Direttive utente persistenti — aggiunta 2026-09-08
- Quote bookmaker: **non interessano all’utente**, non sono requisito editoriale e non vanno prioritarizzate.
- Requisito principale: schede partita complete, accurate, profonde e con parità tra le 7 leghe; nessun dato realmente mancante quando la fonte dovrebbe averlo pubblicato.
- Distinguere sempre `presente`, `atteso dalla fonte` e `mancante`; non inventare dati per riempire campi.
- Fare ricerche approfondite su GitHub e altre fonti prima di scegliere integrazioni; annotare i risultati in `BRIEFING_NUOVA_SESSIONE.md` per gli agenti futuri.
- Implementato audit automatico delle schede future in `src/fda/site/audit.py`, visualizzato in `stato.html`; test suite: 49 passed.
- **Riferimento di mercato come metro esterno** (misurato 2026-09-14, `arena/01a0a1a3`, `docs/19` §1.1): le quote di chiusura Pinnacle di tutte e 7 le leghe sono disponibili sullo **stesso mirror già in uso** per NED1/POR1. Decisione da prendere: **(A, raccomandata)** le quote restano fuori dalla pipeline e diventano solo un benchmark riproducibile in CI (`scripts/benchmark_quote.py` + job mensile) — rischio zero sul modello; **(B)** `datahub_base` esteso alle 5 grandi leghe (5 righe di config) per avere le quote dentro `history.parquet` e nel laboratorio — rompe `tests/test_models.py:79-90` che codifica la decisione opposta e lega il progetto a un repo personale. **Non è in contraddizione con la direttiva utente sulle quote** (non sono un requisito editoriale): qui non si pubblicano quote, si misura la distanza del modello dal livello di mercato.
- **Candidato γ-sharpening RESPINTO** (2026-09-14, `docs/19` §1.3): da non ritestare con griglia stretta. Se si vuole attaccare la sottostima dei favoriti (§1.2), la strada è la **ricalibrazione del vettore 1X2** (temperatura) come candidato del laboratorio con griglia pre-registrata (§1.4, §1.2).
