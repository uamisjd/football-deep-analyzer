# STATO — ARCHIVIO (giri dal tredicesimo al ventesimo, 2026-09-15/16)

> Archiviato il 2026-09-16 dalla sessione `arena/01a0ab67` per la regola A5 (in `STATO.md` erano
> arrivati **10 giri**: la regola chiede di tenere solo gli ultimi 3 + il link all'archivio). La
> verità storica resta qui, identica a come era scritta; in `STATO.md` restano i giri più recenti.
> I giri **fino al 2026-09-15 sera (primo candidato)** sono nell'archivio precedente:
> [`STATO_archivio_2026-09-15.md`](STATO_archivio_2026-09-15.md).

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0aad1`, ventesimo giro) — **P1.14 chiuso** e **due difetti nuovi nei numeri pubblicati corretti** (metodo, misure e coda in `docs/23`). (a) **P1.14 — la rata dei per-90 su pochi minuti era rumore travestito da fatto.** Il prior della contrazione era **180′ fissi** e la media verso cui si contraeva erano le costanti di ruolo dell'**xG+xA** applicate a qualunque statistica: sul sito pubblicato questo valeva **8.705 celle su 54.957** con una stima «stabilizzata» (284 con un grezzo almeno triplo della stima) fino ai casi estremi `90,00 tiri/90` su **1′** giocato e `15,30 xG+xA/90` per un assente con 1′ in stagione. Ora media dei pari e peso **si misurano dal run** in `src/fda/site/rates.py`: media = Σ conteggi / Σ minuti dei pari con ≥270′ (ripiego ≥90′, dichiarato in `Pool.soglia`), peso `k` = **0,25 × mediana dei minuti dei pari** (88-90′ oggi, ~450′ a stagione piena), gruppi (lega, ruolo) → (lega, tutti) → (tutte le leghe) con minimo 8 pari. Regola di pubblicazione: ≥270′ grezzo · 90-270′ grezzo **+ ◎ stima** · <90′ **solo ◇ stima**; i percentili di lega ordinano sulla stima (la pagina lo dichiarava già, ora è vero). `p90_shrunk()` morto eliminato. (b) **Le percentuali erano pubblicate come rate per 90.** «Passaggi riusciti %» e «Duelli vinti %» uscivano grezze sotto i 90′ (`33,3%` su **37′**) con il tooltip che le chiamava **«89,2%/90′»**: per una quota il campione sono gli **eventi** (tentativi, duelli), non i minuti. Ora il denominatore della stima è il numero di eventi (peso 25 tentativi / 30 duelli nei test), la selezione dei pari resta sui minuti, il tooltip dichiara la **frazione esatta** («765 passaggi riusciti su 900 tentati: la percentuale non è una rata per 90 minuti») e i percentili delle quote seguono la stima (2 assi del radar dei difensori erano ordinati per rumore). (c) **`◇` senza numero**: in `match.html` la cella xG+xA/90 poteva stampare il marcatore con il valore reso vuoto da Jinja (0 casi oggi su 241, difetto latente) → terzo ramo `—` con spiegazione, e **`[32]` estesa alle schede partita** (241 celle ◇/◎ verificate). Verifiche: suite **339 passed** (316 a inizio sessione), `fda build` exit 0 **376/2.364/7.478**, `verify_site` exit 0 **nessun problema · 89.448 controlli** (erano 34.144), ruff **173** (baseline 175: 2 in meno, 0 nuove), `audit_match_sections` #13 invariato (media 8,3 righe narrative per scheda, parità fra leghe). **Non verificato**: resa a schermo con browser reale e primo `daily` post-merge. **PR #42 aperta** verso `main` — check `test` **pass**, mergeable **CLEAN**; il merge è di competenza dell'utente (regola D). In coda alla PR è stato chiuso anche un **debito documentale di un'altra sessione**: la deroga di merge di PR #38 non era registrata in `docs/13` — il branch `arena/01a0a9eb` l'aveva scritta come **§9.10** (PR #39), numero che `main` aveva già assegnato alla deroga di PR #35, e la PR #39 è rimasta **CONFLICTING** su `docs/STATO.md` dal 13:07 senza essere mai mergiata. Il contenuto è ora in `docs/13` **§9.11** (rinumerato, con il verificatore post-merge) e la catena delle deroghe si chiude a #38; **PR #39 può essere chiusa come superata** (decisione dell'utente: il merge non è dell'agente).

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0aad1`, diciannovesimo giro) — **Chiuse P1.8, P1.9 e P1.10** di `docs/19` §4 e trovati/corretti **due difetti reali nei numeri pubblicati** (dettagli, tabelle, prove di morso e metodo in `docs/22`). (a) **P1.8, coerenza dei numeri derivati**: 48 schede su 165 (29,1%) pubblicavano una doppia chance che contraddice l'1X2 stampato nella stessa card (506 righe su 2.071 nei Parquet, scarto di 1 punto percentuale; la derivazione nei modelli era già corretta — era la **formattazione**, due arrotondamenti indipendenti) e 88 occorrenze su 330 (26,7%) pubblicavano un totale «a + b (c)» che non era la somma delle due cifre stampate («1,40 + 0,99 → 2,38», grezze 2,3829). Ora ogni derivato si calcola su **ciò che è pubblicato** (`fmt.displayed_sum`/`dec_sum`, filtro `dec_sum` in Jinja), `predict._assert_dc_coerente()` protegge `ensemble()` e `calibrated_prediction()`, e due invarianti nuove del verificatore — **[30]** doppia chance = somma dell'1X2 stampato (e somma 200), **[31]** ogni «a + b (c totali)» chiude e lo stesso totale è identico su tutte le pagine della partita — con **prova di morso**: sul sito pre-fix `verify_site` esce 1 con **301 problemi**. (b) **P1.9, ESPN in backoff**: `espn standings` risponde **HTTP 403 da 76 run consecutivi** (20/20 su `espn:ITA1`; `espn:NEWS` 8) — 14 richieste a run (70 al giorno) per dati già coperti da FotMob/Google News, più 14 righe di avviso identiche in *Stato fonti* che rendevano invisibile qualunque guasto nuovo. Nuovo `src/fda/backoff.py`: stato **derivato dallo storico già versionato** in `source_status` (nessun file di stato da mantenere; la decisione è ricostruibile dal Parquet pubblicato), pausa dopo 5 fallimenti consecutivi e **sonda** ogni 4 run (costo da 14 richieste a run a ~1 al giorno), pill **SOSPESO** dedicata con motivo e ritentativo (token `--s-bg/--s-fg`, contrasto AA 9,07:1 scuro e 6,27:1 chiaro), invariante **[28]** estesa: una riga sospesa deve dichiarare N run e il ritentativo e **non può avere richieste**. Lo scoreboard ESPN, che risponde, resta sempre attivo: la chiave è la coppia (fonte, fase), non il client. (c) **P1.10, il fallback si prova davvero**: Open-Meteo entra solo se FotMob non ha ancora il meteo, quindi poteva restare rotto per settimane senza che nessuno lo sapesse → `scripts/probe_fonti.py` fa **una richiesta vera a settimana** (workflow `lab`, previsione a 24 h sul campo di San Siro), registra l'esito in `data/processed/source_probe.parquet` e la pagina *Stato fonti* pubblica la data dell'ultima prova (oltre 14 giorni dichiara «sonda ferma: il fallback non è verificato»; senza registrazioni dice che non è verificato). Verifiche: suite **316 passed** (+19), `fda build` **376/2.364/7.478**, `verify_site` **0 problemi · 34.144 controlli**, ruff **175** (invariato sul baseline; i file nuovi sono puliti). **Da confermare nel primo daily post-merge** (regola B6): 14 richieste in meno, righe «SOSPESO» al posto degli avvisi, [30]/[31]/[28] verdi in CI e CSS esterno servito da Pages. **Poi in coda**: P1.11 (griglia pre-registrata nel laboratorio), P1.12 (griglia di calibrazione vs bounds), P1.14 (shrinkage per-90), P1.15 (`prossime.html`), P2.

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0aaa1`, diciottesimo giro) — **P1.7 Proiezioni di stagione chiuso** (dettagli in `docs/21` §20). Ricerca UEFA ufficiale 2026/27 registrata: accesso ordinario alla league phase UCL = ITA/ENG/ESP/GER 4, FRA 3, NED 2, POR 1; EPS e vincitrici coppe restano fuori dalla soglia e sono dichiarati. (a) `League.ucl_spots` in config, `simulate_league()` pubblica `top_n`/`p_top_n` e conserva `p_top4` solo come alias di migrazione; la pagina non usa più Top-4 universale. (b) tie-break riproducibile punti → differenza reti → gol fatti → alfabetico, con gol fatti storici passati alla simulazione; scontri diretti non simulati e dichiarati. (c) `NEUTRAL_LAMBDA_BOUNDS=(0,6;2,2)` condiviso fra predict e simulate. (d) `mc_se()` + percentuali intere: 10.000 simulazioni sostengono al massimo ±0,5 punti percentuali di errore standard; nuovo testo di incertezza. (e) card panchina e `verify_site` aggiornati al nuovo contratto, migrazione in lettura degli snapshot legacy marcata come «dato storico». Verifiche: test mirati **61 passed**, suite completa **290 passed**, `fda build` **376/2.364/7.478**, `verify_site` **0 problemi · 32.876 controlli**. Ruff invariato rispetto al baseline; **da confermare nel primo daily dopo il merge** le soglie 3/2/1 rigenerate per FRA1/NED1/POR1. **Poi in coda**: P1.8 doppia chance, P1.9–P1.11 fonti/laboratorio, P1.14–P1.15 e P2.

 2026-09-16 (sessione `arena/01a0a9eb`, sedicesimo giro) — **Confermate al vivo le due verifiche del §17 e svuotata la coda P0** (dettagli e numeri in `docs/21` §18). (a) **`transfers` ha righe vere: 4.230** (imbuto «payload letti 132 · voci viste 4.231 · salvate 4.231», 132/132 squadre) — il parser a disposizioni sovrapposte funziona dal vivo; **invariante [26] attivata** («card mercato riconciliate: 72 pagine», prima vacua). (b) **Card notizie senza doppi** al primo esercizio vero; con le tabelle piene il verificatore ha però mostrato **1 falso positivo di [20]**: lo stesso (url, team_id) raccolto due volte con `published_at` diversi (il feed ripubblica il link con data aggiornata; caso reale `5868080.html`: 07:00 e 01:21) e il controllo guardava solo `iloc[0]` → helper `notizia_in_finestra()` («almeno una riga nella finestra») + test di regressione → **0 problemi · 28.722 controlli**. (c) **Accuratezza onesta**: `outcome_freqs()` sostituisce il 45/27/28 fisso con le **frequenze reali 1·X·2 per lega** da `history.parquet` (7.396 gare; fallback dichiarato sotto le 30; colonna «n base») — Δ Serie A ora −0,031 su base 0,2200 invece di −0,021 gonfiato (P1.1, `docs/19` §1.6); `composizione_campione()` dichiara le versioni nel campione live — **93 gare valutate, solo 12 col modello corrente `dc-elo-tilt-0.4`**, 21 con calibrazione attiva (P0.6, `docs/19` §1.5); nuovo controllo **[3b]** (ogni Δ = RPS − naive ricalcolato; composizione = riga «Tutti»; Σ leghe = «Tutti») che in sviluppo ha morso un doppio conteggio. (d) **P0.5 fatto: CSS esterno con cache-busting — sito 273 MB → 109 MB (−60%)**: i 39.590 byte del design system erano inline in ogni pagina (~10.400 file); ora `assets/site.css` linkato con percorso relativo alla profondità e `?v=<sha256[:10]>`, scritto lazy dal primo `_render`, 14 test di tema ripuntati sul nuovo percorso, controllo nuovo **[29]** (link giusto ovunque, zero `<style>` inline; 4.136 pagine). Verifiche finali: suite **271 passed** (+3), `fda build` 376/2.364/7.490, `verify_site` **0 problemi · 32.867 controlli**, ruff **invariato sul baseline**, preview servita via HTTP (css 200, path corretti). **STATO.md archiviato** (giri 7–12 → `docs/STATO_archivio_2026-09-15.md`, regola A.5). **Da confermare al prossimo daily in Actions**: `verify_site` 0 anche in CI e CSS esterno servito da Pages. **Stesso giro, su richiesta esplicita dell'utente («meglio completare il lavoro prima del merge?», regola D): coda P0 di `docs/19` VUOTA prima del merge** — (e) **P0.9**: `HttpClient.mark()` come unico idioma dei delta, nessuna riga per le fonti non usate (NED1/POR1 non accreditano più Understat) e filtro recenza 48h in `stato.html` (le righe storiche escono di pagina con transizione onesta); test delta fra leghe. (f) **P0.7, decisione A** (quote fuori dalla pipeline, solo riferimento misurato): `scripts/benchmark_quote.py` + workflow mensile `benchmark.yml`; **misura reale riprodotta oggi identica al §1.1** (n=4.372, Δ +0,00960 IC [+0,0079; +0,0112], 0 leghe vinte su 7); raw.githubusercontent.com irraggiungibile dal sandbox → cache popolata via API GitHub (workaround in `docs/21` §18.7), in CI gira sulla rete reale. (g) **P0.8 giudicato coperto** ([9]/[10]/[11]/[11a] + [3b] + [29]). Verifiche finali: suite **276 passed**, `verify_site` **0 problemi · 32.867 controlli**, ruff invariato, build 376/2.364/7.490. **PR #38 aggiornata** — merge solo utente. **Poi in coda**: i P1 di `docs/19` §4 (prossimi: P1.2-P1.5 contenuti, P1.6 SEO/404, P1.13 a11y), i P2 quando capita; da confermare al primo daily post-merge: verify_site 0 in CI, CSS esterno su Pages, righe understat:NED1/POR1 uscite di pagina.


**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0a97c`, quindicesimo giro) — **PR #36 fusa dall'utente** (`f75ea6d`, 09:29Z) → **daily verde** (run `35079677152`, 12m57s, dati `6674adc`, deploy ok): il sito è tornato a muoversi e `news` si è popolata (**5.341 righe**, 132 richieste). Letti i Parquet committati: `transfers` ancora 0 righe ma **con la firma reale** della sezione — attuato il **blocco 4** (`docs/21` §17): ricerca sui tipi Go di `mheers/go-fotmob` (la sezione è `data{"Players in"/"Players out"/"Contract extension"}` + `allTransfers`/`allRumours`/`ourTeamId`, voci con `fromClub(Id)`/`toClub(Id)`/`fee{value,feeText,localizedFeeText}`/`transferType{text}`), parser riscritto a disposizioni sovrapposte (data → allTransfers con direzione da `ourTeamId` → legacy), **rinnovi e voci di mercato mai pubblicati come movimenti** (solo contati), fee/tipi tradotti (mai inglese), whitelist dei nomi di campo estesa agli spazi singoli («Players in»), firma estesa con `campi data`/`campi voce` e disposition nel detail anche con righe > 0. **Card notizie al primo esercizio vero**: `verify_site` segnava **115 problemi** — diagnosi coi dati: **falsi positivi** del verificatore (885 URL su 4.419 raccolti per più squadre: attribuzione per URL invece che per blocco; decimali dentro titoli verbatim della stampa) **più un difetto reale** (116 righe stesso team_id+titolo con URL diversi → 2 pagine con la stessa notizia due volte). Fix: `team_news` dedup per titolo; `[20]` riscritto **per blocchi di squadra** (link esistente per la squadra della card, ≤4, conteggio = dichiarato nell'header); token-checks esclusi dalla card `#notizie` (verbatim). **Prova di morso**: notizia seminata nella card sbagliata → 3 segnalazioni precise; pulito → 0 problemi. Suite **268 passed** (+6), `fda build` 376/2.364/7.490, `verify_site` **0 problemi · 27.501 controlli** (da 115 problemi), ruff invariato sul baseline. **Da confermare al prossimo daily in Actions**: righe vere di `transfers` (imbuto + `transfers.parquet` + invariante [26] che si attiva), e card notizie rigenerata senza doppi. Poi in coda: **P0.5 CSS esterno** (−144 MB) e **baseline naive `45/27/28`** in `build.py:478`.

**Aggiornamento precedente:** 2026-09-16 (sessione `arena/01a0a75d`, quattordicesimo giro) — **Merge di PR #35 eseguito dall'agente su ordine esplicito dell'utente** (deroga regola D; merge commit **`1594170`**, 23:47:17Z) e **primo run post-merge diagnosticato e riparato** (`docs/21` §16). Il daily su `main` (run **`35037211442`**, trigger push) è andato **rosso** nel passo «Run giornaliero»: `deploy` saltato, nessun commit di dati. Diagnosi resa possibile da un **workflow nuovo** (`.github/workflows/diag-fetch-log.yml`) che porta nel repo la coda del log scaricata da un runner — perché il sandbox non raggiunge il blob (EOF) e `gh run rerun`/`dispatch` danno 403. **Causa dal traceback**: `analysis.team_news` leggeva gli indisponibili come righe di tabella mentre `unavailable_for_news` restituisce **nomi** (`TypeError: string indices must be integers`) — difetto **latente** di PR #34, invisibile finché la tabella `news` era vuota (uscita anticipata), scattato al primo run con notizie vere: **il fix del §15.6 ha funzionato** e la card notizie è uscita dal dark launch. Fix di una riga + test di regressione che **fallisce sul codice bacato** e passa col fix; riproduzione locale del percorso attivato (build completa con `news` popolata → card resa, `verify_site` **0 problemi · 26.987 controlli**); dati reali `fda build` 3m12s e `verify_site` **0 problemi · 26.951 controlli**; suite **262 passed** (+1), ruff pulito. **PR #36** preparata (fix + test + strumento di diagnostica + documentazione): da mergiare **subito**, il sito è fermo all'ultimo run verde. **Non verificato**: il run reale successivo al merge (le fonti non sono raggiungibili dal sandbox).

**Aggiornamento precedente:** 2026-09-15 (sessione `arena/01a0a75d`, tredicesimo giro) — **Chiusi i due debiti documentali lasciati dal post-merge.** (a) **Deroga di merge registrata anche in `docs/13` §9.9** (finora solo in STATO, come impone la regola D): PR #34 eseguita dall'agente su ordine esplicito dell'utente, merge commit `bd8a4d5` (2026-09-15T22:29:53Z), 15 commit / 45 file, check GitHub 2/2 SUCCESS; prima del merge suite **246 passed** e `verify_site` **0 problemi · 26.821 controlli**; catena deroghe #23 → #27/#28 → #29 → #34. (b) **Recuperato il commit orfano `07fac55`** (punta di `arena/01a0a5f3`, pushato ma mai entrato in una PR) con `git cherry-pick -x`: ora su questo ramo ci sono il consuntivo post-merge di `docs/21` §14 (run `35031258981`, data commit `a2fbfaf`) e il dodicesimo giro di STATO. (c) **Diagnosi offline delle due fonti a zero righe** (piano in **`docs/21` §15**): letti i Parquet committati, i contatori erano **cumulativi sul client condiviso** (`fotmob:CUPS` 131 → `transfers:TRANSFERS` 263 ⇒ la fase transfers aveva fatto **132 richieste = 1 per squadra**, 132 `team_id` in `fotmob_standings`) e **nessuna voce di errore**: quindi **132 payload su 132 erano arrivati con HTTP 200 e il parser aveva estratto 0 righe** (rete esclusa); per le notizie 139 = 132 RSS + 7 ESPN, tutte HTTP 200 con 0 righe. **Difetto meccanico dimostrato offline**: `news.team_rss_raw` pre-codificava la query e `requests` la ricodificava → URL `q=%2522Ajax%2522%2520calcio` (verificato con `PreparedRequest`) → Google cercava il testo letterale → feed valido con 0 `<item>` → zero righe senza errore. (d) **Blocchi 1+2+3 ATTUATI** (scelta utente «123», dettagli e numeri in `docs/21` §15.6): nuovo modulo **`diagnostics.py`** (`bump`/`key_names`/`shape_of`/`detail`/`digest`, solo nomi di campo, mai valori), `source_status` con **tre colonne nuove** (`rows`, `detail`, `digest`) e **imbuto dei conteggi in tutte e quattro le fasi**, `stato.html` con colonna **«Righe»** e il motivo accanto alla pill per le fonti «OK» con 0 righe, **invariante [28]** in `verify_site.py` (una fonte OK con 0 righe senza spiegazione = problema), **query Google News corretta** (una sola codifica + `Accept` RSS + test di regressione), righe senza data **contate** invece che scartate in silenzio, **contatori per fase** (non più cumulativi: chiude il riscontro di `docs/19` §1.6), `espn news` 403 ora **AVVISO** e non errore, firma dello schema in `parse_transfers`. Verifiche: suite **261 passed** (+15), `fda build` 376/2.364/7.478 in 2m43s, `verify_site` **0 problemi · 26.951 controlli** (`[28] 32 righe`), ruff pulito sul toccato, schema provato su store sintetico (righe vecchie → «—»). **Blocco 4 (parser transfers tollerante) rinviato**: la firma del primo run reale dirà quale forma ha davvero la sezione. **Da confermare in Actions** (l'utente ha scelto il dispatch manuale dopo il merge): righe di `news`, firma di `transfers`, `mercati_monitor` invariato. **Prossimo passo**: merge utente della PR #35 → dispatch manuale del workflow `daily` dalla UI (il token dell'agente dà 403) → l'agente legge dal Parquet committato l'imbuto di `news`/`transfers` e la firma dello schema, poi attua il blocco 4 (o riapre il punto se lo zero ha un'altra causa).

**Aggiornamento precedente (stesso giorno):** 
**Ultimo aggiornamento:** 2026-09-15 (sera, secondo candidato) — **«Dove si colloca questa partita»**: il λ totale della scheda viene letto sulla scala del SUO campionato — nuova card con percentile delle 165 schede: «più alti del X% delle N partite di [lega] fin qui previste dal modello» + media/mediana di lega + etichetta (quartile superiore «partite che promettono più gol», inferiore «partite più chiuse», altrimenti «nella media»). Perché serve: misura campionato per campionato lega BIG-5 2,59-3,00 gol attesi medi vs NED1 3,41 — lo stesso λ=3,0 è 90° percentile in Serie A e mediana in Eredivisie (misura su 2.152 righe predictions stagione 2026/27). Infallibilità: metodo `league_goals_percentile` nello stesso `MatchAnalysis` (regola «conta solo lo stesso campionato» testata col campione-siluro: 80 partite di un'altra lega non toccano n), test dedicato, invariante verify_site **[16]** (ricalcolo da predictions.parquet pagina per pagina: λ, percentile con stessa funzione di arrotondamento Python-round del filtro Jinja, n, media, mediana, nome lega da config) — 165/165 pagine verificate, verify_site **0 problemi · 23.471 controlli**, suite 201 passed, ruff pulito, censo con chiave `posizione_lega` (165). Limite dichiarato nel testo della card: il dato si aggiorna con l'avanzare della stagione perche la distribuzione include partite non ancora giocate.

**Aggiornamento precedente (stesso giorno):** 
**Ultimo aggiornamento:** 2026-09-15 (sera) — giri precedenti archiviati in [`docs/STATO_archivio_2026-09-15.md`](STATO_archivio_2026-09-15.md) (regola A5).
