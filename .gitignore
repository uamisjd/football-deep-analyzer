# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Dati grezzi e cache HTTP: NON versionati (troppo grandi, rigenerabili).
data/raw/
data/cache/
*.log

# Dati elaborati: versionati (data/processed/*.parquet) — vedi docs/03.
# Eccezione: il database DuckDB è una vista derivata dai Parquet, si rigenera in pochi
# secondi (`Store.refresh_views`) e cambierebbe a ogni run (5 commit al giorno): binario
# in history senza informazione. Verificato il 2026-09-17: cancellandolo, `Store` lo
# ricrea con le sue 26 viste. `store.py` lo dichiarava già «non versionato»; lo era.
data/processed/fda.duckdb
# Sito generato
site/_build/

# Segreti
.env

# Artefatti di test e misura locali
.coverage
htmlcov/

# Sito generato da `fda build` (pubblicato via Actions → Pages) — SOLO alla radice,
# non il pacchetto sorgente src/fda/site/
/site/
# Anteprima locale generata da scripts/anteprima_scheda.py
/site_preview/

# Consegna offline di riserva (regola A8bis di docs/00): patch + bundle generati dall'agente
# quando il push è bloccato. Rigenerabili con `git format-patch`/`git bundle`, mai nel repo.
/handover/
